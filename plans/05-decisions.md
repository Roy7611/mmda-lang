# 05 待裁清单（D1–D13）

> 回 `1A 2A 3A` 这种形式即可（**D14 / D15 / D16–D19** 见下）。
> **已裁（✔ 2026-09-26 作者）**：上一轮的 **A**（退役 `~GENERATED PARTS`/`~KEEP PARTS`：生成物整文件覆盖 + 手写 `XxxExt` + 命名约定装配 + `gen --check` 指纹）—— 细则与作者三条原话见 [`06-codegen.md`](06-codegen.md) §1（C1–C6）。 —— 我按结果改 [`01-stack.md`](01-stack.md) / [`02-parser.md`](02-parser.md) / [`03-milestones.md`](03-milestones.md)，并记台账。
> **建议档 = A**（我列的推荐项）。

| # | 问题 | 选项 | 我的建议 | 选了会怎样 |
| --- | --- | --- | --- | --- |
| **D1** | 解析策略 | **A** 手写 lexer + 递归下降 + Pratt ｜ **B** 语法生成器（ANTLR / peg / 组合子） ｜ **C** tree-sitter 主解析 + 语义层 | **A** | A：诊断/错误恢复完全可控，无生成代码债；B：重蹈两次 `.g4` 教训（672 行全废 / 59,020 行在删）；C：高亮好但语义与生成要另写一套，等于两套解析 |
| **D2** | 语法树形态 | **A** 无损 CST + typed AST ｜ **B** 只做 typed AST ｜ **C** 直接上 rowan 红绿树 | **A** | A：注释/CRLF/原样写法都保住，round-trip 可证；B：格式化与"图→文"回写会洗掉现状文件，diff 爆炸；C：成熟但为超大规模增量设计，我们 372 个小文件不值这个复杂度 |
| **D3** | 起点顺序 | **A** 先 CLI + 语料回归，再做 IDE ｜ **B** 先修 IDE | **A** | A：每步都能自动验收；B：Tauri/TS 那层没法自动验收，风险最高收益最低 |
| **D4** | 实现代码放哪 | **A** 新建实现仓（Rust workspace，如 `mmda-kernel`），`mmda-lang` 保持 docs-only ｜ **B** 在 `mmda-lang` 里加 `crates/`（[`../PLAN.md`](../PLAN.md) §2 目录树是这么画的，与 [`../README.md`](../README.md)「不是实现仓」矛盾） ｜ **C** 复活 `E:\Dev\mmda-architect` | **A** | A：真源与实现分离，规范仓继续公开、纯文档；B：要改 README 定位，且规范仓混代码后 review 变重；C：那 1,699 行基本要重写，只留 CLI 命令形态 |
| **D5** | **语料怎么用**（✔ 你 2026-09-26 已指出它是历史逆向产物、语法还在调） | **A** 只作**冒烟 + 漂移报告 + 性能负载**，不进判据（建议） ｜ **B** 当验收基准（"372 文件 0 error"门禁） ｜ **C** 完全不用（不跑） | **A** | A：不锁死正在调整的语法，还顺带产出"哪些文件要跟着改"的**迁移输入**；B：会把错误旧写法冻结成正确性快照，语法一调就红一片；C：失去真实体量输入的冒烟与性能负载 |
| **D6** | 诊断语言 | **A** 稳定英文错误码 + **中文文案** ｜ **B** 纯英文 ｜ **C** 中英双语 | **A** | A：码能进 CI/文档、文案给你看得顺；B：你和团队读起来慢；C：维护两份文案 |
| **D7** | 错误码体系 | **A** `M01xx`–`M07xx` 分段（词法/语法/import/段/唯一性/类型/生成） ｜ **B** 不分段 ｜ **C** 直接用 `doc/` 章节号当码 | **A** | A：一眼知道哪一层、便于按段统计；C：文档章节会动，码跟着飘 |
| **D8** | 首个生成目标 | **A** Java + DDL 先行 ｜ **B** C# ｜ **C** TS | **A** | A：Java 侧是现行底座、编译验证最快；其余按端追加 |
| **D9** | LSP 先接谁 | **A** 先 VS Code（验证协议），再接 Tauri 壳 ｜ **B** 直接 Tauri 壳 | **A** | A：VS Code 是最便宜的 LSP 宿主，协议正确性可先行验证；B：壳和协议一起调，排障翻倍 |
| **D10** | 增量何时做 | **A** M6 单独立项 ｜ **B** 一开始就做 | **A** | A：先对再快，增量等价性可回归；B：解析器与缓存纠缠，出 bug 难定位 |
| **D11** | 外部依赖策略 | **A** 近零依赖（`memchr` / `insta` 级） ｜ **B** 允许 rowan / salsa 等重依赖 ｜ **C** 尽量复用成熟库 | **A** | A：`m-syntax` 可审计、无版本风暴；B：复杂度与调试成本先付；C：与"内核要能被 C#/Java 侧嵌入"的长期目标冲突小，但代码量大 |
| **D12** | `..` 端点包含性 | **A** 取 **C# 语义：`a..b` 左闭右开** ｜ **B** 段范围里 `a..b` 也算闭区间 | **A** | 你说过「这是 C# 的新语法」，A 与之一致；但这会**影响段边界的 1 个数**（`[min..max]` = `[min, max-1]`），所以请你明确一次 |
| **D13** | `plans/` 是否挂指针 | **A** 在 [`../PLAN.md`](../PLAN.md) / [`../README.md`](../README.md) / [`../doc/index.md`](../doc/index.md) 各加一行指针 ｜ **B** 先不挂（本目录独立讨论） | **A** | A：别人（和你将来）找得到；B：不污染现有台账，但容易忘 |
| **D14** | **验收基准从哪来** | **A** 从 `doc/` 逐条提取：每个语法点 1 正例 + `errata` 每条已裁规则 1 用例（建议，命名带规范出处） ｜ **B** 只写端到端集成测试 ｜ **C** 只靠人工验收 | **A** | A：**规范可执行化**，你改 `doc/` 我改用例，度量可追溯；B：出错时无法定位是哪条规则；C：不可回归 |
| **D15** | **语法变更流程** | **A** 改 `doc/` → 加/改用例 → 跑绿（建议） ｜ **B** 先改实现、回头补文档 | **A** | A：真源唯一、变更可追溯（`git log -S` 能查"为什么有这个分支"）；B：文档与实现漂移，正是前两轮翻车的原因 |
| **D16** | **Action 落地形态** | **i** 生成 `class SubmitAction extends ActionBase<ProductionOrder>` 具体子类脚手架（建议） ｜ **ii** 不生成类，泛型装配、需要时再建子类 | **i** | i：同目录附带生成、程序员一打开就知道该改哪；ii：更轻但缺引导，与 C6 原话不符 | ✔ **已裁（2026-09-26）：取 i**（生成具体子类脚手架 + `TODO(mmda):` 提醒） 
| **D17** | **Action 实现类怎么装配进底座** | **A** m 语言声明绑定（如 `action Submit { impl: SubmitAction }`）→ **生成显式装配代码**（建议；零反射、零手写登记） ｜ **B** 生成手写注册类由程序员登记 ｜ **C** 靠反射/扫描注解 | **A** | A：与「java/c# 我不想用反射」一致，删实现类就报错；B：无反射但多一处手写登记；C：与既裁口径冲突 | ✔ **已裁（2026-09-26）：约定优先** —— `action Submit` ⇒ 默认 `SubmitAction`，**不再显式指定**；生成器出显式装配代码；缺实现类 = `mmda check` error 
| **D18** | **C# 手写分片命名** | **A** 统一 `Xxx.Custom.cs`（建议） ｜ **B** 维持现状（239 个分片无约定） | **A** | A：C# 已经是"生成区 + 手写分片"事实，命名一统一就彻底告别保留岛；B：等于留着一个不可校验的 KEEP 区 | ✔ **已裁（2026-09-26）：目录化 + 两端统一** —— 每实体一目录 `Service.java` / `SubmitAction.java` / `ServiceExt.java`；C# 同构 `Service.cs` / `ServiceExt.cs`（partial）；旧扁平命名作废 
| **D19** | **m 内联钩子的求值分期** | **A** 首版只吃**声明式子集**，内核求值通道（M8 / P6）就绪后开完整内联（建议） ｜ **B** 内核求值提前到与解析器并行 ｜ **C** 先把 m 内联体**转译成 Java/C#** | **A** | A：不阻塞 M4/M5；B：拖慢主线但更早兑现"IDE 里写"；C：**两套实现必漂移**，与 Rust 单实现初衷相反（不建议） | ✔ **已裁（2026-09-26）：A**（首版声明式子集，内核通道就绪后开完整内联） 
| **D20** | **`XxxExt`（服务级增量）还要不要** —— 你的点 1/2 已覆盖大部分定制（CRUD 走 m、钩子走 m 内联、重逻辑走 `IAction`） | **A** 不要 `XxxExt`：定制口只有「m 内联钩子 + `IAction` 实现类」两个，**再少一个概念**（建议） ｜ **B** 保留 `XxxExt`（给"给生成的服务加个辅助方法"这类需求） | **A** | A：载体越少越好、装配点唯一；B：多一个"必须靠命名约定才被发现"的隐式契约 | ✔ **已裁（2026-09-26）：A**（`Ext` 大部分不需要，保留 `ServiceExt` 给少数场景） 
| **D21** | **租户与插件的绑定** | **A** 清单加 `scope: global｜tenant` + `tenants: [...]`（保守缺省 = `tenant`）（建议） ｜ **B** 不写进清单，装载时由运维配置指定 ｜ **C** 不做租户维度（只有全局插件） | **A** | A：绑定关系随包走、可评审、可回滚；B：绑定散在环境里，容易漂移；C：等于放弃"租户独有"这个需求 |
| **D22** | **租户插件的数据模型** | **A** 表**租户共享**（靠租户键隔离）+ 结构差异走扩展表 `xxx_ext`（建议） ｜ **B** 每租户独立表 ｜ **C** 插件自带独立 schema/库 | **A** | A：零额外成本、与 `BIGID` 租户键天然一致；B：表爆炸、迁移地狱；C：与"共享表 + 租户键"的地基冲突 |
| **D23** | **卸载语义** | **A** 首版受信插件只承诺**停用**（数据保留），WASM 档支持**真卸载**；`purge` 显式不可逆 + 审计（建议） ｜ **B** 一律只停用 ｜ **C** 一律要求可真卸载 | **A** | A：与 Java `URLClassLoader` / C# ALC 的真实卸载能力对齐（不许愿）；B：WASM 档白白浪费；C：做不到或代价极高（类卸载泄漏） |
| **D24** | **插件新增实体的段** | **A** 清单声明 `segments` + **装载期校验与既有段/已装插件段不重叠**，冲突即拒载（建议） ｜ **B** 插件不领段、共用默认段 ｜ **C** 装载期自动分配 | **A** | A：把插件纳入既有**段硬门禁**；B：UNION 视图族会撞主键；C：自动分配破坏"段由人分配"的既裁口径 |
| **D25** | **谁有权装卸** | **A** 平台（交付方）装卸/升级 + **租户管理员只能开关自己租户的插件**（都进审计）（建议） ｜ **B** 租户自助上传（需市场 + 签名链，属暂缓项） | **A** | A：与"市场与收费暂缓"的现状一致，且安全边界清楚；B：眼下没有市场与签名链，等于开后门 |
| **D26** | **内核实现语言**（作者新想法：在 C#/Java 之上弄解析语言是否更容易） | **A** 维持 Rust 单实现，靠 spike + 声明式子集提速（建议） ｜ **B** 先做 spike（宿主语言快速原型，产品仍 Rust） ｜ **C** 改宿主实现（需同时接受三条让步：放弃三端一致 / 接受可反编译 / 语言服务与生成器不共享语法树） | **A** | A/B 可叠加：B 只是提速手段、不改产品架构；C 会作废 `README` 的"Rust 单实现"与 `PLAN.md`:63 的"不许双实现"，见 [`08-kernel-language.md`](08-kernel-language.md) |
| **D27** | **LLVM / MLIR 是否进产品依赖**（作者 2026-09-26 提问） | **A** 不进依赖：**抄它的做法**（分层 IR / verifier / LangRef / lit / 诊断），需要 native 时用 **Rust（rustc 的 LLVM 后端）**、需要 JIT 时用 **wasmtime（Cranelift）**（建议） ｜ **B** 引 **MLIR** 做多级 lowering ｜ **C** 全量引 LLVM 做 AOT/JIT | **A** | A：零额外构建链，好处已在手（Rust + WASM 两面）；B/C：锁 LLVM 大版本（半年一大版）+ C++ 构建链 + 巨量体积，而我们的产物是**源码**不是机器码（[`08-kernel-language.md`](08-kernel-language.md) §7.1） |
| **D28** | **定位与术语**（作者 2026-09-26：「我们的定位是 m 语言的 transpiler 对吗？」） | **A** 主词 = **编译器 + 代码生成器**（对外可称「**元数据编译器**」），`transpile` 只用于描述后端那一步（建议） ｜ **B** 定位即 **transpiler** ｜ **C** 只叫**代码生成器**（沿用 `mmda-factory` 的旧词） | **A** | A：与既有口径一致（[`../PLAN.md`](../PLAN.md):15 / :28 / :54），且不丢校验/IDE/元数据产物这些输出面；B：窄且带"模板替换"贬义；C：漏掉前端校验与语言服务（[`01-stack.md`](01-stack.md) §0） |
| **D29** | **细粒度增量（红绿树）的启用门槛**（作者 2026-09-26：「为什么现在不上红绿树，出于什么考虑？」） | **A** 四条判据全中才上：① 全量解析 P95 > 200 ms 且解析是瓶颈 ② 键入→诊断 > 150 ms 且防抖+取消救不回 ③ 规模到数千～万级文件 ④ 文件级增量仍不够（建议） ｜ **B** 现在就上 `rowan` + salsa ｜ **C** 永不 | **A** | A：批处理阶段绿树收益 0、成本全价；出事症状是"补全偶尔给旧结果"（不可复现）；语法调整期锁死内存表示最亏。接口已同形（`TextRange` + 无损 CST），将来换实现只动 `m-syntax` 内部 —— 现在不做**不算欠账**（[`02-parser.md`](02-parser.md) §6） |

## 明确不在本轮范围（不用回）

- 语言语义与规范内容的问题 → 一律回 `doc/` / `errata` 裁决，不在 `plans/` 定。
- 那 372 个文件的具体写法要不要修（迁移）→ 属**迁移工具**议题，等漂移报告出来再谈。
- `PLAN.md` 的 P0–P9 阶段表本身要不要改 → 等 M 与 P 的映射跑一两个里程碑再谈。
- IDE 的功能清单（跳转/图形/插件）→ 属 M7，届时单独开一份 `plans/06-ide.md`。

## 相关

[`README.md`](README.md) · [`01-stack.md`](01-stack.md) · [`02-parser.md`](02-parser.md) · [`03-milestones.md`](03-milestones.md) · [`04-verification.md`](04-verification.md)
