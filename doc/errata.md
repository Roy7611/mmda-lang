# 待裁决口径与校勘记录

> 本文件记录**已知冲突**与**未裁决事项**。写解析器、生成器、IDE 之前先看这里；
> 每条给出证据位置、影响面与可选方案，但**不代替决策**。

---

## 一、五处口径冲突（2026-09-24 实测）

前置事实：文档有两批来源——**早期文档**（2026-06-16~18，即本目录根文档的前身）与**实际语料**（2026-06-27~29 用 `tools/reverse_mmda_project.py` 从 `mmda_metadata` 库反向生成的 `E:\Dev\mmda-architect\examples\mmda-mes`，381 个文件）。两者已经分叉。

### 冲突 1：字段关系/约束的语法两套

| 形态 | 早期文档 | 实际语料 |
| --- | --- | --- |
| 引用 | `ref Partner as customer`（`doc/readme.md:64`）、`indexed`、`unique`、`computed` | `@Ref Country(countryCode,fullName)`（`examples/mmda-mes/data/models/base/Address.mm`）、`@One`、`@Many`、`@Computed`、`@Index`、`@Id` |
| 计数（381 文件） | — | `@Ref` 90、`@Many` 48、`@Computed` 28、`@State` 41、`@Index` 117、`@Id` 82；而 `ref ` **0**、`computed` **0**、`@Action` **0**、`#ge(` **0** |

**影响面**：解析器输入、IDE 补全、AI 提示词、381 文件能否当回归集。
**可选方案**：(a) 以语料形态为基线，早期文档标注历史；(b) 以早期文档为基线，重跑反向导出；(c) 两套都支持并定弃用时间表（成本最高，最不推荐）。

> **✔ 已裁（2026-09-25，取语料形态）**：**以 `@` 注解制为基线** —— `@Ref Country(countryCode,fullName)`、`@One` / `@Many`、`@Computed`、`@Index`、`@Id`、`@State`；
> 早期文档的 `ref X as y` / `indexed` / `unique` / `computed` 写法**标为历史、不进解析器**。
>
> 理由：**381 文件即回归集、零迁移成本**（实测 `ref ` 与 `computed` 零命中，`@Ref` 90 / `@Many` 48 / `@Index` 117 / `@Id` 82 / `@Computed` 28 / `@State` 41）；
> `@` 也是 Java / C# 程序员熟悉的形态。**P2 语法基线闸门由此解除**（`PLAN.md` §6.1）。落点：[`records.md`](records.md) 顶部、`PLAN.md` §6.3-39。

### 冲突 2：`.ma` 的正文形态

- 文档：`doc/project.md`（原 `architect/project-format.md:125`）称「所有 `.ma`–`.mi` 文件正文为 M 语言方言」，`:201` 给出 `subsystem mes in Erp { module M.03 … }` 示例。
- 实际：`examples/mmda-mes/biz/mes.ma` 是 **JSON**（`{"name":"Mes","$schema":"https://mmda.dev/schemas/ma-module/v1",…}`）。

**影响面**：解析器分派、编辑器语言模式、`biz/` 的编辑体验。
**可选方案**：(a) `.ma` 保持 JSON（结构化、好程序化修改，但架构师手写体验差）；(b) 改为 M 语言 DSL（与文档一致，但要迁移现有文件）。

> **✔ 已裁（2026-09-25，取语料形态）**：**`.ma` 正文 = JSON**（模块树，设计器产出，`$schema = …/ma-module/v1`）；
> **`.mm` / `.me` / `.ms` / `.mi` = M 语言文本**。实测（`E:\Dev\mmda-architect\examples\mmda-mes`，共 378 个语言文件）：
> `.mm` **215 / 全文本**、`.me` **113 / 全文本**、`.ms` **41 / 全文本**、`.mi` **1 / 文本**（`ui BomList for Bom { index: BomList }`）；
> `.ma` **2 / 全 JSON**、`*.g`（图布局）与 `.mmda`（清单）同属 JSON。
>
> **判据：人写与评审的走 M 语言文本（可 diff、可图形编辑）；设计器产出的结构性文件走 JSON。** 落点：[`project.md`](project.md) §1.2、[`ide/README.md`](ide/README.md)、`PLAN.md` §6.3-39。

### 冲突 3：`.mmda` 后缀的两种含义

- 现状：`.mmda` = **项目清单 JSON**（根目录唯一 `{projectCode}.mmda`），语言分片是 `.mm / .me / .ms / .mr / .mc / .mf / .mi`。
- 决策台账 B8：语言文件后缀 **`*.mmda`**。

**影响面**：文件关联、图标、LSP 语言 id、归档包（`.mmdax`）命名、IDE 项目识别。
**可选方案**：(a) 统一 `.mmda`，清单改名（如 `project.json`）；(b) 保持扩展名族，`.mmda` 只做清单；(c) `.mmda` 用于语言文件、`.mmdaproj` 用于清单。

> **已裁决（2026-09-24）：选 (b)**，并补一条与扩展名无关的前置规则——你在 2026-09-24 明确同意「保族 + 解析器按内容首关键字判 partType」。详细清单见 [`contracts-inventory.md`](contracts-inventory.md) §8。
>
> 1. **保持扩展名族**：`.mmda` 继续只表示项目清单。
> 2. **解析器按内容首关键字判定 partType**（`record` / `enum` / `stm` / `ui` / `view`），扩展名仅作约定、图标与文件关联提示。→ 将来真要统一扩展名，解析器一行都不用改，把不可逆决定变成可逆的。
>
> 理由：扩展名之争其实是**文件粒度**之争的影子。你要求「细粒度版本控制 + git/svn 集成」→ 需要**一对象一文件** → 扩展名族正好表达类型（`Order.mm`、`OrderStatus.me`、`BomApproval.ms`）；只有改成「一功能一文件」（record + STM + UI + 枚举装一个文件，像 `.csproj`/`.resx`）时，统一 `.mmda` 才划算。而你的产品是「图形为主、脚本为辅」，粒度应保持对象级。迁移成本对比：保族 = 0（381 文件语料、`crates/mmda-core/src/lang.rs` 的分派、`package.rs` 的 pack/unpack 全不动）；统一 = 380 个文件重命名 + Rust 分派 + VS Code 关联 + 图标 + CI + 文档 + 归档内路径。

### 冲突 4：动作与状态转移的形态

| 形态 | 早期文档 | 实际语料 |
| --- | --- | --- |
| 行为 | `@Action 付款:给新订单付款` + `pay(NEW->PAYED)` 写在 record 内 | `data/stms/mes/BomApproval.ms`：`stm BomApproval on Bom.status { action approve { transition CERTIFIED->APPROVED, } }` |

**影响面**：STM 与 Record 的耦合方式、`data/stms/*.ms` 是否保留、IDE 状态图编辑。
**可选方案**：(a) 保留 `.ms` 独立文件（行为与结构解耦，便于图形编辑）；(b) 行为内联进 record（一处看全，但大表会很长）。

> **✔ 已裁（2026-09-25，取语料形态）**：**行为住独立 `.ms` 文件**（`data/stms/<模块>/<对象>.ms`：`stm BomApproval on Bom.status { action approve { transition CERTIFIED->APPROVED } }`），
> **不内联进 record**；早期文档的 `@Action 付款:给新订单付款` + `pay(NEW->PAYED)` 内联形态**标为历史**。
>
> 理由：与「一对象一文件 + 细粒度版本控制」一致，状态图可单独图形编辑；语料已是此形态（41 个 `.ms`）、零迁移。落点：[`statements.md`](statements.md) §5、`PLAN.md` §6.3-39。

### 冲突 5：字符串长度与可空的位置

| 写法 | 出处 |
| --- | --- |
| `varchar?[30]`、`char[11]` | `doc/readme.md:39`、`:41` |
| `varchar(15)`、`nvarchar(255)` | `doc/readme.md:61`、`:27` |
| `varchar?(30)` | `archive/2026-06/language/records.md:19` |
| `varchar(80)`（语料实际） | `examples/mmda-mes/data/models/base/Address.mm` |

**影响面**：词法/语法层最底层的选择，影响所有字段。
**建议**：长度一律 `type(size)`，可空一律尾部 `?`（`varchar(80)?`）——与语料现状最接近，改动最小。**仍需你确认**。

> **✔ 已裁（2026-09-25，取 `type(size)` + 尾 `?`）**：`varchar(80)?`、`char(11)?`、`decimal(18,3)?` —— **容量与可空各司其职**（`(size)` 管容量、`?` 管可空，不混进方括号）；
> 早期文档的 `varchar?[30]` / `char[11]` / `varchar?(30)` **标为历史、不进词法器**。落点：[`datatypes.md`](datatypes.md) §2.1、`PLAN.md` §6.3-39。

---

## 二、语法专题待裁决清单（另开专题讨论）

早期文档形态内部还有这些不一致，逐个专题时要一并裁掉：

> ⚠️ **位置列校正（2026-09-25）**：`doc/readme.md` 已重写为《m 语言规范（总览）》，原表里指向 `doc/readme.md:39 / :41 / :64 / :72 / :75 / :76 / :84 / :106 / :115 / :124 / :137 / :158 / :186` 的行号**全部失效**（第 41 行已是空行、第 76 行是 `CANCELED = 4`）。本表各行的出处**已逐条实测重新定位**到早期语言示例的真实位置：`doc/archive/2026-06/language/{records,types,expressions,behaviors,overview}.md` 与 `doc/archive/2026-06/architect/meta-model.md`。

| # | 冲突 | 位置 |
| --- | --- | --- |
| 1 | ~~命名约束 `indexed/unique/positive/future/cancellable` 未定义时静默通过（灰区）~~ → **✔ 已裁（2026-09-25，作者纠正助手）**：**约束按层级分两种形态** —— **① 字段级 = 跟在字段行尾的裸关键字**（`mobile char(11) indexed,`、`orderDate date default now indexed future,`、`addressId uint64 identity generated readonly,`；**简洁**）；**② 对象级 / 组合 = 写在 record 体末尾、单独具名声明**（**两个及以上字段建一个索引必须走这条**：`@Index IDX_flowtrails(objName,objId,actTime)`、`@ForeignKey`、`@Check`）。**语料实测**：行尾 **`readonly` 1040 / `indexed` 491** 处；具名声明 **342** 处，其中 **2 列及以上 45** 处（2 列 32 / 3 列 12 / 4 列 1）。**本轮撤掉助手上一条的「约束一律走注解」**。**✔ 未知名 = 解析期 error（2026-09-25 作者）**：作者原话「**posittive 写错了肯定要报错，语法都不对，你咋能过。你没有词法分析器吗？**」——**两套名字表（行尾关键字 + 注解名）都进词法分析器的 token 表，拼错 / 未知名 → 解析期报错**；**扩展白名单方案（`Profile` / `customProperties`）作废**（没有「自定义关键字」这回事）；解析器 = **P2 `mmda-syntax`**（仓里尚未实现，故现阶段无东西可拦） | [`records.md`](records.md) §1.2 / §3、[`design-notes.md`](design-notes.md) §7.1、[`archive/2026-06/language/records.md`](archive/2026-06/language/records.md) `:22-23` / `:36-38` |
| 2 | `@Computed` 的位置与载荷不一致（注解自带公式 vs 行尾注释 vs constraint） | `doc/archive/2026-06/language/behaviors.md:70`、`records.md:127`、`architect/meta-model.md:134` |
| 3 | `[+]` / `[*]` 集合基数与 `type[]` 数组语法会撞 | `doc/archive/2026-06/language/records.md:54` / `:60-61` / `:73`、`overview.md:46` |
| 4 | 文档标签写进语法：`@Action 付款:给新订单付款`；建议一律走 `///` 文档注释 | `doc/archive/2026-06/language/behaviors.md:27` |
| 5 | `b0000` 位字面量与 BitSet 宽度：定宽？加成员是否变更存储宽度？ | `doc/archive/2026-06/language/records.md:156` |
| 6 | ~~**`BIGID` 是错字还是别名**~~ → **✔ 已裁（2026-09-25）：都不是 —— `BIGID` 是自有类型**：**本身即 partitionID（分区主键）**。**位布局同日补定**（作者给的常量 + 底座源码实测）：**高 28 位 = tenantId（`MAX_TENANT_ID = 0x7FF_FFFF`，bit 63 恒 0）、低 36 位 = realId（`MAX_REAL_ID = 0xF_FFFF_FFFF`）**；解析 `partitionId >>> 36`；`NO_TENANT_ID = 0`（平台公共数据）、`MIN_TENANT_ID = 1`；**realId 由分布式唯一 ID 生成、由底座 `buildEntityID` 合成**（不是 DB 生成）；组合主键第一段即 partitionId（`"partitionId.xxx"`）；真源 `D:\2026\java` 的 `Tenancy.java:15-19 / 42-44 / 85-103`。**✔ 27 位已确认（2026-09-25，作者原话：「27位没错」）**：**高 28 位字段中 27 位有效、bit 63 保留恒 0**（ID 恒为正 `long`），**不是笔误**。**同轮实测补正**：**语料里没有 `BIGID` 这个 token**（case-insensitive **0 命中**，该名字只在早期文档）——语料形态是 `@PartitionID [10000,0x000F_FFFF]` 独占一行（字段级注解）+ 下一行 `addressId uint64 identity generated readonly,`（`uint64 identity` **102 处**、`identity generated` 16 处）；`[min,max]` = **该对象在 realId 空间领的区间**（186 文件同值），与 `MetaObject.minID`/`maxID` 一一对应。**✔ 剩余两条同轮裁定（2026-09-25 作者）**：① **`BIGID` 保留为语言类型，等价于 `uint64 identity partitioned`** —— `partitioned` = 元对象的**物理表分区标记**；**分段在 `MetaObject` 上配置 `minId` / `maxId`**，取值是 **真实 id（realId —— 去掉租户标识之后的那部分）范围** → 见 §五-45；② **类型名与约束名一律大小写不敏感**（作者原话：「**类型一律大小写不敏感，包括那些约束，这个跟 SQL 类似**」）→ 见 §五-46。**§二-6 至此清零**。另注：`doc/archive/2026-06/` 里仍留「高 16 位 / 低 48 位」**存档原文不动**（历史留档，只在活文档改正）。 | 作者原话（2026-09-25）；[`datatypes.md`](datatypes.md) §5、[`records.md`](records.md) §2.4、[`glossary.md`](glossary.md) §5 |
| 7 | `as` 三种词义：字段别名 / join 别名 / 类型转换 | `doc/archive/2026-06/language/records.md:71-72`（`@One … as alias`）与 `:221-223`（`view … : X as it` / `join … as o`） |
| 8 | 无符号两套写法：`decimal(19,4) unsigned` vs `uint64` | `doc/readme.md:76` vs `doc/datatypes.md:18` |
| 9 | 约束表达式 `#ge(0)` / `#(d{11})` 与命名约束 `positive` / `future` 是两套机制还是同义？语料里 `#ge(` 零命中 | `archive/2026-06/language/expressions.md:71` 与 `doc/readme.md:115` |
| 10 | 自然语言式聚合 `sum(amount of each items)` 的文法边界（`of`/`each` 无词法标记） | `doc/archive/2026-06/language/records.md:127` |
| 11 | **重计算/算法节点如何声明为 native**：`algorithm` 契约、在行为里怎么引用（`@Action dispatch : X` / `@Native(...)` / `.ms` 内），以及 `protection` / `license` 声明是否进语言 | 草案见 [`protection.md`](protection.md) §5 |
| 12 | **`.mt` 用例语法形态**：`given/when/expect` 是否够、是否复用 M语言 表达式语法、`covers`/`source`/`reviewedBy`/`baseline` 的写法 | 已裁进语言族（§五-6）；草案见 [`testing.md`](testing.md) §9 |
| 13 | **拦截器 / 钩子这类「纯脚本」是否用 `.m` 扩展名**：作者原话「**我是想那些拦截器，是不是纯脚本程序阿，用 `*.m`**」——现状：规范里**独立文件只有模型分片族**（`.mm` / `.me` / `.ms` / …），脚本只有**内嵌脚本块**（[`runtime.md`](runtime.md) §4.5/§4.6，引擎取 A）；「拦截器」在规范仓仅出现 1 处（[`runtime.md`](runtime.md):57 讲架构图画法：事务/业务逻辑同块、上下夹 Before/After 拦截器），真正在用的是**钩子**（Service 层业务钩子 / `store` 级 SQL 整形钩子），已裁口径是**钩子体 = 程序员用 Java / C# / TS 写**（KEEP 区 + 插件）。**待裁两点**：① 是否给**纯脚本文件**一个独立扩展名 `.m`（判据：正文判不出 partType —— 不是 `record`/`enum`/`view`/`stm`，而是语句序列）；② 若给，拦截器脚本放哪（`flow/interceptors/` 独立目录 / 与对象同目录 `<对象>.m`）。**说明**：助手上一轮把作者这句误读为「整个扩展名族收敛为 `.m`」并落盘，已由 **`df4dc99`（revert `d67fe7e`）整体撤销**，分片族原样保留；本行才是真问题 | [`project.md`](project.md) §1、[`runtime.md`](runtime.md) §4、`doc/ide/plugins.md` |

---

## 三、工程与架构待裁决

- 见 [`..\PLAN.md`](..\PLAN.md) §6.2（仓库关系、`.ma` 形态、留档处置、svn 集成、旧库连接、`generated/` 与 KEEP 区边界、MCP 面、C# 仓的 SVN+嵌套 git）。
- 逐行对照清单：[`contracts-inventory.md`](contracts-inventory.md)（P0.5 交付物，60+ 概念带 `file:line`）。
- **2026-09-24 随想录带来的范围问题**（逐条给出处，建议见各自落点）：

| # | 待裁 | 出处 |
| --- | --- | --- |
| 9 | ~~**设计器宿主形态**：独立桌面壳（Tauri + Vue，现规格）vs VS Code / IDEA 插件（随想录要的）vs 混合~~ → **✔ 已裁（2026-09-24）独立壳优先**，插件列为后续可选宿主 | [`ide/specification.md`](ide/specification.md) §4.9 |
| 10 | ~~**组织架构 / 岗位 / 职员清单**：是**数据**还是**元数据**？~~ → **✔ 已裁（2026-09-24）是「数据」**；**同轮补裁：Role（关键用户）是语言元素、与 Module 同级 → 见 §五-12**（余：岗位 → Role 的映射规则待裁） | 同上 §4.8 域 2、[`meta-model.md`](meta-model.md) §8.1 |
| 11 | **层级菜单**：是**声明**（进模型）还是**投影**（由模块树/权限生成） | 同上 §4.8 域 1 |
| 12 | **首页过滤器与展现器**（Index / Report / Dashboard / CRUD）与现有**五视图**是不是同一件事，谁是主人 | 同上 §4.8 域 5 |
| 13 | **局部（模块级）ER 与全局 ER** 的合并规则（随想录要求"针对模块小范围定义局部数据模型"） | 同上 §4.8 域 3 |
| 14 | ~~**报表与 BI 的归属**：语言核心 / IDE 工具面 / 底座能力？~~ → **✔ 方向已裁（2026-09-24）：BI 要有元数据、架构设计师要能建模**（属元模型）；**`MetaBiCube` 不成熟 → 具体形态与是否移植容后再议**。余：**ClickHouse** 是否默认分析存储、枚举→维度与固定维度模型的生成规则 | 同上 §4.8 域 8；[`contracts-inventory.md`](contracts-inventory.md) §5 报表行 |
| 15 | **UI 配色与 ColorRole** 是否进元数据（设计器级主题 vs 模型级声明） | 同上 §4.8 域 7 |
| 16 | **BPMN XML 互转**（与外部 BPMN 工具集成） | 同上 §4.8 域 6；[`design-notes.md`](design-notes.md) 流程架构一节 |
| 17 | **发布链路**：生成 → 编译 → 打包 → 部署与 CI/DevOps 对接的形态（目前只有 `mmda pack` 与生成器） | 同上 §4.8 域 9 |
| 18 | **需求条目与用例的语法形态**：`REQ-x` 进语言还是 IDE 侧清单；`UseCase` 是否上图形；**SRS 导出形态与基线冻结**；需求覆盖门禁是否分层次 | [`requirements.md`](requirements.md) §6 / §7 |
| 19 | **绘图工具的缺口**：表单设计器、BI 看图器、脚本编辑器 | [`ide/specification.md`](ide/specification.md) §4.8 域 11 |
| 20 | ~~**拦截点的语言声明形态**：挂在 Action / 视图上还是独立 `hooks` 段；生命周期点是否**封闭枚举**（不许自定义名）；`save` 是否合并 insert/update；存储级钩子是否对设计师开放~~ → **✔ 全部已裁（2026-09-24，§五-31；其中 `store` 钩子于同日补裁取 A = 不开放）**：挂点 = **Action / 视图**（不新造 `hooks` 块）；生命周期点 **封闭枚举**、**不要新增**；`save` **语言语义合并但服务端保留 `insert`/`update`**；**Action 的准入走 `canDo`**（三端统一、支持 m 语言写逻辑，Java/C# 不用反射）；**仅「`store` 钩子是否对设计师开放」仍待裁**（§9-4，作者回「不明白」→ 解释见 [`runtime.md`](runtime.md) §4.4） | [`runtime.md`](runtime.md) §9-1…3 / §4.2 / §4.4 |
| 21 | ~~**事务传播规则**：跨模块调用时事务怎么传（同模块同事务、跨模块新事务？）；与事件补偿的配合~~ → **✔ 已裁（2026-09-24，§五-31 ⑦）**：**默认同模块同事务、跨模块新事务**；跨模块一致性走事件补偿；**2PC / 分布式事务属具体实现**（不在语言层承诺） | [`runtime.md`](runtime.md) §9-7 / §3 |
| 22 | ~~**`EntityFactory` 与 `Repository` 的关系**~~ → **✔ 已裁（2026-09-24，§五-31 ⑥）**：**两个都要、分层不替换**——`EntityFactory` 更像 .NET EF（出对象 / 组装查询），`Repository` **可基于 `EntityFactory` 实现**（javaer 更习惯）；**硬目标 = 消灭手写 SQL 语句与手写字符串字段名**。原问题：新 Java 代码引入 `EntityFactory`（`mmda-core-entities` + `mmda-base-repository/BaseEntityFactory`），与 core 的 `Repository` 并存——是替换还是分层？ | [`runtime.md`](runtime.md) §9-6 / §6 / §8、[`contracts-inventory.md`](contracts-inventory.md) 持久化行 |
| 23 | ~~**API 契约的落法**~~ → **✔ 已裁（2026-09-24，作者逐条取定：1–9 依建议、12 = `moduleName_featureName_op`、13 = 按现状模块权限 + Action 权限；**⑭ 亦已裁（取 B：首版不带 `webhooks`）**）——结果入 §五-30**（**语言层面怎么定义 API + 对接 YApi/Apifox/Swagger**）：① 暴露边界默认值；② 路径推导规则（`/模块路径/资源`？）与命名规范；③ 稳定度标记语法与 `since`/`sunset`；④ OpenAPI 版本与扩展字段白名单；⑤ 命令面（`mmda api export/diff/check`）；⑥ Mock 归属；⑦ 网关与 Controller 的认证分工；⑧ 逆向导入的产出形态 | [`api.md`](api.md) **§8.2（11 条，全部已裁）**——原 16 条中 ⑩ 存量路径兼容、⑪ `decimal`/`int64`/`Timestamp` 序列化、⑮ 官方 Schema 校验进门禁、⑯ AI 造数固化 **已于 2026-09-24 裁掉**（见本文件 §五-16）；其余 ⑨ 基础模块固定名与 `sops` 映射、⑫ `operationId` 命名、⑬ scope 命名与粒度、⑭ `webhooks` 首版范围 **同日全部裁定（见本文件 §五-30）** |
| 24 | **愿景 5 条已全部裁决**（✔ 2026-09-24）：~~④ L2 矩阵~~ → §五-18；~~⑤ 共赢/插件市场~~ → §五-19；~~① 许可（MIT vs CC-BY-4.0）~~、~~② 移动端宿主~~、~~③ OWASP ASVS~~ → §五-20（**MIT / Flutter / ASVS 进门禁**）。余：**龙芯（LoongArch）何时纳入生产验收**、**申威是否需要**（等客户点名再开） | [`vision.md`](vision.md) **§8**（余为新开的 2 条：`MetaUi` 第二渲染方、移动端是否进首版） |
| 25 | **插件市场与内核侧插件**（本轮新开）：① **内核侧插件的加载形态**——sidecar 子进程 + JSON-RPC / WASM（wasmtime）/ Rust dylib 三选（**助手建议 sidecar**：语言无关、天然隔离、可复用 CLI 协议；**dylib 因 Rust 无稳定 ABI 不可行**，只可作官方内置扩展的实现方式）；② **插件市场商业条款**（分成比例 / 伙伴分级 / 认证伙伴）/ 是否做公共 registry 还是先只做企业私有与离线目录；③ 权限清单的**具体粒度**（§11.1 的六项是否够用）；④ 插件是否允许**付费 / 许可绑定**（与 [`protection.md`](protection.md) §6–§7 的在线激活、离线授权共用一套吗） | [`ide/plugins.md`](ide/plugins.md) **§11.3 / §12**（形态三选带建议）、[`errata.md`](errata.md) §五-19 | **⚠️ 2026-09-24 部分收口**：② 市场商业条款与 ④ 插件付费 / 许可绑定 **暂缓**（作者：「目前不考虑收费和市场」）；**① 内核侧加载形态、③ 权限粒度仍待裁**。 **⚠️ 2026-09-25 部分收口**：**① 内核侧插件加载形态取 B = WASM（wasmtime + component model），见 §五-41**；② ④ 已随「不考虑收费和市场」暂缓；**③ 权限清单粒度仍待裁**。
| 26 | **`MetaUi` 的第二渲染方（Flutter）与移动端进版次**（本轮新开，由「移动端 = Flutter」直接引出）：① 现有口径「UI 契约 = mmda-vue、`capability ui` 只在 `target ts`」要不要改成 **ts + flutter**（`target flutter`）——**助手建议改**（Flutter 只渲染 `MetaUi`，UI kit 仍不进后端契约与语言）；② 一致性测试的 UI 维度是否只测一个渲染方（建议**只测一个**，避免双份 UI 用例）；③ 移动端**是否进首版承诺**（建议不进，排 P8 之后） | [`targets.md`](targets.md) **§8-4 / §8-6**、[`vision.md`](vision.md) §8-6/7、[`runtime.md`](runtime.md) §7（业务插件与多端的关系） | **⚠️ 2026-09-24 部分收口**：作者**已有 Flutter 早期实现**，移动端版次与第二渲染方判据**延后（「后面再说」）**——① ② ③ 三条**暂挂**，不进首版承诺。
| 27 | ~~**事件总线与集成编排的 7 条待裁**~~ → **✔ 已裁（2026-09-24，作者取 `1A, 2B, 4A, 5A, 6A, 7A`；第 3 条「数据流的图元落点」随后由作者另给方案——**`.mf` = 数据流（DataFlow）、跨模块流程另立 `.mb`**——**9 条至此全部已裁**，见 §五-34）——结果入 §五-33**（真源与依据 = [`event_bus.md`](event_bus.md) §15，**助手原建议 1A…7A**）：① **执行层引擎**（内嵌轻量 / 直接上 Flink 集群 / C# 用第三方消息框架）；② **外部端点的定义存哪**（适配层配置 + 对账记录 / 反向导入成语言声明 / 只在外部工具）；③ **数据流的图元落点**（`*.mf.g` 增 `mf-flow`/`mf-map` view 种类 / 新文件类型 `*.mx` / 只放 Profile）；④ **端到端一致性承诺写到哪一档**（状态 exactly-once + 汇端幂等 / 宣称全链 / 只承诺至少一次）；⑤ **多租户隔离档**（共享执行 + 租户键 / 每租户独立作业 / 每租户独立环境）；⑥ **水位线与迟到数据默认策略**；⑦ **数据映射可否含外部报文形态**（JSON Schema / XSD） | [`event_bus.md`](event_bus.md) **§15**（含选项与建议）、§5.3 / §7.2 / §9.3 / §10；与 §9-8 **合并裁决**（插件隔离级别 = 总线多租户隔离档，同一件事） |
| 28 | ~~**术语两条待拍**~~ → **✔ 已裁（2026-09-24，作者取 `8C` / `9A`）——结果入 §五-33**：① **Sink 的正式名**——A `Sink`（数据汇，沿用 Flink 词、程序员最熟）／ B `DataSink`（与 EventSource 对称）／ C `EventSink`；② **「端点」的限定规则**（`api.md` 的 HTTP 接口 vs 集成连接点）——A **接口一律写「API / 接口」、集成连接一律写「端点」**（强调时写「集成端点」）／ B 集成侧改叫 `Connector`（**不可行**：Connector 已被「端点的实现」占用，会二次撞车）／ C 保持同名靠上下文区分 | [`event_bus.md`](event_bus.md) **§15-8/9**、[`glossary.md`](glossary.md) §3.1 文末「两处撞车」 |
| 29 | ~~**运维面 8 条待裁**~~ → **✔ 已裁（2026-09-24，作者取 `1A 2A 3A 4A 5A 6A 7A 8A` —— 全部取建议档）→ 结果入 §五-32**：① **首版唯一必需的监控出口** = `/metrics`（OpenMetrics 文本，拉模型）+ 可选 OTLP ／ 只做 OTLP ／ 三出口并列；② **交付包是否附监控后端与采集器镜像**（A 附但可选启用 / B 不附 / C 内置默认开）；③ **Zabbix 侧形态** = A 只出模板 + 让 Zabbix 抓 `/metrics`（HTTP agent + Prometheus pattern）／ B 额外支持 trapper 推送 ／ C 自研 agent 插件；④ **Open-Falcon 兼容优先级** = A 只做 Prometheus 兼容（客户经夜莺 / 兼容层消费）／ B 额外做 JSON 推送 ／ C 不做；⑤ **`mmda ops` 只读诊断命令**（采集面 / 失败队列 / 对账差异 / 生效版本）是否进首版（P9）；⑥ **P9 是否扩为「一致性 + 验收 + 运维出口」**（不新增 P11）；⑦ `mmda doctor` 是否加**时间源一致性 + 监控出口可达性**两项；⑧ **客户端监控首版范围** = A 只做 Web（mmda-vue）／ B Web + Flutter ／ C 不做 | 全部见 [`operations.md`](operations.md) **§11**（选项、助手建议与理由） |
| 30 | ~~**命名约定 8 条待拍**~~ → **✔ 全部已裁（2026-09-24：`1A 2A 3A 4B 5A` + 索引前缀建议；逐条结果见 §五-25 ⑪）**（助手建议 1A…8A）：~~① **枚举成员风格**~~ → **✔ 已裁（2026-09-24，取 B：`UPPER_SNAKE`，单词间 `_` 隔开，三端一致；见 §五-25 ⑨）**；② **命名违规检查强度**（A warning ／ B `mmda check` 失败 ／ C 仅 IDE 提示）；③ **C# 属性是否放宽为 Pascal**（A 不放宽保持 camel ／ B 放宽 + 序列化别名 ／ C 按项目 Profile 开关；**放宽即破坏跨端同名**）；④ 方法名是否统一（A 随各端 ／ B 全 camel ／ C 全 Pascal）；~~⑤ **数据库标识符**~~ → **✔ 已裁（2026-09-24，取 B+：表 / 视图 = 类名 PascalCase、列 = 属性名 camelCase，逐字同模型名；见 §五-25 ⑩ 与 `naming.md` §3.3）**；⑥ 既有代码 / KEEP 区 `Impl` 是否清理（A 不强制 ／ B 提供迁移脚本 ／ C 硬禁全量改造）；⑦ 命名约定是否进硬门禁（与 ② 同一件事，**合并裁决**）；⑧ **主键 / 外键 / 索引 / 唯一约束名格式**（A `PK_` / `FK_` / `IX_` / `UX_` + 模型名原样 ／ B 数据库默认名 ／ C 交 Dialect） | 全部见 [`naming.md`](naming.md) **§6（已清零，保留裁决与理由）**；**逐条结果**：② **warning** ／ ③ **C# 属性不放宽** ／ ④ **方法名随各端** ／ ⑥ **提供迁移脚本**（不强制改名）／ ⑦ **不进硬门禁** ／ ⑧ **不做硬约束**、建议前缀 `IDX_` · `FUNC_` · `PROC_`——**本文件 §五-25 ⑪** |
| 31 | ~~**旧版用户手册并入后的 3 条新议题**~~ → **✔ 全部已裁（2026-09-24 作者取：① 不算 ② 补 `beforeSearch` ③ 保留）——结果入 §五-27**：① **前端工程目录/文件约定**（旧手册：`src/models`、`src/modules/<m>/<m>_logic.ts` / `_view.ts` / `_listview.ts`、`locales/{zh,zh-hant,en}.ts`、`main.ts` 注入模块插件 + `provide(KEY)`）——在「UI 契约只到 `MetaUi`、渲染方 = 现成 mmda-vue」的新口径下，这套**还算不算规范内容**？建议：**不算**，降为 `mmda-vue` 项目内部约定（规范只保证 `MetaUi`）；② **五视图的钩子齐备性**——[`presentation.md`](presentation.md) §1 定义五个视图（`index`/`editor`/`details`/`search`/`report`），实现只有 `beforeEdit`/`beforeDetails`/`beforeIndex` 三个钩子：`search`/`report` 要不要钩子、名字要不要按五视图统一（`beforeSearch`/`beforeReport`）？建议：**要**，且钩子名 = 视图名 + 前缀 `before`（对齐 `ui/**/*.mi` 的五视图键）；③ **`customProperties.$字段` 是否进新 API 契约**——`@Ref`/枚举的显示标签现在放 `customProperties.$<字段名>`（不污染主字段类型、随 locale 变），新契约（[`api.md`](api.md)）要不要保留该形态，还是改成投影视图里的 `xxxLabel` 字段？建议：**保留** `customProperties` 并写进 [`api.md`](api.md)（旧实现与新前端都已依赖） | [`guide/quickstart.md`](guide/quickstart.md) §7–§9、[`presentation.md`](presentation.md) §1、[`api.md`](api.md) §3.5 |
| 32 | ~~**嵌入式脚本执行引擎（DB 触发器的可移植替代）**~~ → **✔ 已裁（2026-09-24，作者取 A）→ 结果入 §五-35**（本轮新开，作者原话：「**实际上 service 层也有钩子，是给业务逻辑用的**。我原先想能否有**脚本执行引擎**，嵌入到 java、c# 后台，能够写一些简单的脚本，**起到类似数据库的触发器的作用**。**技术上有没有可能？数据库的触发器没有可移植性，换数据库就废了**」）：**取 A / B / C**——**A（助手建议）受限脚本 = m 语言受限子集，由 Rust 内核求值、走 P6 嵌入通道（Panama / P-Invoke + WASM）**，三端同一语义 + 沙箱默认 + 可静态校验 + 随元数据版本走；**B 宿主脚本引擎**（Java GraalJS／C# Roslyn·Jint）表达力最强但三端各写一遍、GraalJS 在 stock JDK 上不受支持、沙箱须自建；**C 不做脚本**（只留声明式，临时逻辑会退回 DB 触发器）。**技术事实已核并写进 [`runtime.md`](runtime.md) §4.5**（Nashorn 已随 JDK 15 移除、GraalJS 不保证 stock JDK、.NET 三方案、**引擎限额 ≠ 隔离边界**）。**作者取 A**，并放开**跨模块调用**与**全 Entity 访问**（查询 / 条件更新 / 删除），**能力 = 内核函数**（[`runtime.md`](runtime.md) §4.6 起始清单，可用 `capability` 不断增补）；**余 1 条待裁**：脚本运行身份（继承调用者 / `runAs` / 默认继承 + 可声明 `system`） | [`runtime.md`](runtime.md) **§4.5（含脚注技术事实）+ §4.2 注 + §4.4**、`PLAN.md` §4（P6 嵌入求值） |
| 32 | ~~**体验面（UX）4 条待裁**~~ → **✔ 全部已裁（2026-09-24 作者取 `1B 2B 3B 4A+B`）——结果入 §五-29**（助手建议 1A / 2B / 3B / 4A+B）：① **上线后度量的回流腿**（UCD 的 measure 闭环）——A **把 `mmda ops` 的只读诊断指标（任务完成率、校验失败 top N、最常放弃的操作）做成需求与设计评审的输入清单、只进报告** ／ B 进 `mmda check` 出 warning ／ C 不做，保持单向流程；② **十原则清单的生效强度**——A 只进 `quality-report.json` ／ B **A 类子集进 `mmda check` 出 warning** ／ C 择条进硬门禁；③ **无障碍基线**——A 只声明「以 WCAG 2.2 AA 为目标」出报告 ／ B **AA 的可自动化子集（语义标签 / 焦点顺序 / 错误文本）进 `mmda check` 出 warning** ／ C 进硬门禁；④ **服务设计「旅程」的建模位置**——A 不进语言与元模型 ／ B **A + 设计器插件侧留只读「旅程视图」口子** ／ C 新增元模型元素 | 全部见 [`ux.md`](ux.md) **§11**（选项、建议与理由）；相关：§5（UCD 闭环缺口）、§8（包容性）、§4（十原则清单） |
| 33 | ~~**脚本的查询形态与兜底层**~~ → **✔ 已裁（2026-09-24）→ 结果入 §五-36**（本轮新开，作者原话：「**还有一种可能，给上下文后，写类 SQL 的语句，然后 C#, java 都有 sql 包，能自动翻译执行，也是很好**」；「**或者干脆注入 EntityFactory，直接 java/c# 写**」）：① **类 SQL 查询块由内核编译（建议）还是交宿主 SQL 包翻译**——**建议「路线 1：内核解析 → 内核生成方言 SQL → 宿主只执行」**，且**只用于只读查询**（join / 聚合 / 子查询），写入仍走 `script.entity.*`；② **`EntityFactory` / `Repository` 直写宿主代码**——**建议定位为 KEEP 区兜底层、非第三档脚本**（已裁 §9-6：两层不替换、目标消灭手写 SQL 与字符串字段名；入口必须是接口、禁手写 SQL，钩子实现进评审清单） | [`runtime.md`](runtime.md) **§4.6**（查询形态与兜底层）+ §4.2 / §4.5、`PLAN.md` §6.2-27、[`glossary.md`](glossary.md) §2 |
| 34 | ~~**宿主语言运行期编译（A′）**~~ → **✔ 已裁（2026-09-24）→ 结果入 §五-36**（本轮新开，作者原话：「**Java Compiler API**」「**Roslyn / DLR**」）：**脚本语言 = 宿主语言本身**（Java / C# 片段，运行期 in-memory 编译后加载）——**建议 A′ 定位为 KEEP 区宿主扩展的「运行期加载方式」（热更版），而不是钩子脚本的语言**；**不取 A′ 做钩子脚本**（理由已实测：脚本与宿主同权、Java 21 无 SecurityManager、三端各写一遍、元数据静态校验与 L3 对账覆盖不到、纯 JRE 上 `getSystemJavaCompiler()` 为 `null`） | [`runtime.md`](runtime.md) **§4.6（3）宿主语言运行期编译**（含本机实测表）、`PLAN.md` §6.2-28 |

---

## 四、校勘记录

| 日期 | 动作 |
| --- | --- |
| 2026-06 | `E:\Dev\mmda-architect` 一轮：Rust 内核 1699 行、35 篇文档、381 文件语料、1196 行 Python 反向工具 |
| 2026-09-24（一轮） | 以 `D:\2026\rust\doc`（当时名为 `D:\2026\c`，2026-09-24 改名）为根合并：语言类文档并入根文档（`readme/datatypes/records/statements/events/presentation`）；工具与 IDE 类文档原样迁入 `doc/ide/`；AI、legacy、guide、templates 原样迁入；被合并原文留档 `doc/archive/2026-06/`（13 篇）；新增本文件与 `doc/index.md`、`doc/meta-model.md`、`doc/project.md`、`doc/glossary.md`；修复迁入文档 39 处失效内链 |
| 2026-09-24（二轮） | 勘察 C# 后端 `D:\2026\cs\MMDA`（762 `.cs` / 70,831 行、模块与同名 `Meta*` 类、有 `IEventBus`、`Mmda.Core/` 嵌套 git）；新增 [`targets.md`](targets.md)（三端契约矩阵 + 能力分档 + 一致性测试口径）；`PLAN.md` 升 v0.4（新增 §2.5、§3.10、P0.5，纠正 §3.4「三语言对等底座」为 2 后端 + 1 前端）；冲突 3 补入建议方案 |
| 2026-09-24（三轮） | 三条裁决落地（见 §五）：`PLAN.md` 升 v0.5（新增 §6.3 与 P9，§2.4 改为五处冲突 + 状态列）；新增 [`contracts-inventory.md`](contracts-inventory.md)（60+ 概念逐行 `file:line`，含 `同/≈/✗/独` 统计）；同步 `project.md` 抬头、`presentation.md` §5.1（渲染契约）、`targets.md` §3/§8、`index.md` |
| 2026-09-24（四轮） | 新增 [`protection.md`](protection.md)（算法与知识产权保护）、[`workflows.md`](workflows.md)（五类职责 / 三种工作模式 / 变更分级 L0–L3 / AI Agent 三道闸 / 业务人员路径）、[`testing.md`](testing.md)（用例从声明生成、AI 生成人审、三层验收物、覆盖率与变异）；`PLAN.md` 升 v0.6→v0.9（新增 §3.1 算法宿主、§3.11 测试与验收、P9 扩为「一致性套件与验收」+「测试前置」）；[`ai/tools.md`](ai/tools.md) 补 §2.5 测试与验收工具面与 Agent 工作流；`targets.md` 增「算法 / 重计算载体」行；`errata.md` 语法待裁新增第 11 项 |
| 2026-09-24（五轮） | 裁决落地：**`.mt` 进语言族** + 验收口径 6 条（`testing.md` §11 转「已裁」记录）；`testing.md` 新增 §4.1 **AI 增强能力总表**（10 项）、§4.2 六条护栏、§6.1 三个新覆盖维度（变更覆盖 / 缺陷回溯覆盖 / 分支覆盖）、§6.2 **用例质量评估**（7 维）与三层分层（黄金 / 普通 / 草稿）；`workflows.md` §12 第 7 条转已裁；`PLAN.md` 升 v1.0（§3.11 补 AI 增强、§6.2 第 16 条转已裁）；本文件语法待裁新增第 12 项（`.mt` 语法形态） |
| 2026-09-24（六轮） | 新增 [`quality.md`](quality.md)（**ISO/IEC 25010:2023 九特性** → MMDA 可自动信号、A/B/C/D 可判定性分级、质量报告与门禁、**AI 时代 review 分层**与三种高价值用法、**IDE 全生命周期八阶段**）；`testing.md` 验收物增补「质量报告」；`workflows.md` §8 补 review 分层；`PLAN.md` 升 v1.1（新增 §3.12，§3.9 补全生命周期，§6.2 第 17 条） |
| 2026-09-24（七轮） | 新增 [`architecture-review.md`](architecture-review.md)（**ISO/IEC/IEEE 42010:2022** 关注点/视角映射、**ARCH-101…503 规则集**、Martin 度量 `I`/`A`/`D`、**SOLID 操作化**、打分模型与建议模板、评审仪式与首版门禁）；`quality.md` 增「结构合规」行；`testing.md` 验收物 ④ 含架构评估；`workflows.md` 架构师第 7 步门禁；`ai/tools.md` 增 §2.6 质量与架构评估工具面；`PLAN.md` 升 v1.2（新增 §3.13，§6.2 第 18 条） |
| 2026-09-24（八轮） | 架构评估口径裁决落地（见 §五-7）：`architecture-review.md` §5 首版硬门禁转「已裁」、§2.2 抽象度 `A` 定义标已裁、§8 拆成「裁决记录 + 待裁（余 3 项按建议值先行）」；`quality.md` §7-6、`workflows.md` §12-9 同步；`PLAN.md` 升 v1.3（§3.13 门禁句转已裁、§6.2 第 18 条转已裁） |
| 2026-09-24（九轮） | UI 契约收紧（见 §五-8）：`presentation.md` §5.1 重写为「唯一 UI 通道 = mmda-vue；后端只出 `MetaUi`」（含 Java `MetaUi.java:21` 族 / C# `MetaUi.cs:12` 族 / 前端 `packages/core/src/metaui/*` 8 文件 2,626 行的实测证据）；`targets.md` §1/§4（新增「MetaUi 元数据（UI 契约本体）」行）/§6/§8-4；`contracts-inventory.md` §6 抬头与再裁说明；`workflows.md` §1.3/§4-6；`quality.md` 交互能力行；`architecture-review.md` ARCH-103；`PLAN.md` 升 v1.4（§3.4 收紧段、§6.2 第 19 条） |
| 2026-09-24（十轮） | **质量文档吸收内部资料与量化方法**（用户提供 `D:\项目\2026 mmda\软件质量.pptx` 与同目录《基于量化指标分析的软件质量度量方法》，后者为 2007 年《北京化工大学学报》扫描件，经 PDF 逐页渲染 + 视觉核读）：`quality.md` 新增 **§1.0 标准谱系**（9126:1991 → 25010:2011 → 25010:2023 命名对照）、**§2.1 三层度量模型与加权聚合**（特征根法 AHP 权重、算例 `V_c2 = 0.648`、三条收窄）、**§2.2 默认质量目标基线**（可用性 ≥ 99.9% / MTTF ≥ 10 天 / MTTR ≤ 30 分钟 / MTBF ≥ 10 天/次 / 响应 ≤ 5 s / 并发 ≥ 500·800 / TPS ≥ 80）、**§2.3 运行期 14 项指标**与三个采集面；§1.2/§1.5 补交叉引用、§7 新增 2 条待裁（加权口径、阈值基线下发）、§8 补三条来源；`testing.md` §11-6 性能用例阈值指向 `quality.md` §2.2；`PLAN.md` 升 v1.5（§3.12 补定量层与阈值基线、§5 目录、§6.2 第 17/20 条） |
| 2026-09-24（十一轮） | **rui/vui 补裁**（见 §五-8 补注）：前端 kit **`vui`（Vue）与 `rui`（React）都可选、随技术人员喜好**——自定义前端 UI 插件时按熟悉度挑；属同一前端项目内部自由，**不构成两套 UI 契约**（契约边界只到 `MetaUi`）。落点：`presentation.md` §5.1 界限 ②、`workflows.md` §1.3、`PLAN.md` §6.2 第 19 条补裁 |

| 2026-09-24（十二轮） | **随想录归档并落成两篇规范**（用户提供《MMDA 随想录》，原文留档 `doc/archive/2026-09/随想录.md`）：新增 [`requirements.md`](requirements.md)（需求为什么难 / 三层需求→MMDA 落点 / **四标准的机械信号** / SERU 四要素 / 需求管理四步 / SRS 形态 / 6 条待裁）；`readme.md` 新增 §7 **与低代码的区别**；`ide/specification.md` 新增 §4.8 **建模域清单（11 域）**与 §4.9 **宿主形态**、§4.2 用例与需求条目挂接；本文件 §三 新增 11 条工程待裁（#9–#19）；`PLAN.md` 升 v1.6（§3.14 需求工程、§3.15 设计器建模域与宿主形态、§5 目录、§6.2 第 21/22/23 条、变更记录 v1.6）；`index.md` 阅读顺序与索引同步 |
| 2026-09-24（十三轮） | **随想录三问裁定**（用户「独立壳优先 / 组织架构是数据 / BI 要有元数据但要能建模、MetaBiCube 容后再议」）：`ide/specification.md` §4.9 由「待裁张力」改**已裁（独立壳优先）**、§4.8 域 2 / 域 4 / 域 8 / 域 10 更新（组织架构=数据、BI 方向已裁细节后议）；本文件 §五 新增第 9/10/11 条、§三 第 9/10/14 条转已裁；`PLAN.md` 升 **v1.7**（§3.15、§6.2-22/23、§6.3 新增第 5/6/7 条、变更记录）；`index.md` 规范版本升 0.5 |
| 2026-09-24（十四轮） | **Role 进语言（与 Module 同级）**（用户：「组织架构虽然是数据，但对于业务流程来说是必须的；惯例是在需求阶段就识别关键用户即角色（Role），因此架构设计时 Role 作为和 Module 分解同等重要的设计，进 mmda-lang 语言」）：[`meta-model.md`](meta-model.md) **新增 §8.1 Role（关键用户 · 与 Module 同级）**——属性表（`roleCode`/`auth module`/`actions`/`scope`）、为什么与 Module 同级、与「权限在 IDE 配置」的关系；[`requirements.md`](requirements.md) §2 新增「关键用户（角色 / Role）」行 + 惯例补注（需求阶段识别关键用户）；[`project.md`](project.md) §3 `.mr` 行注明；[`architecture-review.md`](architecture-review.md) 关注点表注明同级；[`workflows.md`](workflows.md) §1.2 增补「配置的是账号→Role 的授予，Role 本身是设计元素」；[`ide/specification.md`](ide/specification.md) §4.8 域 2 改为两层分治；本文件 §五 新增第 12 条、§三 第 10 条补注；`PLAN.md` 升 **v1.8**（§3.15、§6.2-23、§6.3 第 8 条、变更记录） |
| 2026-09-24（十五轮） | **运行架构落成（吸收作者《分层架构》概念设计图）**：新增 [`runtime.md`](runtime.md)——四层职责（Controller = API 开放 / Service = 商业逻辑 / Repository = 数据读写 / **缓存 = 横切面**）、两条路径（进入三件事：认证授权 / 校验 / 默认值；返回：组装 → 聚合）、**事务夹在 Before / After 拦截点中间**、**拦截点上升到语言**（`before`/`after` × **封闭生命周期点**枚举 + **设计师配置 / 程序员定制的固定模式**）、缓存单点出口、装配与聚合是生成物、现状与缺口（业务 Controller 手写 74/16/9/3、`EntityFactory` 未融合 Repository）、**7 条待裁**；[`architecture-review.md`](architecture-review.md) 新增 **§2.1b（ARCH-107…110，首版不进硬门禁）**；[`targets.md`](targets.md) L2 补「事务边界 / 生命周期钩子」、能力矩阵新增「生命周期钩子」行 + 缓存行补租户实现；本文件 §五 新增第 13 条、§三 新增第 20/21/22 条；验收钩子实测（Java `EntityService.java:337/378/388/650/658/878/2433`、`ReactiveEntityController.java:1292`、`EntityRepository.java:509/1518`；TS `entity_logic.ts:171/175/183/185/199/201/207`）；`PLAN.md` 升 **v1.9**（§3.16、§5 目录、§6.2 第 24 条、§6.3 第 9 条、变更记录） |
| 2026-09-24（十六轮） | **API 契约落成（语言层面定义 API + 对接 API 管理工具）**（用户：「后端服务要能集成 API 管理，例如 yapi/apifox/swagger 等插件，从设计、测试和运维几个方面考虑」→ 追加澄清：「就是在语言层面，模块分解成 module 后，怎么考虑定义 api，并支持 api 文档生成，支持外部工具、open api 互动」）：新增 [`api.md`](api.md)——**API 由「模块分解 + Feature + 视图 + Action + Role + 字段约束」推导**（不造 `api` 顶层块 / 不造 DTO 概念）、语言层只补 **`expose` 暴露边界** 与 **稳定度/版本**（`stable`/`beta`/`deprecated` + `since`/`sunset`）、**OpenAPI 3.1 由 Rust 内核生成** + **契约测试成为一致性测试的 API 维度**、设计/测试/运维三面（API 面板 / 机械用例 + Mock / **网关粗粒度 vs Controller 细粒度** + `deprecated` 调用量作退役倒计时）、**与外部工具单向互动**（导出 ✅ / 逆向导入 ✅ / **回写 ❌** / 对账 ✅）、现状实测（**三端 API 文档能力全缺**：Java 无 springdoc、`@Operation/@Tag` 0 个；C# `Swashbuckle.AspNetCore 6.9.0` 0 注解；TS 无生成；端点模板 `/create`·`GET ""`·`/{id}`·`/save`·`/{id}/delete`·`/deleteAll` 在 `mmda-mes` 多控制器逐字重复；`SearchParam.java:7-10` 已有 `pageNo`/`pageSize`/`sorts`）、**8 条待裁**；[`runtime.md`](runtime.md) §1 交叉引用；[`targets.md`](targets.md) L2 + 能力矩阵新增「API 文档 / OpenAPI」行；[`testing.md`](testing.md) §10 补一行；本文件 §三 第 23 条；`PLAN.md` 升 **v1.10**（§3.17、§5 目录、§6.2 第 25 条、变更记录）；`index.md` 版本升 0.8 |
| 2026-09-24（十七轮） | **API 边界 = module 边界（插件不侵入语言）**（用户：「我期望这些插件不要侵入我的语言层面……实际上就约定了应该开放的 API，对不？至于没有 module 归属的元对象，我还没想清楚」→ 助手答「对」并给出硬理由，用户回「好的」批准落笔）：[`api.md`](api.md) **新增 §1.1**（第一原则 + 插件两条边界 + 判据 + **无归属元对象的六类决策表**，实测语料 `biz/base.ma` 绑定 `data/models/base/` 77 个对象、`data/models/mes/` 138 个）、§6 新增「无归属对象」实测行、§7 新增第 9 条待裁；[`architecture-review.md`](architecture-review.md) §2.1 新增 **ARCH-111**（对外可见的 Record 必须有唯一 owner module，首版不进硬门禁）；[`PLAN.md`](..\PLAN.md) 升 **v1.11**（§3.17 补第一原则、§6.2-25 补第 9 条、§6.3 第 10 条、变更记录）；本文件 §五 第 14 条、§三 第 23 条补注；`index.md` 版本升 0.9 |
| 2026-09-24（十八轮） | **OpenAPI 原生支持（OAS 3.1.0 基准）+ AI 造数**（用户：「open api 是有参考标准的，这个要原生支持」（附 `https://spec.openapis.org.cn/oas/v3.1.0.html#operation-object-example`）；「API 测试要能借助 AI 的能力自动生成 mock 数据，这个也很关键，这是撰写测试用例的提速手段」）：[`api.md`](api.md) **新增 §3「与 OAS 3.1.0 的逐条对齐」**——§3.1 四条判据（内核一等后端 / 过官方 Schema 校验 / 契约测试双向对账 / 语言层不出现 OAS 术语）、§3.2 根对象与公共对象逐字段来源、§3.3 **Operation 12 字段逐条**（`tags`/`summary`/`description`/`externalDocs`/`operationId`/`parameters`/`requestBody`/`responses`/`callbacks`/`deprecated`/`security`/`servers`）、§3.4 五视图与 Action → 端点与状态码（含非法转移 409 与"存量 `POST /save` 冲突"三选项）、§3.5 **逻辑类型 → JSON Schema 2020-12 映射**（实测 3.1 差异：`nullable` 全文 **0 次**、`contentEncoding` 替代 `format: byte`、`webhooks` 原生、`info.summary` 与 `license.identifier` 新增）、§3.6 securityScheme/scopes、§3.7 `x-mmda-*` 溯源指针、§3.8 **覆盖度自检（23 + 5 + 2 = 30 个对象）**、**§3.9 Mock 数据两层造数**（机械层零 AI + AI 语义层；四条护栏）；§7 现状新增「无归属对象」行、§8 待裁 9 → **16 条**；[`testing.md`](testing.md) **新增 §4.3**（Mock 数据生成：两层分工 / 四条护栏 / 命令面 / 用途三面）+ §8 命令面 `mmda mock` + MCP `mmda_mock_gen` + §10 与 §11 指针；[`ai/tools.md`](ai/tools.md) §2.4 补 `mmda_api_export`/`mmda_api_check`、§2.5 补 `mmda_mock_gen` 与"造数不需双签、断言必须双签"的边界；[`targets.md`](targets.md) L2 与能力矩阵补 OAS 3.1.0 原生与 mock 造数；[`PLAN.md`](..\PLAN.md) 升 **v1.12**（§3.17 两条已裁、§6.2-25 补第 ⑩–⑯ 条、§6.3-10 补充、变更记录）；本文件 §五 第 15 条、§三 第 23 条更新；`index.md` 版本升 0.10 |
| 2026-09-24（十九轮） | **API 契约四条落定**（用户逐条回：「1. REST语义」「2. 按你建议」「3. 进」「4. 要」）：[`api.md`](api.md) §3.1 判据 2 标 **✔ 已裁：官方 Schema 校验与契约测试进硬门禁**、§3.4 存量路径冲突改写为 **✔ 已裁取 A 方案**（REST 语义 + `legacyPathStyle` 兼容开关、双版本并存、老路径 `deprecated`/`sunset`）、§3.5 类型映射三行标已裁（**精度优先**：`decimal`/`money` → `string` + `pattern`，超 JS 安全整数的 `int64`/`uint64` → `string`，`Timestamp` → `date-time`）、§3.9 护栏 3 标已裁（**种子 + provenance 进版本控制**）、**§8 重构为「8.1 已裁（第 10/11/15/16 条）/ 8.2 待裁（12 条）」**；[`testing.md`](testing.md) §4.3 护栏 3 同步；[`targets.md`](targets.md) L2 补序列化约定；[`PLAN.md`](..\PLAN.md) 升 **v1.13**（§6.2-25 标已裁 4 条、§6.3 第 11 条、变更记录）；本文件 §五 第 16 条、§三 第 23 条更新；`index.md` 版本升 0.11 |
| 2026-09-24（二十轮） | **仓重命名 `D:\2026\c` → `D:\2026\rust` + 规范真源入 git**（用户：「把 D:\2026\c 重命名为 D:\2026\rust，我当时想着用 c 语言。然后也改下现在项目的路径配置」；远端由用户建 `https://github.com/Roy7611/mmda-lang`，**公开仓** MIT）：[`PLAN.md`](..\PLAN.md) 升 **v1.14**——§0 `git init` 行改「✔ 建仓并接入远端」、§2.1「版本控制」行、§4 P0 第 ② 项、§7 风险表「无版本控制」行（**收窄为 architect 仓仍裸奔**）、§8 回滚段、§6.2-1 散文仓名 `c` → `rust`；本文件校勘一轮记录里的历史根路径改 `D:\2026\rust\doc` 并注明"当时名为 `c`"；[`protection.md`](protection.md) 算法仓路径同步；仓外：技能 `mmda-lang-development` 21 处、`mmda-lang-spec-repo` 6 处、探针脚本 12 处，**共 46 处路径引用**同步；仓内新增 `README.md`（入口：状态 / 怎么读 / 目录）与 `.gitignore` MMDA 段（**生成物不入库**）；首提交 `0679a30`（57 文件 / 56 md）已推远端并**回读核对**（58 blobs、明文口令 `grep -c` = 0）；`index.md` 版本升 0.12 |
| 2026-09-24（二十一轮） | **愿景与四层目标落成**（用户给出〔To Boss〕开放源码 / 协同 / 共赢 / 降成本，〔To CTO〕开放源码 / 易定制 / 易运维 / **支持国产化**，〔To User〕易用 / 轻松 / **AI 赋能** / 容易二开，〔架构〕分层多租户 / 热插拔模块化 / 微服务 / 容器化 / 高性能 / 安全防黑客 / 可靠性 / 跨平台 / 多端多语言——「这是为什么要做 m 语言和 mmda 框架的本意和初衷」；并回「国产化，我会实现一个，例如 naive+别的表格插件，**syncfusion 只是一个选项**」「同意 1A 2C 3只是部署方式支持 4同意你的建议」）：**新增 [`vision.md`](vision.md)**（四层目标 + 逐条机制 + 现状 + open core 边界 + 国产化三级 + 部署方式口径 + 业务可测指标 + 5 条待裁；实测证据：`mmda-core-sql/.../dialects/` 8 个方言类含 **`DmDialect`（达梦）** 与 **`KingbaseDialect`（人大金仓）**、`MetaDataType.java:209/213/220/224` 有 `kingbaseType`/`kingbaseName` 与达梦字段、全仓检索「国产/信创/麒麟/鲲鹏/LoongArch」**0 命中**、`packages/vui-syncfusion/package.json:84-109` **30 个 `@syncfusion/ej2-*@34.2.2` 商业控件**、`MetaDb.java:3` 版权头 **PROPRIETARY**）；[`PLAN.md`](..\PLAN.md) 升 **v1.15**（§1 拆为 §1.1 商业目标 / §1.2 工程目标 / §1.3 非目标，非目标新增 2 条；§0 台账新增「愿景落成」行；§6.3 新增第 12–15 条）；[`quality.md`](quality.md) 新增 **§3.1 业务目标的可测指标**（4 项，只进报告不进硬门禁）；[`protection.md`](protection.md) §7 拆为 **§7.1 open core 边界** + §7.2 授权形态；[`targets.md`](targets.md) §8 新增第 5 条（国产化与 `capability` **正交**）；[`readme.md`](readme.md) 抬头加愿景指针；[`index.md`](index.md) 阅读顺序新增第 **0** 篇 + 版本升 0.13；本文件 §三 新增第 24 条（愿景 5 条待裁）、§五 新增第 17 条 |
| 2026-09-24（二十二轮） | **L2 国产化目标矩阵落定**（用户回「1A, 2A, 3A,4A,5A」）：**实测依据先落**——Rust `loongarch64-unknown-linux-gnu` 已是 **Tier 2 with host tools**（kernel ≥5.19 / glibc ≥2.36 / LSX，来源 rustc book）；**毕昇 JDK 21 官方只出 Linux/AArch64 与 Linux/x86_64**（openEuler 镜像仓 README）；**.NET RID 目录含 `linux-loongarch64` 但官方 supported-os 表 Linux 仅 Arm32/Arm64/x64**（Microsoft Learn），故龙芯上 C# 端属社区级。**裁决**：① CPU = x86_64 + aarch64 双架构（海光 / 兆芯 + 鲲鹏 / 飞腾），龙芯不进验收矩阵、申威不承诺；② 验收 OS = 麒麟 V10 SP3 服务器版 + 统信 UOS 服务器版 V20，CI 基线 = openEuler LTS；③ JDK = 毕昇 21 + 上游 OpenJDK 21；④ **设计器不进国产 OS**（只承诺运行时 + 内核 CLI）；⑤ 交付 = 离线包 + `mmda doctor` 自检；**架构支持等级写进 capability 声明**。落点：[`vision.md`](vision.md) **新增 §5.2.1**（五条决策 + 依据 + 验收口径 + 阶段落点）、§5.2 的 L2 行改标已裁、§8-4 待裁标已裁；[`ide/specification.md`](ide/specification.md) §4.9 「两条口径」→ **三条**（新增第 3 条：设计器不承诺国产 OS）；[`targets.md`](targets.md) §8-5 补矩阵摘要；[`PLAN.md`](..\PLAN.md) 升 **v1.16**（§4 新增「L2 验收腿」段、§6.3 新增第 16 条、变更记录）；本文件 §五 第 18 条、§三 第 24 条更新；`index.md` 版本升 0.14 |
| 2026-09-24（二十三轮） | **共赢落成「插件市场 + 设计阶段原生支持插件式开发」**（用户原话：「共赢做插件市场，留这个口子，设计阶段原生支持插件式开发」）：[`ide/plugins.md`](ide/plugins.md) **升 0.2**——新增 **§8 设计阶段原生支持**（「原生」落成五条可检判据：插件清单进 `*.mmda` 项目真源随 git 评审 / 扩展点在设计阶段即存在 / 内置与第三方同路径无特权通道 / 插件产物进追溯链 / 离线可用；硬边界：**语言层不出现插件与市场概念**）、**§9 插件市场**（三形态 registry 同一协议、清单增补 `kernel`·`targets`·`capabilities`·`publisher`·`signature`·`license`、上架三关与可离线算的兼容矩阵、三级签名与信任链、与算法保护咬合）、**§10 扩展点规划**（`validationRules`·`importers`·`reportPanels`·`templatePackages`·`codegenHooks`·`stageActions`·`kernelBackends`）、**§11 权限与硬边界**（六项最小权限；**禁写语言文件按后缀拒绝 + 审计**、插件校验只出 warning、不进语法）、**§12 加载形态待裁**（首版只开设计器插件；内核侧三选，建议 sidecar + JSON-RPC，Rust dylib 因无稳定 ABI 不可行）；[`vision.md`](vision.md) §2 共赢行补「市场 + 闭源增值内容分发渠道 + 设计阶段原生」、§8-5 标已裁；[`PLAN.md`](..\PLAN.md) 升 **v1.17**（§6.3 第 17 条、§4 P7 验收增「插件宿主可用」、抬头十七条、变更记录）；本文件 §五 第 19 条、§三 第 24 条 ⑤ 标已裁 + **新增 §三 第 25 条**（内核侧插件形态 / 商业条款 / 权限粒度 / 付费与许可绑定）；`index.md` 版本升 0.15 |
| 2026-09-24（二十四轮） | **插件语义纠正 + 核心平台 MIT + 移动端 Flutter + OWASP ASVS 进门禁**（用户原话：「我说的插件是支持用户自己开发业务功能模块，至于 IDE 插件对他们没那么重要，支持更好」「核心平台 MIT」「移动端考虑 flutter 支持」「OWASP ASVS 进入」）：**[`runtime.md`](runtime.md) 新增 §7「业务功能模块插件（插件的主形态）」**（§7.1 与既有概念对齐——**插件就是 `module`**，语言层零新增；§7.2 包形态 `jar`/`dll`/npm + 清单 `mmda-plugin.json`；§7.3 数据模型硬约束——只在自己的 module 内新建、不许改他人表结构、不许直接读写对方库表、装载期冲突检测**冲突即拒载**；§7.4 装载与运行语义——启动 + 热装载、跨插件 = 跨 module、升级不覆盖 KEEP 区、内核版本不满足则拒载；§7.5 二开链路；§7.6 主/次与「一个市场，两类插件」），同文 **§7 现状与缺口 → §8、§8 待裁 → §9**（待裁新增 8/9/10：隔离级别 / 插件与 module 粒度 / 数据模型扩展边界），全仓 4 处引用随之改号；[`vision.md`](vision.md) **§5.1 许可表改「全栈 MIT」**（原「规范 MIT / 内核 Apache-2.0」，写明代价：MIT 不带专利授权与商标条款）、§2 共赢行、§5.4 安全与多端行、§8-1/2/3 标已裁 + **新增待裁 6/7**；[`quality.md`](quality.md) **新增 §3.2 OWASP ASVS 门禁口径**（L1 自动化子集进 `mmda quality gate`、L2·L3 与人工渗透不进、Top 10 作报告项、引用必写版本号）+ §1.6 补安全验收基准；[`targets.md`](targets.md) §8 新增第 6 条（Flutter 对 `capability ui` 的冲击）；[`protection.md`](protection.md) §7.1 许可行改 MIT；[`PLAN.md`](..\PLAN.md) 升 **v1.18**（§6.3 新增第 18–21 条、§6.3-17 ⑥ 标注纠正、§4 P4 增「业务功能模块插件可装 / 卸」/ P6 注明 Dart 端服务 Flutter、抬头十九条→二十一条、变更记录）；本文件 §五 第 20 条、§三 第 24 条更新 + **新增 §三 第 26 条**；`index.md` 版本升 0.16 |
| 2026-09-24（二十五轮） | **事件总线与集成编排（底座 ESB 能力）落成**（作者原话：「我一直想让 mmda 底座提供 ESB 的能力……**只要配置就能基本覆盖 80% 的 API 接口集成**」「**Event → Message → 数据 Data**」「java 里我看好 Flink 有 RocksDb、Time Window 和 Watermark，做到 Exactly Once，而 Spring Integration 需要自己管 Message Store」「我更偏向 **Flink 的概念**，适合未来开发 IOT、实时数据流监控」，并给出两张调研图）：**新增 [`event_bus.md`](event_bus.md)**（概念模型 Event → Message → Data = Header + Payload / 三类集成：数据·流程·接口 / 端点三类来源：内部 module 边界零配置推导 + 外部可配 + 底座内置 / **DataFlow 三张图**：节点图·数据流图·数据映射图 + 节点类型清单 + **到货补料例** / DataMapper 四算子 / State·Event Time·Watermark·Window·Checkpoint / **Outbox 事务性发件箱** / **Exactly-once 写实口径** / 多租户三档 / 监控指标与自有 UI / CLI·MCP / 与标准 ESB 的差异 / **7 条待裁（建议 1A…7A）**）；**引擎裁决 = 借 Flink 的语义、不绑 Flink 的运行时**（硬事实：Flink 只有 JVM/Python 面、无 .NET 实现；Flink 是集群运行时而底座是随应用部署的库 → `RuntimeProfile.engine`：`embedded` 默认 / `flink` 留口子 / 第三方 .NET 消息框架不作引擎）；**语言层零新增**；[`events.md`](events.md) 补分工与指针；[`targets.md`](targets.md) §1 L2、[`ide/graph-files.md`](ide/graph-files.md) §4.5、[`ide/specification.md`](ide/specification.md) §4.8 域 6、[`quality.md`](quality.md) §2.3 同步；`..\PLAN.md` v1.19（P8 扩为「事件层与总线」+ 新增 P10 + §6.3-22）；`index.md` 版本升 0.17（阅读顺序插入 `event_bus.md` 为第 7 篇） |
| 2026-09-24（二十六轮） | **事件与集成的术语统一（唯一命名）**（作者原话：「这里面不同的框架概念不同，但是我想能够在 mmda 里面统一认识」「我认同数据的校验 Validation/Validator，转化器 **Converter**（**我不用 Transformer，免得与那个 AI 的 Transformer 架构混淆**），过滤器 Filter，聚合计算 Aggregator。**Endpoint 和 Channel 我也认同**」「而 Source 我认同**事件源 EventSource** 的概念。我需要你统一术语，不要让程序员误解」）：**命名真源 = [`glossary.md`](glossary.md) 新增 §3.1**（位置 / 统一名 / 定义 / **禁用别名** 四列 + 两条判据「一个概念只留一个名字」「同一名字不许指两件事」）；统一清单：Flink `Source` → **EventSource（事件源）**（`kind`：设备·定时·回调·CDC·文件·消息·进程内）、Flink `Sink` → **Sink（数据汇）**（**Write / Push / Call 是投递方式，不是三个概念**）、SI `Channel Adapter` → **Endpoint（端点）**（配置单元）且其**实现 = Connector（连接器）**、SI `Transformer` → **Converter**（**既有概念 `.mc`/`flow/converters/`，不是新造**）、`Transformation` → **Processor** 四类 **Validator/Converter/Filter/Aggregator**（+ 编排必需的 `Router`/`Splitter`）、`Message Channel`/Topic → **Channel**（点对点/发布订阅只是投递语义）、**DataMapper = 配置面不是节点**、**Trigger 降为 EventSource 的配置项 `on`**、**Producer/Consumer 只在描述外部系统时用**（MMDA 统一 **Publisher/Subscriber**）；两处撞车入 §三-28；落点：[`event_bus.md`](event_bus.md) **§1.1 术语位置图** + §5.1（标明 SI 词只作对照）+ §6 端点契约 + **§7.3 节点表与到货例全部改用统一名** + §15 新增 8/9；[`events.md`](events.md) 四处（Producer 说明 / Consumer 是端点 / Sink 拆解 / Source 改名）；[`ide/diagrams.md`](ide/diagrams.md) §4.1 DFD 元素映射补统一名列；`..\PLAN.md` v1.20（§6.3 第 23 条）；`index.md` 版本升 0.18 |
| 2026-09-24（二十七轮） | **易混淆概念辨析（四对 + `stream`）**——作者问：「**publish/subscribe 对应 publisher/subscriber、produce/consume 对应 producer/consumer、channel / pipe、inbound,outbound / inbox,outbox**」要求「总结一下，写到文档里」。落点：[`glossary.md`](glossary.md) **新增 §3.2**（§3.2.1 可见性 vs 传输拿走 / §3.2.2 只用 Channel 不用 Pipe / §3.2.3 方向 vs 落库机构·**Outbox 保不丢 Inbox 保不重** / §3.2.4 速查表）、[`event_bus.md`](event_bus.md) §1.1 指针 + §6 端点契约 + **§9.3 补 Inbox（收侧去重表）**；`..\PLAN.md` v1.21（§6.3 第 24 条）；`index.md` 版本升 0.19 |
| 2026-09-24（二十八轮） | **新增运维篇 [`operations.md`](operations.md)（运维与可观测性）**（作者原话：「关于软件的可监测性，涉及到运维架构。也是我关注的一个重点！1. DevOp，如 jenkins 集成 2. 系统资源、微服务实例、网络、数据流量、报警推送等」「运维的角度会关注：软件生命周期（SLM）……运行监控：基础设施、应用、数据；应急处理：问题定位、响应处理、分析 KPI」「跟开源的 Zabbix，Prometheus，Open Falcon 等如何能集成」，并给出**五层监控图**）：五层监控模型与责任归属（底座只做中间三层）、**零手写打点**（业务层 = `Action` 元数据、应用层 = `runtime.md` §1 四层、数据层 = Repository / 缓存横切面）、三支柱与三个关联键（`traceId` / `eventId` / `correlationId`）、四个标准出口（OpenMetrics `/metrics` · OTLP 1.11.0 · 告警规则文件 · 推送）、三家开源对照（Prometheus 拉取 / Zabbix HTTP agent + Prometheus pattern 或 trapper / Open-Falcon 已转向夜莺走 Prometheus-Like·Remote Write）、Jenkins 三种集成形态（零私有插件）、CI 输出契约（JUnit XML / SARIF / `quality-report.json` / `mmda diff` / `SHA256SUMS`）、配置漂移检测、应急 SOP 与 DORA KPI、运维面安全与离线交付；[`readme.md`](readme.md) 导读 + [`index.md`](index.md) 阅读顺序第 21 篇（glossary → 22 / errata → 23）+ [`quality.md`](quality.md) §2.3 指针 + [`event_bus.md`](event_bus.md) §12 指针；`..\PLAN.md` v1.22（§6.3 第 25 条）；`index.md` 版本升 0.20 |
| 2026-09-24（二十九轮） | **命名约定（标识符与生成代码）**（作者原话：「接口全部 I 开头……以 `Impl` 结尾的有点啰嗦。类和对象都是 Pascal 命名，字段、属性都是小写开头的 camel 命名，其他的可以尊重 java、c# 和不同编程语言的习惯」）：**新增 [`naming.md`](naming.md)（§0–§7，命名真源）**——两条判据（契约名三端逐字一致 / 其余随本地习惯）、硬规则（接口 `I` + Pascal、实现类禁 `Impl`、类与对象 Pascal、字段与属性 camel、枚举成员 Pascal、Handler 接口 `I<事件>Handler`）、禁 `Impl` 四条理由、C# 属性 camel 的代价与收益、生成器与 Profile 责任（契约名不许有端开关）、检查落点、待裁 7 条；**收拢散落口径**：[`records.md`](records.md) §1.1、[`project.md`](project.md)、[`statements.md`](statements.md)、[`templates/conventions.template.md`](templates/conventions.template.md) 改为指针；[`targets.md`](targets.md) §5 L3 判定增**标识符逐字对账**；[`glossary.md`](glossary.md) §3.2 末加「词 vs 怎么写」分工指针；`..\PLAN.md` v1.23（§6.3 第 26 条）；`index.md` 版本升 0.21（第 23 篇，errata → 24） **※ 其中「枚举成员 Pascal」已于第三十轮改判为 `UPPER_SNAKE`。** |
| 2026-09-24（三十轮） | **命名约定补裁：常量与枚举成员 = `UPPER_SNAKE`**（作者原话：「**常量和枚举成员全部大写，单词之间用 _ 隔开**」）：[`naming.md`](naming.md) **§1 新增「常量」行、并把「枚举成员」由 PascalCase 改为 `UPPER_SNAKE`**（**三端一致**；枚举成员名进载荷 → 属契约名，按名序列化即为 `DRAFT` 这样的字符串）、§2 契约名清单新增常量行、§3.1「随各端习惯」清单**删去常量行**（改为三端统一）、§6 **原待裁 1 标已裁（取 B）**；`..\PLAN.md` v1.24（§6.3-26 补第 ⑥ 项，待裁减为 6 条）；`index.md` 版本升 0.22 |
| 2026-09-24（三十一轮） | **数据库标识符 = 模型名（便于 ORM 一致性）**（作者原话：「**sql 字段命名同属性，表名同类名，视图也是和表一样，便于 orm 的一致性**」）：[`naming.md`](naming.md) **§0 判据② 删去「SQL 标识符」并把 SQL 列为第二类例外**、**§1 硬规则新增「数据库标识符」行**（表 / 视图 = 类名 PascalCase、列 = 属性名 camelCase，不转写不加前缀）、**§2 契约名清单新增一行**（DDL 标识符逐字对账）、§3.1 删去原 `snake_case` 行、**新增 §3.3 数据库标识符**（规则表 + **四条前置条件**：方言引号 / MySQL `lower_case_table_names=0` / 达梦 `CASE_SENSITIVE` 进 Profile / **ORM 关掉 camelCase→snake_case 自动转写** + 代价与收益 + `mmda doctor` 自检）、§6 原待裁 5 标已裁（取 B+）+ 新增待裁 8（约束 / 索引名格式）；`..\PLAN.md` v1.25（§6.3-26 补第 ⑦ 项）；本文件 §三-30 ⑤ 划掉、标题改 8 条、新增 ⑧；`index.md` 版本升 0.23 |
| 2026-09-24（三十二轮） | **命名约定 6 条收口**（作者取「**1 A, 2 A, 3A, 4 B, 5 A**」，第 6 项「**不做硬约束，建议遵循前缀 `IDX_`, `FUNC_`, `PROC_` 开头**」）：[`naming.md`](naming.md) **§6 待裁清零**（①②③④⑤⑥⑦⑧ 全部标已裁并写回裁决理由）、**§1「不溯及既往」补「另提供迁移脚本」**（4B）、**§3.2 改写为「✔ 不放宽」**（2A）、**§3.3 约束 / 索引名行改「不做硬约束 + 建议前缀 `IDX_` / `FUNC_` / `PROC_`」**（第 6 项）、**§5 落点表补「迁移脚本（P9，dry-run 出清单）」行 + 检查强度写死 warning 且不进硬门禁 + 建议类只出 suggestion**；`..\PLAN.md` v1.26（§6.3-26 补第 ⑧ 项、待裁 0 条）；本文件 §三-30 整条标已裁（逐条结果入小节）；`index.md` 版本升 0.24 |
| 2026-09-24（三十三轮） | **前端与 Flutter 的本地命名 = 尊重生态**（作者原话：「**前端和 flutter 尊重他们的习惯，允许 snake 命名法，包括 `-` 和下划线，例如组件名称，css bem**」）：[`naming.md`](naming.md) **§0 补「一类放宽」**（前端 / Flutter 本地名允许 `-` 与 `_`）、**§3.1 随各端清单新增一行**、**新增 §3.4「前端与 Flutter 的本地命名」**（组件名 Pascal 或 kebab 均可 · 文件名与目录名 · **CSS BEM 的 `__` / `--`** · 静态资源名 · **Dart 文件名 `snake_case`**；**边界四条**：不进契约（[`presentation.md`](presentation.md) §5「`MetaUi` 里不许出现组件名 / CSS / 事件名」）· `MetaUi` 与载荷名仍按 §1/§2 · **Dart 类名仍 Pascal、成员仍 camel** · 理由 = 这些名字由工具链消费且契约面在后端）；`..\PLAN.md` v1.27（§6.3-26 补第 ⑨ 项）；`index.md` 版本升 0.25 |
| 2026-09-24（三十四轮） | **约束与索引名的前缀建议补齐**（作者原话：「**数据库主键 PK_, 外键 FK_ 开头，默认值 DF_ 开头是推荐的习惯**」）：[`naming.md`](naming.md) **§3.3 规则表拆细**——主键 `PK_` / 外键 `FK_` / **默认值约束 `DF_`** / 索引 `IDX_` / 函数 `FUNC_` / 存储过程 `PROC_`（**全部为建议、不做硬约束**；**唯一约束与检查约束未提、暂无建议**），并补三条注：**前缀后照抄模型名** · **要拿到这些名字生成器须显式 `CONSTRAINT <名>`**（SQL Server 不显式命名会自生成 `DF__表__列__<hash>`，EF Core 到 10 才支持显式命名）· **约束名与索引名不进 L3 对账**；[`targets.md`](targets.md) §5 L3 判定同步该例外；§6 第 8 行改写；`..\PLAN.md` v1.28（§6.3-26 补第 ⑩ 项）；`index.md` 版本升 0.26 |
| 2026-09-24（三十五轮） | **旧版《MMDA 快速上手》精华并入上手篇**（作者：「我在 `D:\项目\2026 mmda\md` 目录下写过快速上手，请你查阅，把有价值的内容加进文档落盘」）：[`guide/quickstart.md`](guide/quickstart.md) **扩为 §0–§12**（§0 五阶段全貌 + 原图 `assets/mmda-dev-workflow.png`、§1 八步实操、§4 DDL 注释 → `.m`、§6 旧命令对照、§7 配置面速查、§8 载荷形态、§9 定制点全景、§12 常见坑 6 条）；**新增 `doc/assets/`**（三张作者原图）；`meta-model.md` 补 `fixedFilter` 语义 + Relation 的 `displayLabel`/`relationIdx`；`legacy/runtime-java.md` 补 `setCustomEditor`/`setCustomRenderer`；**`vision.md` 新增 §0「思想源头：没有银弹」**；本文件 §五-26 + **§三-31（新开 3 条）**；`..\PLAN.md` v1.29（§6.3-27）；`index.md` 版本升 0.27 |
| 2026-09-24（三十六轮） | **旧版手册并入后的 3 条议题裁决**（作者原话：「1. 不算 / 2. +beforeSearch / 3. 保留，如果改约定动作太大」）：[`presentation.md`](presentation.md) §1 补**视图钩子映射**（`beforeSearch` 新增、`report` 暂不设）；[`api.md`](api.md) §3.5 **新增一行「`@Ref` / 枚举的显示标签 → `customProperties.$<字段名>`」**并标已裁保留；[`guide/quickstart.md`](guide/quickstart.md) §9 更新钩子清单并标明**前端工程目录划分不是规范内容**；本文件 §五-27、§三-31 标已裁；`..\PLAN.md` v1.30（§6.3-27 ④ 改已裁）；`index.md` 版本升 0.28 |
| 2026-09-24（三十七轮） | **新增体验篇（`doc/ux.md`）—— UX 落成**（作者：「我在 `D:\项目\UX` 目录下有关于前端和用户体验的学习，我希望你读一下，把有价值的内容落盘 ux.md」）：材料 = `学习笔记.docx`（2,092 字 / 31 段 / 6 张内嵌图，经 docx 内部 XML 提取）+ `UX 广义.webp` / `UX 狭义.webp`（视觉核读）+ `设计心理学.pdf`（247 页扫描件，逐页渲染核读版权页与序）。[`ux.md`](ux.md) **新增（§0–§12）**：术语（UX vs Usability vs ISO 25010 交互能力）、**体验四层环**、**认知心理学五条判据**（示能/意符 · 映射 · 反馈 · 概念模型 · 人的差错）、**尼尔森十大原则 → A/B 可判定信号清单**、**UCD 六阶段 → 五阶段对照**（指出缺「上线度量回流」）、**MVP 金字塔**、**Lean / Agile / 传统 UX 三方对照**、服务设计边界、**包容性三条可判定检查**、**一手材料与版权边界**（源材料不进仓）、读书清单、待裁 4 条；[`readme.md`](readme.md) 导读加指针；[`index.md`](index.md) 阅读顺序**插入第 22 篇**（glossary → 23 / naming → 24 / errata → 25）；本文件 §五-28 + **§三-32（UX 4 条待裁）**；`..\PLAN.md` v1.31（§2 目录树加 `ux.md`、§6.3-28）；`index.md` 版本升 0.29 |
| 2026-09-24（三十八轮） | **体验面 4 条裁决落定**（作者回「**1 B, 2 B, 3 B, 4 A + B**」）：[`ux.md`](ux.md) **§4 十原则清单标已裁**（A 类 6 条进 `mmda check` 断言集、B 类 4 条进报告清单；并写明**「可判定性等级 A/B」≠「检查强度 warning」**）、**§5 补「回流的两半」表**（静态可判定 → `mmda check` warning；运行期度量 → `mmda ops` 报告，**不许当构建断言**）+ 标出**前置依赖 `mmda ops` 进首版**、**§7 服务设计标已裁**（不进语言 + 插件只读「旅程视图」）、**§8 无障碍标已裁**（AA 可自动化子集出 warning，视觉项留皮肤）、**§11 整节改为「已裁」**（保留原选项与理由供复核）；[`quality.md`](quality.md) §3 加「**体验面（UX）检查的落点**」段（warning 不阻断、不进 `mmda quality gate`）；[`operations.md`](operations.md) §11-5 标注**被 `ux.md` 1B 引为前置**；[`ide/plugins.md`](ide/plugins.md) §10 扩展点新增 **`journeyViews`**（只读展示面）；本文件 §五-29 + §三-32 标已裁；`..\PLAN.md` v1.32（§6.3-28 补第 ⑫ 项）；`index.md` 版本升 0.30 |
| 2026-09-24（三十九轮） | **API 契约 11 条裁决落定**（作者逐条回：「1. 同意你的建议 / 2. 同意你的建议，`/模块路径/资源`，我们是 `/service/repository` / 3.–9. 同意 / 12. 我希望是 `moduleName_featureName_op` / 13. 这个我们已经实现，按照现状来，模块权限，Action 权限 / 14. 不明白」）：[`api.md`](api.md) **§8.2 由「待裁」改为「✔ 已裁」四列台账**（# 议题 裁决 落点）——**#2 增加作者口径「路径两段 = service / repository」并新开残留项 2b（路径段与 `operationId` 的大小写 / 复数规则）**、**#12 `operationId` = `moduleName_featureName_op`**、**#13 scope 粒度 = 模块权限 + Action 权限（Feature 级不出 scope）**、**#14 `webhooks` 标「仍待裁 —— 作者回『不明白』」**；正文接线：§1.1（`Base` 与 `sops` → 权限映射，**作者「还没想清楚」的部分清掉**）、§1.2（`expose` 默认 `internal`、稳定度进语言）、§3.3（`operationId`）、§3.4（路径两段）、§3.6（scope 粒度）、§3.9（Mock IDE 内置）、§5（网关边界）、§6（逆向导入先出报告）；[`naming.md`](naming.md) §2 与 §8 相关行同步（**路径段与 `operationId` 不许出现 `Impl`** 的明令保留）；本文件 §五-30 + §三-23 标已裁；`..\PLAN.md` v1.33（§6.2-25 更新为「16 条中 15 条已裁」、§6.3-29）；`index.md` 版本升 0.31 |
| 2026-09-24（四十轮） | **API 契约余下两条收口**（作者回：「**webhooks `GET /events/mes/WorkOrders` 这样的习惯，我选择 B**」+「**我现在 api 是：`GET /api/mes/WorkOrders` 复数形式**」）：[`api.md`](api.md) **§8.2-14 取 B**（**首版不带 `webhooks`**，对外事件走拉取式端点 `GET /events/<模块>/<资源复数>`，订阅 / 重试 / 签名留 `event_bus.md` §15）、**§8.2-2 字形定为「复数」**（**`/api/<模块小写>/<模型名复数>`**，例 `GET /api/mes/WorkOrders`）+ **新开残余细则 2c**（`/api` 前缀写死还是 Profile 可配、复数变形规则）；正文接线：§3.4 表前新增「**路径形态**」说明 + 表内端点示例改复数、**§3.3 `operationId` 示例改 `mes_WorkOrder_create`**（字形 = 模块名照抄模块段、Feature 名照抄模型名）；[`naming.md`](naming.md) §2 与 §8 同步；本文件 §五-30 + §三-23 更新（**§8.2 至此 11 条全部已裁、无余项**）；`..\PLAN.md` v1.34（§6.2-25 更新为「**16 条全部已裁**」、§6.3-29 更新）；`index.md` 版本升 0.32 |
| 2026-09-24（四十一轮） | **运行架构 10 条裁决落定**（作者逐条回：「1. ts 实际上可以自定义…但是 java/c# 我不想用反射，所以程序都写在 action 里面，专门有一个 canExecute 函数。后面统一，现在前端 EntityAction 里面有 canDo 函数，后续重构统一 canDo 函数，然后支持 m 语言写逻辑，其他还是固定，例如 beforeDelete / 2. 不要新增，标准的几个定了 beforeXxx/afterXxx，action 走 canDo 拦截 / 3. save 只提供给程序员方便，但是 insert, update 在服务器端还是得有 / 4. 没明白啥意思 / 5. 缓存策略在设计里可以声明默认策略，后面加语言级定义。但是允许程序员重写 Profile / 6. EntityFactory / Repository 都要，前者更像 .Net EF，后者可基于 EntityFactory 实现，javaer 更习惯。目标是消灭手写 SQL 语句，手写字符串字段名 / 7. 同意，比如 2 阶段提交或者分布式事务，这个具体实现 / 8. 可以 / 9. 同意 / 10. 同意，这个得详细设计」）：[`runtime.md`](runtime.md) **§9 由「待裁」改为「✔ 已裁」四列台账** + **新增 §4.4 解释 `store` 级钩子**（A 不开放 / B 开放，④ 待作者选）；正文接线：§3（跨模块新事务 + 2PC 属实现）、**§4.2 新增「封闭枚举与 Action 的准入」段**（`canDo` 口径 + 三端一致化 + 禁反射）、§4.2 `save` / `action` / `store` 行、§5（缓存策略：设计声明默认 + Profile 可重写）、**§6 新增「数据访问的两层：`EntityFactory` 与 `Repository`」+ 硬目标（消灭手写 SQL 与字符串字段名）**、§7.3 第 2 条、§7.4（隔离级别 + 一插件多 module）、§8 缺口表 `EntityFactory` 行；本文件 §五-31 + §三-20/21/22；`..\PLAN.md` v1.35（§6.2-24 标已裁、§6.3-30）；`index.md` 版本升 0.33 |
| 2026-09-24（四十二轮） | **运维面 8 条裁决落定**（作者取 `1A 2A 3A 4A 5A 6A 7A 8A`——全部取建议档）——① **`operations.md` §11** 整节由「待裁」改为「✔ 已裁」四列台账（含作者原话）；② 就地标注 §2 第三条、§4.1 指标 / 遥测两行、§4.2 Zabbix / Open-Falcon 两行、§9 离线交付包 + `mmda doctor`、§10 `mmda ops` 行与 P9 建议段；③ **`ux.md` §11** 标注 **1B 的前置已于本日满足**（`mmda ops` 进首版）；④ **`PLAN.md`** 抬头改 **v1.36 / 三十一项裁决**、§3.11 P9 落点、§4 阶段表 P9 行、§6.2-25 标已裁、**§6.3 新增第 31 条**、变更记录 v1.36；⑤ `index.md` 版本升 **0.34**；⑥ 本文件 §三-29 标已裁 + §五 新增第 32 条 |
| 2026-09-24（四十三轮） | **事件总线与集成编排 9 条裁决落定**（作者取「**1A, 2B, 4A, 5A, 6A,7A,8C, 9A端点是要在API的基础上增加定义数据的转化、过滤规则的**」，**9 条全部已裁**；第 3 条先标「待定」、随后由作者另给方案（`.mf` = 数据流、`.mb` = 跨模块流程），见四十四轮）——① [`event_bus.md`](event_bus.md) **§15 由「待裁」改为「✔ 已裁（仅第 3 条待定）」四列台账**（含作者原话）；正文接线 §1.1（术语映射行 + 拓扑 ASCII 图）、§4.1（执行层方块内端点写法）、§5.3（**引擎 = 内嵌轻量执行器**，1A）、**§6（新增「端点 = API + 集成定义」定义块（9A）；外部端点定义改为「反向导入成语言声明」（2B））**、§7.2（数据映射**可含**外部报文形态，7A）、§7.3（出口行 + 到货例图元）、§9.1（迟到数据默认策略，6A）、§9.3（端到端一致性档，4A）、§10（多租户，5A）；② **术语改名 `Sink` → `EventSink`**（8C，与 `EventSource` 对称，全仓现行文档一致：[`glossary.md`](glossary.md) §3.1 / §3.2、[`events.md`](events.md) §2、[`ide/diagrams.md`](ide/diagrams.md) §4、[`index.md`](index.md) 第 23 篇、`event_bus.md` §1.1 / §4.1 / §7.3）；引用 Flink / Kafka 官方词处保留 `Sink`；③ [`operations.md`](operations.md) §9 的多租户三档**随 §15-5 一并收口**；④ `..\PLAN.md` **v1.37**（§6.2-22 标已裁、§6.3 新增第 32 条、抬头「三十二项裁决」）；⑤ [`index.md`](index.md) 版本升 **0.35**；⑥ 本文件 **§三-27 / §三-28 标已裁 + §五 新增第 33 条** |
| 2026-09-24（四十四轮） | **扩展名职责重划：`.mf` = 数据流（DataFlow）、`.mb` = 跨模块流程（BPMN）**（作者原话：「**我想把 `.mf` 给数据流图用，跨模块流程 `.mb`**」）——① 真源 [`project.md`](project.md)：§1.1 分区表（「数据流 · `flow/` · `.mf`」「跨模块流程 · `flow/` · `.mb`」，原「工作流 `.mf`」行拆二）、**§1.2 扩展名总表**（`.mf` = 数据流、新增 `.mb` = Meta BPMN / partType `bpmn`、`.g` 族 → `{ma, mm, ms, mf, mb}`）、§1.2 注（扩裁口径）、§1.4 core 打包 glob；② [`glossary.md`](glossary.md) §5 分片行 + §6 速查表；③ [`design-notes.md`](design-notes.md) 目录树 + 扩展名表（留改判注）；④ **图元落点收口**：[`ide/graph-files.md`](ide/graph-files.md) 原则表 SSOT 行、§2 统一规则 / 命名表 / Glob、§3 partType、§4.1 `views` 行、**§4.4 图元表拆成 `mf-flow` / `mf-dfd` / `mf-map` + `mb-bpmn`**、**§4.5 由「`.mf.g` 的 DFD + BPMN」改为「数据流 `.mf.g`（三 sheet）＋ `.mb.g`（BPMN）」**、§6 对照表、§7 插件目录、§8 命名表与同步模块、尾部 ⏳ 规划行改 ✔ 已裁；⑤ [`ide/diagrams.md`](ide/diagrams.md) §4、[`ide/diagram-adapters.md`](ide/diagram-adapters.md) §（adapter 表加 `mf-flow` / `mf-map` / `mb-bpmn`）、[`ide/workflow.md`](ide/workflow.md)（第 4 步资源 + §4.2 数据流 + §4.3 工作流）、[`ide/plugins.md`](ide/plugins.md) §4 / §11.2 后缀白名单；⑥ 上下游引用同步：[`workflows.md`](workflows.md)（权限表 + 两处交付检查项）、[`testing.md`](testing.md)（BPMN 用例行改 `.mb`、新增数据流用例行）、[`requirements.md`](requirements.md)（SERU 的 E 与用户需求行）、[`architecture-review.md`](architecture-review.md)（数据流证据改 `flow/*.mf`）、[`readme.md`](readme.md) 目录树；⑦ `PLAN.md` v1.38 / §6.2-22 ⑤ / §6.3-33 / 抬头；⑧ [`index.md`](index.md) 版本升 0.36；⑨ 本文件 §三-27 收口 + §五-33 ③ 就地改判 + **§五 新增第 34 条** |
| 2026-09-24（四十五轮） | **运行架构 §9-4 补裁（取 A）+ 新开「嵌入式脚本引擎」待裁**（作者原话：「**runtime store钩子选A**」；「**实际上 service 层也有钩子，是给业务逻辑用的。我原先想能否有脚本执行引擎，嵌入到 java、c# 后台，能够写一些简单的脚本，起到类似数据库的触发器的作用。技术上有没有可能？数据库的触发器没有可移植性，换数据库就废了**」）——① [`runtime.md`](runtime.md) **§4.4 由「⏳ 待裁」改「✔ 已裁取 A」**（不开放：只给程序员 KEEP 区 + 显式勾选 + 评审可见；语言/模型侧不给 `store` 钩子声明语法，保住 [`quality.md`](quality.md) L3 逐字对账）；② **§9 表第 4 条标已裁**（至此 §9 十条**全部已裁**）；③ **§4.2 表下新增一行注**：两个「钩子的家」——`beforeXxx`/`afterXxx` 住 **Service**（业务逻辑）、`store` 住 **Repository**（存储语义）；④ **新增 [`runtime.md`](runtime.md) §4.5「钩子里能写什么」**：技术事实（**Nashorn 随 JDK 15 移除 JEP 372**、GraalJS Maven 包但 **stock JDK 不受支持**、.NET Roslyn Scripting / Jint / ClearScript、**脚本引擎的限额不是隔离边界**）+ 三条路（**A 受限脚本 = 自己的 Rust 内核求值器**（建议，落 P6 嵌入通道）／B 宿主脚本引擎／C 不做脚本）+ 「替代 DB 触发器」的抓手（**拦截点 + Outbox 已是可移植版触发器**）+ 两条待裁问题（取哪档 / 能力清单边界）；⑤ 本文件 §三 新增第 32 条（该议题登记为**待裁**）；⑥ `PLAN.md` v1.39（§6.2-24 标全裁、§6.3-30 ④ 就地补裁、变更记录 49）；⑦ [`index.md`](index.md) 版本升 0.37 |
| 2026-09-24（四十六轮） | **嵌入式脚本执行引擎落定（取 A）+ 脚本能力机制落成**（作者原话：「**2要允许跨模块调用，能访问所有 Entity，例如查询，条件更新，删，能力我可以不断增加，都是函数**」）——① [`runtime.md`](runtime.md) **§4.5 由「⏳ 待裁」整节改写为「✔ 已裁取 A」**：脚本 = **m 语言受限子集**、Rust 内核求值、走 **P6 嵌入通道**（不引入宿主脚本引擎，附 Nashorn / GraalJS / .NET 三方案脚注）；**作者三条口径**（允许跨模块调用 → 事务按 §9-7 跨模块新事务；全 Entity 访问 → 查询 / 条件更新 / 条件删除经内核代理、不生成裸 SQL；能力 = 内核函数）；**两层红线**（**表达式层仍纯函数**（1A 不受影响）vs **脚本层可读可写**）与**脚本层仍禁**（任意 IO / 反射 / 随机与时间依赖）；**语言层新增 = 0**（语法形态归语法专题）；② **新增 [`runtime.md`](runtime.md) §4.6「脚本能力清单：一个能力 = 一个内核函数」**（命名 `script.<域>.<动作>`、`capability` 声明制、未声明即生成期报错、只增不改名、首版起始清单五域：查询 / 写入 / 上下文 / 事件 / 日志）；③ [`glossary.md`](glossary.md) §2 新增三个术语（**表达式层 / 脚本层 / 能力函数**）；④ [`runtime.md`](runtime.md) §4.2 注指向 §4.5 / §4.6；⑤ 本文件 §三-32 标已裁 + **§五 新增第 35 条**；⑥ `PLAN.md` v1.40（§6.2-26 标已裁、§6.3-34 新增、抬头「三十四项」、变更记录 50）；⑦ [`index.md`](index.md) 版本升 0.38 |
| 2026-09-24（四十七轮） | **脚本的查询形态与兜底层登记为待裁**（作者原话：「还有一种可能，给上下文后，写类 SQL 的语句，然后 C#, java 都有 sql 包，能自动翻译执行，也是很好」；「或者干脆注入 EntityFactory，直接 java/c# 写」）——`runtime.md` **§4.6 追加「查询形态与兜底层」**（两条路线 + 范围建议 + 四档分层结论）；[`errata.md`](errata.md) §三 新增第 33 条；`PLAN.md` §6.2 新增未决项 27、升 **v1.41**（变更记录 51）；`doc/index.md` 升 **0.39**。**已核事实**：jOOQ **开源版只覆盖开源库**（SQL Server / 达梦 / 金仓不在其列，商业库需 99 / 399 / 799 € 授权）；**两侧现有 ORM 不同构**——Java 底座 = **Spring Data JPA**（11 个模块 pom 命中）、C# 底座 = **Dapper 2.1.35**（14 处 `PackageReference`，**字符串 SQL + 对象映射、无查询 DSL 翻译能力**）。 |
| 2026-09-24（四十八轮） | **「宿主语言运行期编译（A′）」登记为待裁**（作者原话：「Java Compiler API」「Roslyn / DLR」）——`runtime.md` §4.6 追加第 3 条并记入**本机实测**（JDK 17 / **JDK 21** 双跑：内存内编译一个小脚本 **34 / 23 / 24 ms**、318 B、可调用；**脚本能读环境变量、能列宿主目录、能拿进程号、能 `Runtime.exec` 起进程** → **无沙箱**；`ForwardingJavaFileManager` 走内存字节码，`ToolProvider.getSystemJavaCompiler()` 在纯 JRE 上为 `null`）；[`errata.md`](errata.md) §三 新增第 34 条；`PLAN.md` §6.2 新增未决项 28、升 **v1.42**（变更记录 52）；`doc/index.md` 升 **0.40**。 |
| 2026-09-24（四十九轮） | **脚本语言维持 A + 类 SQL 归 m 语言未来语法 + A′ 不做脚本语言**（作者原话：「维持A, m语言，未来如果支持类SQL语法也是有可能的」）——`runtime.md` §4.5 重申「维持 A」（在 Java Compiler API / Roslyn 实测之后）、**§4.6（1）类 SQL 标已裁**（方向 = m 语言未来的查询语法，不做宿主 SQL 包翻译层、首版不进、语法归语法专题）、**§4.6（2）兜底层标已裁**（KEEP 区）、**§4.6（3）A′ 标已裁**（不做脚本语言，保留为 P6 之后的 KEEP 区运行期加载候选）；本文件 §三-33 / §三-34 标已裁 + §五 新增第 36 条；`PLAN.md` §6.2-27 / §6.2-28 标已裁、§6.3 新增第 35 条、抬升 **v1.43**（变更记录 53）；`doc/index.md` 升 **0.41**。 |
| 2026-09-24（五十轮） | **脚本运行身份取 C**（作者原话：「C」）——`runtime.md` §4.5 尾由「⏳ 仍待裁一条」改为 **✔ 已裁取 C**（默认继承调用者 + 可选 `runAs: system`；声明进评审清单 + `mmda check` warning；身份枚举封闭 `caller` / `system`；`system` 不受数据范围但仍受功能开关与审计、必须记 `actor`；租户键仍必须有）；**脚本引擎议题至此全部清零**；`PLAN.md` §6.2-26 尾与 §6.3-34 就地补裁、§6.3 新增第 36 条、升 **v1.44**（变更记录 54）；`doc/index.md` 升 **0.42**。 |
| 2026-09-24（五十一轮） | **API 2c 取 1A 2A + 插件口径补两条 + Flutter 早期实现**（作者原话：「1. 1A 2A 实际上我在 module.moduleUrl 中配置了，复数形式遵循英文单词，前后端都有实现」；「flutter 我也有早期版本的实现，后面再说。业务插件是指用户用 java, c#, ts 做的，目前不考虑收费和市场」）——`api.md` §3.4 + §8.2-2 标 ✔ 已裁（前缀按模块 `moduleUrl` 配置、复数遵循英文单词且前后端同一实现）、`meta-model.md` `moduleUrl` 行补注；`runtime.md` §7 补「插件 = 用户用 Java / C# / TS 写的」+「不做收费与市场」；`ide/plugins.md` §9 改判 ⏸ 暂缓（§8 / §9.1 加注）；`vision.md` 共赢行、`protection.md` 分发渠道项加注；本文件 §五 新增第 38 / 39 条、§三-25 / §三-26 部分收口；`PLAN.md` §6.2-17 / §6.2-20 / §6.2-25 加注、§6.3 新增第 37 / 38 条、升 **v1.45**（变更记录 55）；`doc/index.md` 升 **0.43**。 |
| 2026-09-25（五十二轮） | **语法基线取语料（四条结构性冲突一并裁掉）+ `BIGID` 定义为自有类型**（作者原话：「1 语料 / 2. 语料 / 3. 语料 / 4. 按 varchar(80)?」；「另外 bigid 是我们特有的定义，就是指本身是 partitionID，高位存 tenantId」）——冲突 1 / 2 / 4 / 5 标 ✔ 已裁（各带口径与实测数字：`.mm` 215 / `.me` 113 / `.ms` 41 / `.mi` 1 全文本）；§二-6 的 `BIGID` 部分标已裁（余位宽 / 生成方 / 写法待补）；`datatypes.md`、`records.md`、`statements.md`、`project.md`、`ide/README.md`、`glossary.md` 同步；本文件 §五 新增第 40 条；`PLAN.md` §6.1 改写（**P2 闸门解除**）、§6.2-3 标已裁、§6.3 新增第 39 条、升 **v1.46**；`doc/index.md` 升 **0.44**。 |
| 2026-09-25（五十三轮） | **内核侧插件加载形态取 B（WASM 组件）+ §二-1 方向已裁（裸命名约束不进首版、约束一律注解）+ §二 位置列校正**（作者原话：「**B**」；「**1. 约束不一定有阿，未定义即报错你是啥逻辑阿**」）——[`ide/plugins.md`](ide/plugins.md) §12 标 ✔ 已裁取 B（A 降备选、C 仍限官方内置）；本文件 §三-25 ① 部分收口、§五 新增第 41 条；§二-1 改写为「约束可选、**要定的是名字写错怎么办**」并记录语料实测（`positive` / `future` / `cancellable` 零命中，`@Unique` 94 / `@Index` 117 / `@PartitionID` 186 / `@Computed` 28）；**§二 位置列校正**（原指向 `doc/readme.md:39/:41/...` 的行号全部失效 → 重新定位到 `doc/archive/2026-06/language/*` 与 `architect/meta-model.md`）；`PLAN.md` §6.3 新增第 40 条、升 **v1.47**；`doc/index.md` 升 **0.45**。 |
| 2026-09-25（五十四轮） | **`BIGID` 位布局定死 + 约束分字段级 / 对象级两种形态**（作者原话：「int MAX_TENANT_ID = 0x7FF_FFFF;//高28位是租户id」「long MAX_REAL_ID = 0xF_FFFF_FFFFL;//低36位是实际id」；「约束一律走注解？这个不方便，你还要区分字段级和表级约束」「如果是两个字段建立一个索引，那就得单独定义了」）——[`datatypes.md`](datatypes.md) §5 补位布局与生成方；[`records.md`](records.md) §1.2 两层形态改写、**§2.4 多租户布局改正**（原「高 16 位 / 低 48 位」= 旧布局残留）、§3 加「组合约束必须具名声明」；[`glossary.md`](glossary.md) 同步 `BIGID` 行；[`design-notes.md`](design-notes.md) §7.1 补；本文件 §二-1 / §二-6 改写、§五 新增第 42 条；`PLAN.md` §6.3 新增第 41 条、升 **v1.48**；`doc/index.md` 升 **0.46**。 |
| 2026-09-25（五十五轮） | **租户位宽确认 27 位 + `BIGID` 语料形态实测**（作者原话：「**27位没错**」）——[`datatypes.md`](datatypes.md) §5 标 ✔ 并关掉「是否笔误」一条、补语料形态实测（`bigid` 0 命中；`@PartitionID [min,max]` + `uint64 identity` 102 处；`[min,max]` 是每个对象领的 realId 区间）；[`records.md`](records.md) §2.4、[`glossary.md`](glossary.md) §5 同步；本文件 §二-6 收口、§五 新增第 43 条；`PLAN.md` §6.3 新增第 42 条、升 **v1.49**；`doc/index.md` 升 **0.47**。 |
| 2026-09-25（五十六轮） | **标识共享（分段）机制落定 + 语料逐段实测核对**（作者原话：「**有时候我需要多个表 UNION 成视图，不想 id 冲突，所以分段**」）——[`records.md`](records.md) §2.3 补「为什么分段：标识共享」整块（目的 / 两级划分 / 默认段 / 逐段对照表 / 六个组 / 四条新待裁），§7 补「视图与标识共享」；[`datatypes.md`](datatypes.md) §5、[`glossary.md`](glossary.md) §5（新增「标识共享」术语行）、[`meta-model.md`](meta-model.md) §5（`minID`/`maxID` 语义补全）；语料实测：173 张表带 `@PartitionID`，其中 **169 张共用默认段 `[10000, 0x000F_FFFF]`**，人组四段与作者文档**逐一吻合**；本文件 §五 新增第 44 条；`PLAN.md` §6.3 新增第 43 条、升 **v1.50**；`doc/index.md` 升 **0.48**。 |
| 2026-09-25（五十七轮） | **`BIGID` 定为语言类型（`uint64 identity partitioned`）+ 分段配置归属 + 大小写敏感面裁定**（作者原话：「**BIGID 作类型，等价于 uint64 identity partitioned，分段在 metaobject 里面配置 minId, maxId，这是真 id 去掉租户标识的**」；「**类型一律大小写不敏感，包括那些约束，这个跟 SQL 类似**」）——[`datatypes.md`](datatypes.md) §5 把「是否保留 `BIGID`」标 ✔ 并写出等价定义、§2.1 大小写行改写；[`records.md`](records.md) §2.3 补分段配置归属、§1.2 补约束关键字大小写；[`meta-model.md`](meta-model.md) §5 **新增 `partitioned` 行**并把 `minID`/`maxID` 语义补全；[`naming.md`](naming.md) §1 新增「大小写敏感面」；[`guide/quickstart.md`](guide/quickstart.md) 与 [`legacy/import-from-ddl.md`](legacy/import-from-ddl.md) 的**旧布局残留（16/48）已改正 / 标注**；本文件 §二-6 清零、§五 新增第 45 / 46 条；`PLAN.md` §6.3 新增第 44 / 45 条、升 **v1.51**；`doc/index.md` 升 **0.49**。 |
| 2026-09-25（五十八轮） | **标识共享段的分配主体落定（架构师 / 设计师）**（作者原话：「**段是架构师、设计师分配阿**」）——[`workflows.md`](workflows.md) §1 五类职责表下新增归属说明（跨表 / 视图族的段规划 = 架构师；单表在既定段内落地 = 设计师；程序员与 AI Agent 不私下改）；[`meta-model.md`](meta-model.md) §5 `minID`/`maxID` 行、[`datatypes.md`](datatypes.md) §5、[`records.md`](records.md) §2.3 同步（该节「待裁四条」→ **剩三条**）；本文件 §五 新增第 47 条；`PLAN.md` §6.3 新增第 46 条、升 **v1.52**；`doc/index.md` 升 **0.50**。 |
| 2026-09-25（五十九轮） | **自查补正（作者追问「我原来写的例子被你删了？」）** —— 逐提交审计后确认**零删除**（见下），只修我自己没跟上的两处：① [`datatypes.md`](datatypes.md) §5 尾巴原写「⏳ 仍待你定：只剩大小写敏感面…见 §二-6」已过期 → 改 ✔ 已裁（大小写敏感面同日已裁、§二-6 清零）；② [`records.md`](records.md) §2.3 「⏳ 待裁三条」编号仍从 ② 起 → 回正为 ①②③，并把 `@PartitionID` **范围写法**（闭 / 开 / 半开 / `..` 省略一侧）指向 [`design-notes.md`](design-notes.md) 的范围语法表。**审计口径**：「近 8 个提交 `--diff-filter=D` = 零文件删除；`doc/` 被删行仅 5 行（全在 §二 「位置」列，属替换而非删除）」。 |
| 2026-09-25（六十轮） | **标识共享组 = 视图声明（视图即组）**（作者问「**标识共享组在视图那里可否**」）——[`records.md`](records.md) §7 新增整块：语料四个视图名与作者组名的对应、`/// VIEW` + 列形态、`Maintainable` 自带段、`MetaView.java` 缺 UNION 成员概念、三条可校验规则、待裁三点；§2.3「待裁三条」→ **剩两条**（组归属已裁）；[`datatypes.md`](datatypes.md) §5 指向 §7；本文件 §五 新增第 48 条；`PLAN.md` §6.3 新增第 47 条、升 **v1.54**；`doc/index.md` 升 **0.51**。 |
| 2026-09-25（六十一轮） | **术语统一：成员表 / 来源表 → 基础表（base table）**（作者问「**啥叫成员表？**」）——把我在标识共享相关段落里自造的两个词统一为 SQL 现成词：**全仓 `成员表` 16 处 → `基础表`**、**`来源表` 5 处改为明说「来自哪张基础表」的标记列**；[`records.md`](records.md) §2.3 / §7 给出定义（「**基础表 = UNION 进该视图的那几张表**，即 SQL 里 view 的 base table」）；[`glossary.md`](glossary.md) 新增「基础表（base table）」术语行；`PLAN.md` §6.3-43 / 47 台账文案同步、升 **v1.55**；`doc/index.md` 升 **0.52**。 |
| 2026-09-25（六十二轮） | **修正视图一节：基础表就在视图定义的 `join` / `union` 子句里**（作者：「**你看 records.md 322 行，就有视图的定义阿**」）——[`records.md`](records.md) §7 **重排**（视图定义 + `OrderItemV` 例子 + 两条 ⚠️ 回到最前，标识共享块置后）；明确「**基础表 = `join` / `union` 子句里的表**，不用新声明位」；§2.3「待裁两条」文案同步；本文件 §五 新增第 49 条；`PLAN.md` §6.3 新增第 48 条、升 **v1.56**；`doc/index.md` 升 **0.53**。 |
| 2026-09-25（六十三轮） | **视图的 `:` = C# 风格的继承 / 实现（作者澄清语义）** —— [`records.md`](records.md) §7 改写「`:` 的语义」段（基 + 引用别名 + join/union 都是「基」的写法；证据 = 语料唯一的 `view Product : Bom`，`Bom` 是 record）；§7 待裁改为继承语义下三条（列合并冲突规则 / 基能否是视图 / 视图自身要不要段）；[`glossary.md`](glossary.md) 新增 **view 词条**（定义式、非查询式）；本文件 §五 新增第 50 条；`PLAN.md` §6.3 新增第 49 条、升 **v1.57**；`doc/index.md` 升 **0.54**。 |
| 2026-09-25（六十四轮） | **视图定义规则三条落定**（作者原话：「每个表的字段都要写，并且名字要对的上，用 `as`，或者你模仿 SQL」「能」「视图可以要段，大多情况不更新，没必要段」）—— [`records.md`](records.md) §7 「待裁三条」→ **✔ 已裁三条**（逐字段显式写 + `as` 对齐 / 基可以是视图 / 视图段可选）；§2.3 待裁收敛为**一条**（段重叠门禁）；[`glossary.md`](glossary.md) view 词条补三条规则；本文件 §五 新增第 51 条；`PLAN.md` §6.3 新增第 50 条、升 **v1.58**；`doc/index.md` 升 **0.55**。 |
| 2026-09-25（六十五轮） | **段重叠进 `mmda check` 硬门禁**（作者原话：「**同意进**」）—— [`records.md`](records.md) §7 校验段标注门禁级别（error、生成期拦）并把 ②③ 并入同一批校验；§2.3「待裁一条」→ **✔ 全部已裁**；本文件 §五 新增第 52 条；`PLAN.md` §6.3 新增第 51 条、升 **v1.59**；`doc/index.md` 升 **0.56**。**标识共享议题清零。** |
| 2026-09-25（六十六轮） | **未知名 = 解析期 error（关键字表封闭）+ 仓库加 `.gitattributes`** —— ① [`records.md`](records.md) §1.2「仍未定」→ **✔ 已裁**（行尾关键字与注解名都是语言关键字、进词法分析器 token 表；拼错/未知名 = 解析期 error，带 `file:line:col`；**扩展白名单方案作废**；解析器 = P2 `mmda-syntax` 尚未实现）；② [`errata.md`](errata.md) §二-1 尾同步；③ [`datatypes.md`](datatypes.md) §5 把 `D:\2026\java` 标注为**老库**（作者指正）并说明位布局以作者给的常量为准；④ **仓库新增 `.gitattributes`（`* text=auto eol=lf` + 二进制清单）**：`git add --renormalize .` 后**零行尾变更**（blob 本就是 LF），从此不再刷 LF/CRLF 警告；⑤ 本文件 §五 新增第 53 条；`PLAN.md` §6.3 新增第 52 条、升 **v1.60**；`doc/index.md` 升 **0.57**。 |
| 2026-09-25（六十七轮） | **取证口径回正（`D:\2026\java` = 现行实现，老库在 `D:\Java`）**（作者原话：「**`D:\2026` 的是新的，以前老库在 `D:\Java` 下面**」）—— [`datatypes.md`](datatypes.md) §5 真源标注改为现行实现（新库，入库点 `056ef96`）、新库两处旧注释残留（`Tenancy.java:82`「48bits」、`:27`「高 28 位」）写明「以常量为准」；本文件 §五 新增第 54 条；`PLAN.md` §6.3 新增第 53 条、升 **v1.61**；`doc/index.md` 升 **0.58**。 |
| 2026-09-25（六十八轮） | **老库路径 = `D:\Java\mmda`，16+48 旧布局真源取证**（作者：「**老库 `D:\Java\mmda`**」）—— [`datatypes.md`](datatypes.md) §5 补老库路径与**旧布局真源**（老库 `Tenancy.java:14` `0x7FFF` / `:39` `>>> 48` / `:82-83` `<< 48` 与 `0xFFFF_FFFF_FFFFL`），并说明新库 `Tenancy.java:82` / `:27` 的旧话术是**从老库抄来漏改**；本文件 §五 新增第 55 条；`PLAN.md` §6.3 新增第 54 条、升 **v1.62**；`doc/index.md` 升 **0.59**。 |
| 2026-09-25（六十九轮） | **`@PartitionID` → `@Partitioned` 改名 + 字段级 `partitioned`**（作者原话：「**`@PartitionID` 改为 `@Partitioned`，字段级支持 `partitioned` 跟在后面**」）—— ① 规范正文档 **10 文件 49 处改名**（archive 与台账保留旧名，那里是历史与语料原文）；② [`records.md`](records.md) §1.2 字段级关键字表**新增 `partitioned` 行**、§2.3 新增改名说明 + 两种形态 + 唯一性校验；③ [`glossary.md`](glossary.md) 新增 `@Partitioned` / `partitioned` 词条；④ [`datatypes.md`](datatypes.md) §5 回标「语料原文用旧名」；⑤ 本文件 §五 新增第 56 条；`PLAN.md` §6.3 新增第 55 条、升 **v1.63**；`doc/index.md` 升 **0.60**。 |
| 2026-09-25（七十轮） | **旧名迁移命令落定 `mmda migrate --rename`**（作者回「**好**」）—— ① [`records.md`](records.md) §2.3 补迁移条款（默认 dry-run 出清单、`--write` 落盘；只动语言文件；归 P9）；② [`naming.md`](naming.md) §5 迁移脚本行**扩为通用改名**（命名约定 + 关键字/注解改名两类，如 `@PartitionID` → `@Partitioned`）；③ [`project.md`](project.md) 迁移段补语法级改名迁移（旧项目升级必经）＋与 [`naming.md`](naming.md) §5 同一条线；④ 本文件 §五 新增第 57 条；`PLAN.md` §6.3 新增第 56 条、升 **v1.64**；`doc/index.md` 升 **0.61**。 |
| 2026-09-25（七十一轮） | **撤销误读 + 真问题登记**：助手把作者「**存 m 语言就用 `.m`**」误读为「整个扩展名族收敛为 `.m`」并落盘（`d67fe7e`）—— 作者纠正：「**你误解我的意思了，没叫你改原来的，我是想那些拦截器，是不是纯脚本程序阿，用 `*.m`**」→ **`df4dc99`（`git revert d67fe7e`）整体撤销**，**分片扩展名族与 `errata` 冲突 3 原裁决恢复生效**（收敛表 / `glossary` `.m` 词条 / `ide/README` 注 / §五-58 全部撤回）；真问题登记为 §二 第 13 条（**拦截器 / 钩子这类纯脚本是否用 `.m`**）；`PLAN.md` §6.1 第 13 条同步、升 **v1.65**；`doc/index.md` 升 **0.62**。 |

> 注：§五 记录的是**已裁决**项，一、二 两节保留**未裁决**项——两者不要混读。

---

## 五、✔ 已裁决（2026-09-24）

| # | 议题 | 裁决 | 落点 |
| --- | --- | --- | --- |
| 1 | `.mmda` 后缀（冲突 3） | 保持扩展名族，`.mmda` 只表示项目清单；**解析器按内容首关键字判 partType** | 本文冲突 3、[`project.md`](project.md) 抬头 |
| 2 | 能力标记 capability 语法 | 按 [`targets.md`](targets.md) §3 草案进语言；目标缺能力 → 生成期报错 | `targets.md` §3/§8、`..\PLAN.md` §6.3-2 |
| 3 | UI 契约 | **不统一 UI 抽象**：后端只出渲染描述，各端 kit 自己渲染（**同日已收紧**：UI 契约 = 现有 mmda-vue 前端项目，不考虑 C# MVC 与 Java 的 UI，后端只提供 `MetaUi`——见第 8 条） | [`presentation.md`](presentation.md) §5.1、`..\PLAN.md` §6.3-3 |
| 4 | 一致性测试归属 | **由 Rust 内核统一驱动**（`mmda test --target java,csharp,ts`） | `..\PLAN.md` §6.3-4、P9 |
| 5 | 职责、模式与权限配置 | **五类职责不按岗位划分**（一人可兼任业务 + 技术）；**模式与角色权限在 IDE 项目管理里配置**，导航与写入边界按角色权限限制；默认 `mode = 单人`，门禁不因模式而变 | [`workflows.md`](workflows.md) §1.1 / §1.2 / §1.3 |
| 6 | 测试用例与验收 | **`.mt` 进语言族**；覆盖率门禁（字段/迁移/权限/能力 100%、需求 ≥ 90%）；**AI 用例业务 + 设计师双签**；变异里程碑 + 每日（存活 ≤ 10%）；**中文验收单作为验收凭证**（签字留痕进 changelog）；性能/安全用例分开 | [`testing.md`](testing.md) §11 已裁表、[`workflows.md`](workflows.md) §12 |
| 7 | 架构评估口径 | **首版架构硬门禁 = ARCH-101 / 102 / 104 / 105 + 302 / 304 / 305**（ARCH-2xx 先告警一个迭代、ARCH-5xx 只进看板）；**抽象度 `A` = 对外契约数 /（对外契约数 + 具体对象数）**（契约 = Action + 视图 + 被外部引用的 Record/Enum） | [`architecture-review.md`](architecture-review.md) §5 / §8 |
| 8 | UI 契约（收紧） | **UI 契约 = 现有 mmda-vue 前端项目**（`D:\2026\ts\mmda`）；**不考虑 C# MVC 与 Java 的 UI**；**后端只提供 `MetaUi` 元数据**（Java `MetaUi.java` 族 / C# `MetaUi.cs` 族）。UI kit 不进契约、不进语言；一致性测试 UI 维度只测 TS；`capability ui` 只在 `target ts`——**补裁：前端 kit `vui`（Vue）与 `rui`（React）都可选、随技术人员喜好**（自定义前端 UI 插件时按熟悉度挑），属同一前端项目内部自由，**不构成两套 UI 契约** | [`presentation.md`](presentation.md) §5.1、[`targets.md`](targets.md) §8-4 |
| 9 | 设计器宿主形态 | **独立壳优先**（Tauri + Vue 3 + Syncfusion，本阶段唯一在做的宿主）；**VS Code / IDEA 插件作为后续可选宿主**——内核（`mmda-core` Rust）与图形接口按「宿主可替换」设计（LSP 面 + `*.g` 布局/语义分离），但**不并行开工两套宿主** | [`ide/specification.md`](ide/specification.md) §4.9、`..\PLAN.md` §6.2-22 |
| 10 | 组织架构建模 | **组织架构 / 岗位 / 职员是「数据」不是「元数据」**——用普通 `Record` + 关系表达（部门树、岗位、任职），**不新增元模型元素**；设计器提供图形化输入（组织架构图 / 岗位矩阵图）与职员清单导入，**产物是数据模型文件**；余：「角色由岗位生成」的映射规则待裁 | [`ide/specification.md`](ide/specification.md) §4.8 域 2、[`workflows.md`](workflows.md) §1.2 |
| 11 | BI 元数据 | **BI 要有元数据、最终让架构设计师能建模**（属**元模型**范畴，不是纯 IDE 工具面）——**方向已裁**；**但既有 `MetaBiCube/Dimension/Hierarchy/Level/Measure` 很不成熟 → 具体形态与是否移植「容后再议」**（ClickHouse 是否作默认分析存储同此）。可先落：**枚举 → 维度**、固定维度模型 | [`ide/specification.md`](ide/specification.md) §4.8 域 4/域 8、`..\PLAN.md` §6.2-23 |
| 12 | 角色（Role）的层级 | **Role 是架构设计元素、进 m 语言，与 Module 分解同级**（`flow/roles/*.mr`）——**需求阶段识别关键用户（关键用户即角色）**，故架构设计时 Role 与 Module 同等重要；**组织架构 / 岗位 / 职员仍是「数据」**，是角色的实例来源（设计元素 vs 数据实例，两层不混）。余：**岗位 → Role 的映射规则**待裁（一个岗位可对应多个 Role） | [`meta-model.md`](meta-model.md) §8.1、[`project.md`](project.md) §3、[`ide/specification.md`](ide/specification.md) §4.8 域 2、`..\PLAN.md` §6.3-8 |
| 13 | 运行架构（四层职责） | **概念设计已确认**：`Controller` = **API 开放**、`Service` = **商业逻辑**、`Repository` = **数据读写层**、**缓存 = 横切面**（`CacheProvider`，Controller 与 Service 两侧直达、`reactive` 双向、**不绕 Repository**）；**事务夹在 Service 的 Before / After 拦截点中间**（`before*` 事务内、`after*` 提交后且必须幂等）；进入路径三件事（认证授权 / 校验 / 默认值）**在 MMDA 里是声明、Controller 是执行点**；返回路径的**组装与聚合是生成物** | [`runtime.md`](runtime.md) §1–§6、[`architecture-review.md`](architecture-review.md) §2.1b（ARCH-107…110）、`..\PLAN.md` §6.3-9 |
| 14 | API 边界与插件边界 | **module 边界 = API 边界 = 权限边界 = 文档分组边界（四者一个来源）**——权限按 module 授予（`Role.auth module`/`actions`/`scope`）+ `sops` + ARCH-104/106 ⇒ **无归属的元对象结构上不可开放**；**插件不得侵入语言**：只许**读产物**（OpenAPI/`MetaUi`）与**报对账**，**禁止写元数据、禁止把工具概念写进语法**（tag 由 module 树推导）；判据「**凡能从 module / 数据模型 / 权限推导出来的，语言里不许再声明一遍**」；**无归属对象默认不开放**——枚举不需归属（随视图下发）、从属与技术对象默认 `internal`、共享基础数据归属基础模块（语料 `Base`：`biz/base.ma` 节点 `model` 绑定 `data/models/base/` **77 个**对象、模块级 `sops: READ`） | [`api.md`](api.md) §1.1、[`architecture-review.md`](architecture-review.md) §2.1（**ARCH-111**）、`..\PLAN.md` §6.3-10 |
| 15 | **OpenAPI 原生支持（基准 OAS 3.1.0）+ AI 造数** | **原生支持，基准 = OAS 3.1.0**（人读 `https://spec.openapis.org.cn/oas/v3.1.0.html`；机器校验用官方 Schema `https://spec.openapis.org/oas/3.1/schema/2022-10-07`（实测 200）与方言 `…/oas/3.1/dialect/base`（实测 200）；`.cn` 镜像只有 HTML）：**四条判据**＝内核一等后端 / 过官方 Schema 校验 / 契约测试双向对账 / **语言层不出现 OAS 术语**；30 个 OAS 对象逐字段**指明推导来源**（Operation 12 字段、根对象、Schema/security/`x-mmda-*`）；**首版覆盖 23 + 按需 5 + 不做 2**。**AI 造数**（作者："借助 AI 自动生成 mock 数据，是撰写测试用例的提速手段"）：**两层造数**（机械层零 AI、可复现、进基线 + AI 语义层填业务真实感与刁钻样本），**四条护栏**（不得发明结构 / 过 schema + 约束双校验 / 种子 + provenance / **造数不需双签、断言必须双签**） | [`api.md`](api.md) §3（§3.1–§3.9）、[`testing.md`](testing.md) §4.3、[`ai/tools.md`](ai/tools.md) §2.4/§2.5、[`targets.md`](targets.md) L2 + 能力矩阵、`..\PLAN.md` §6.3-10 |
| 16 | **API 契约四条落定**（作者回「1. REST语义 / 2. 按你建议 / 3. 进 / 4. 要」） | ① **存量路径取 A 方案**：生成物用 **REST 语义**（POST 创建 / PUT 更新 / DELETE 删除 + `/{模块路径}/{资源}` 前缀），**同时给 Profile 开关 `legacyPathStyle: true`** 保留 `POST /save`、`GET ""`、`POST /{id}/delete` 老路径，迁移期双版本并存（老路径标 `deprecated` + `sunset`，调用量归零后移除）；② **序列化取「精度优先」**：`decimal(p,s)` / `numeric` / `money` → **`string` + `pattern`**（TS 端同样生成 `string`，不是 `number`），`int64` / `uint64` 超出 JS 安全整数范围时用 `string`，`Timestamp` → `string` + `format: date-time`（不用 epoch 整数）——**三端序列化必须一致**，进 capability 一致性用例；③ **官方 Schema 校验与契约测试进硬门禁**（B 级：生成后可判定，`mmda api check` 失败即阻断 CI）；④ **AI 造数样本要固化**：种子 + provenance 进版本控制（能重建同一次造数）、过 schema + 约束双校验；**默认只进 mock 与开发期**，断言始终不由 AI 造数产生 | [`api.md`](api.md) §3.1-2 / §3.4 / §3.5 / §3.9 / §8.1、[`testing.md`](testing.md) §4.3、[`targets.md`](targets.md) L2、`..\PLAN.md` §6.3-11 |
| 17 | **愿景与四层目标 + 三条口径落定**（作者给出 To Boss / To CTO / To User / 架构四层目标，逐条回「国产化我会实现一个，例如 naive+别的表格插件，**syncfusion 只是一个选项**」「同意 1A 2C 3只是部署方式支持 4同意你的建议」） | ① **新增 [`vision.md`](vision.md) 为愿景真源**（四层目标逐条给机制与现状；每条都指到规范正文，不在别处另起口径）；② **开放源码 = open core（取 A）**：开源**规范（MIT）+ Rust 内核 / 三端薄适配 / IDE 壳（Apache-2.0）**，闭源**算法库 + 行业业务包 / 模板 / SaaS**；**既有 Java/C# 实现不随本体开源**（`MMDA.CLOUD PROPRIETARY/CONFIDENTIAL` 版权头，只作对照与回填来源）；③ **国产化 = 全三级承诺（取 C）**：L1 数据层（达梦 / 人大金仓已有 `DmDialect`/`KingbaseDialect`；+ OceanBase / openGauss）、L2 运行层（国产 OS + 国产 CPU + JDK 国产发行版 + Rust 交叉编译矩阵，**此前零口径**）、L3 界面层（**至少一套无商业控件皮肤**；Syncfusion 由唯一皮肤降为**可选皮肤**，自研国产皮肤 naive-ui + 表格插件与 `vui`/`rui` 并列，**换皮肤不构成第二套 UI 契约**）；④ **微服务 / 容器化 / 热插拔 / 高可用只是部署方式**（取「只是部署方式支持」）：语言层**零新增**——微服务 = Profile 的**部署拓扑视图**（服务边界 = module 边界）、容器化 = P5 生成物（`Dockerfile` + `compose`/`k8s` 骨架，内核不做编排）、热插拔 = module 粒度、高可用 = 拓扑 + 运行时；**P5 只出单体拓扑**，微服务拓扑留 **P8**（跨进程后事务语义需先裁）；⑤ **业务可测指标**（降成本 / 轻松 / 易用可举证）：生成覆盖率 / 变更成本 / 上手时间 / 模板复用率，**只进报告、不进硬门禁**（C/D 类） | [`vision.md`](vision.md) §5–§6、[`protection.md`](protection.md) §7.1、[`quality.md`](quality.md) §3.1、[`targets.md`](targets.md) §8-5、[`readme.md`](readme.md) 抬头、[`index.md`](index.md)、`..\PLAN.md` §1.1 / §1.3 / §6.3-12…15 |
| 18 | **L2 国产化目标矩阵**（作者回「1A, 2A, 3A,4A,5A」） | ① **CPU 承诺面 = x86_64 + aarch64**（海光 / 兆芯 + 鲲鹏 / 飞腾）；**龙芯 LoongArch 只承诺「内核交叉编译通过 + 给构建说明」，不进验收矩阵**——依据：`loongarch64-unknown-linux-gnu` 已是 Rust **Tier 2 with host tools**（要求 kernel ≥5.19 / glibc ≥2.36 / LSX），但 **JDK 只有龙芯自维护的 `loongson/jdk21u`**、**.NET 官方 supported-os 表不含 LoongArch**（RID 目录有 `linux-loongarch64`，属社区级）→ C# 端在龙芯上无官方支持；**申威（SW64）Rust 无官方目标，不承诺**；② **OS**：验收 = **麒麟 V10 SP3 服务器版（x86_64 + aarch64）+ 统信 UOS 服务器版 V20**，CI 基线 = **openEuler LTS**（开源免费、是前两者上游根社区）；③ **JDK = 毕昇 JDK 21（AArch64 / x86_64）+ 上游 OpenJDK 21 兜底**（毕昇 21 官方只出这两个平台），Kona / Dragonwell / 龙芯 JDK 只列「已知可替换」；④ **设计器不进国产 OS**——L2 只承诺**运行时 + 内核 CLI**（Tauri 壳仍在 Windows / macOS / Linux-x86；麒麟·统信桌面版 WebKitGTK / 字体 / 输入法 / deb·rpm 打包不进首版）；⑤ **交付 = 离线包**（生成物 + 两套交叉编译产物 + 国产 OS 基镜像 `Dockerfile` + `docker save` tar + 校验和清单）+ **`mmda doctor`** 自检（内核 / glibc / 架构 / JDK）。**验收口径**：目标 OS + CPU 上跑通 `mmda test --target java,csharp,ts` 与 DDL 规范化文本一致；**架构支持等级写进 capability 声明**（如龙芯将来纳入，C# 端须显式标 `community`），不许凭口头说「支持国产化」。交叉编译与容器基座随 **P5**、验收腿随 **P9** | [`vision.md`](vision.md) **§5.2.1**（五条决策 + 依据 + 验收）、[`ide/specification.md`](ide/specification.md) §4.9 第 3 条、[`targets.md`](targets.md) §8-5、`..\PLAN.md` §4 L2 验收腿 / §6.3-16 |
| 19 | **共赢 = 插件市场 + 设计阶段原生支持插件式开发**（作者原话：「共赢做插件市场，留这个口子，设计阶段原生支持插件式开发」） | ① **定位**：市场 = 第三方扩展分发渠道 + **闭源增值内容（算法库 / 行业包 / 模板）的合法分发渠道**（与 open core 边界咬合：契约与内核开源、增值闭源但可插）+ 伙伴体系载体；② **三种 registry 同一协议**——公共市场 / 企业私有（内网 registry 或离线目录）/ 内置，**离线可用是硬要求**；③ **清单增补**：`kernel`（兼容范围，不匹配则内核拒载）、`targets`、`capabilities`（最小权限）、`publisher` / `signature` / `license`——将来加计费与授权**不改插件 API**；④ **上架三关**（清单与签名校验 / 权限声明审查 / 危险能力人工审核）与**兼容矩阵可离线算**（不兼容必须拒载并给可行动提示，不许静默失败）；⑤ **三级签名**（官方 / 发布者 / 企业私有可对接 PKI），安装·升级·加载各校验一次，不匹配 → 拒载 + 审计；⑥ **设计阶段原生 = 五条可检判据**（插件清单进 `*.mmda` 项目真源随 git 评审 / 扩展点在设计阶段即存在 / 内置与第三方**同路径无特权通道** / 插件产物进追溯链 / 离线可用）；⑦ **权限与硬边界可执行**：最小权限声明、超出即拦；**禁写语言文件——内核按后缀拒绝 + 审计**；插件校验**只出 warning / suggestion，不许 fail 构建**（进硬门禁必须内核收录）；**语言层无插件与市场关键字**；⑧ **插件分两类**（✔ 2026-09-24 纠正：**主形态 = 业务功能模块插件**，见 §五-20 与 [`runtime.md`](runtime.md) §7；**IDE / 设计器插件为次**，支持更好、不做首版承诺），内核侧插件留口子——加载形态待裁（见 §三-25）；⑨ **商业条款（分成 / 伙伴分级 / 认证伙伴）属商业决策，不进技术契约** | [`ide/plugins.md`](ide/plugins.md) **§8–§12**、[`vision.md`](vision.md) §2 共赢 + §8-5、[`protection.md`](protection.md) §7.1、`..\PLAN.md` §4 P7 / §6.3-17 |
| 20 | **插件语义纠正 + 核心平台 MIT + 移动端 Flutter + OWASP ASVS 进门禁**（作者原话：「我说的插件是支持用户自己开发业务功能模块，至于 IDE 插件对他们没那么重要，支持更好」「核心平台 MIT」「移动端考虑 flutter 支持」「OWASP ASVS 进入」） | ① **插件主形态 = 业务功能模块插件**（真源 [`runtime.md`](runtime.md) **§7**）：**插件就是 `module`**（`module` 已是一等边界、热插拔 = module 粒度、KEEP 区承载定制）→ **语言层零新增**；包形态 `jar`/`dll`/npm + 清单 `mmda-plugin.json`（`id`/`version`/`kernel`/`modules[]`/`requires[]`/`capabilities[]`/`signature`/`publisher`/`license`）；**数据模型硬约束**——只在自己的 module 内新建、**不许改他人表结构**（走主仓 MR，替代手段扩展表 `xxx_ext`）、不许直接读写对方库表、装载期冲突检测**冲突即拒载**；**升级不覆盖 KEEP 区**、内核版本不满足则拒载；**IDE 插件为次**（支持更好、不做首版承诺），两者**共用一套清单 / 签名 / 兼容矩阵 / 市场**；② **核心平台统一 MIT**（规范 + 内核 + 三端薄适配 + IDE 壳一份许可；原记「规范 MIT / 内核 Apache-2.0」）；代价：**MIT 不带专利授权与商标条款**；③ **移动端宿主 = Flutter**（Dart 端，不做响应式 Web 与小程序；**首版不做**，排 P8 之后）→ 随之新开：`MetaUi` 第二渲染方与 `capability ui` 范围（本文件 §三-26）；④ **OWASP ASVS 进入**：**ASVS L1 自动化子集进 `mmda quality gate` 硬门禁**（认证与会话 / 访问控制 / 输入校验 / 敏感数据 / 错误处理 / 密码学），**L2·L3 与人工渗透不进**，**Top 10 作报告项**，引用必须写版本号 | [`runtime.md`](runtime.md) **§7 + §9-8…10**、[`vision.md`](vision.md) §5.1 / §2 / §5.4 / §8-1…3、[`quality.md`](quality.md) **§3.2** + §1.6、[`targets.md`](targets.md) §8-6、`..\PLAN.md` §4 P4/P6、§6.3-18…21 |
| 21 | **事件总线与集成编排（底座 ESB 能力）落成**（作者原话：「我一直想让 mmda 底座提供 ESB 的能力……只要配置就能基本覆盖 80% 的 API 接口集成」「**Event → Message → 数据 Data**」「我更偏向 **Flink 的概念**」） | ① **新增 [`event_bus.md`](event_bus.md)**（概念模型 / 三类集成 / 端点三类来源 / DataFlow 三张图 / DataMapper / State·Watermark·Window·Checkpoint / Outbox / 多租户 / 监控 UI / 与标准 ESB 的差异 / 7 条待裁）；② **引擎裁决：借 Flink 的语义，不绑 Flink 的运行时**——硬事实：**Flink 只有 JVM/Python 面、无 .NET 实现**（与已裁 B3 冲突）+ **Flink 是独立集群运行时而底座是随应用部署的库** → **引擎可替换**（`RuntimeProfile.engine`：`embedded` 默认 / `flink` 留口子 / 第三方 .NET 消息框架 ❌ 不作引擎）；③ **语言层零新增**（编排落图不落语法）；④ **P8 扩为「事件层与总线」**、**新增 P10**（Flink 可选后端）；⑤ Exactly-once 口径写实：**状态 exactly-once + 汇端幂等**，外部系统不承诺 | [`event_bus.md`](event_bus.md)（全文 + §15 待裁）、[`events.md`](events.md)（分工）、`..\PLAN.md` §4 P8/P10、§6.3-22 |
| 22 | **事件与集成的术语统一（唯一命名）**（作者原话：「这里面不同的框架概念不同，但是我想能够在 mmda 里面统一认识」「我认同数据的校验 Validation/Validator，转化器 **Converter**（**我不用 Transformer，免得与那个 AI 的 Transformer 架构混淆**），过滤器 Filter，聚合计算 Aggregator。**Endpoint 和 Channel 我也认同**」「而 Source 我认同**事件源 EventSource** 的概念。我需要你统一术语，不要让程序员误解」） | ① **命名真源 = [`glossary.md`](glossary.md) §3.1**（四列：位置 / 统一名 / 定义 / **禁用别名**；两条判据：一个概念只留一个名字、同一名字不许指两件事）；② **统一清单**：`Source` → **EventSource（事件源）**（`kind`：设备·定时·回调·CDC·文件·消息·进程内）·`Sink` → **Sink（数据汇）**（**Write / Push / Call 是投递方式，不是三个概念**）·`Channel Adapter` → **Endpoint（端点）**（配置单元）+ **Connector（实现）**·`Message Channel` / Topic → **Channel**·**~~Transformer~~ → Converter**（既有概念 `.mc` / `flow/converters/`，**不是新造**）·`Transformation` → **Processor** 四类（Validator / Converter / Filter / Aggregator）+ 编排必需的 Router / Splitter·**DataMapper = 配置面不是节点**·**Trigger 降为 EventSource 的配置项 `on`**·**统一 Publisher / Subscriber**（Producer / Consumer 仅描述外部系统）；③ **两处撞车**见 §三-28；④ 落点：[`glossary.md`](glossary.md) **§3.1（新增）**、[`event_bus.md`](event_bus.md) **§1.1** + **§7.3（节点表与到货例全改统一名）** + §15-8/9、[`events.md`](events.md) 四处、[`ide/diagrams.md`](ide/diagrams.md) §4.1 | [`glossary.md`](glossary.md) **§3.1**、[`event_bus.md`](event_bus.md) §1.1 / §7.3 / §15-8/9、本文件 §三-28、`..\PLAN.md` §6.3-23 |
| 23 | **易混淆概念辨析（四对 + `stream`）**（作者原话：「这里面还有容易混淆的概念：publish/subscribe 对应 publisher/subscriber；produce/consume 对应 producer/consumer；channel、pipe；inbound、outbound / inbox、outbox。你可否给我总结一下，写到文档里」） | ① **落 [`glossary.md`](glossary.md) 新增 §3.2**（四小节 + 速查表）：**`publish/subscribe` ↔ `produce/consume`** = 业务可见性（一对多、订阅关系决定可见范围、Subscriber 必有业务语义）vs 传输拿走（一对一、Consumer 可以无业务语义）——**一句话「谁看得见 vs 谁读走了」**；**`channel` ↔ `pipe`** = **只用 Channel**（有投递语义 / 可持久 / 可跨进程），**Pipe 不进术语**（与 Channel 叠概念，且程序员联想全撞车：shell 管道、**Angular `\|` 管道 = 转换器**、Node `stream.pipe`、Go `chan`）；**`inbound/outbound` ↔ `inbox/outbox`** = **方向 vs 落库的机构**——**Outbox 保「不丢」、Inbox 保「不重」**，相加才是「至少一次 + 幂等 = 业务上的 exactly-once」（`Inbox` = 按 `eventId` 去重表，**首次显式命名**）；另立 **入参/出参**（params，参数方向）与端点方向区分；② `stream` 也收编（`DataFlow` 的口语说法，指中间件时写其名）；③ 命名保留：`Inbox`/`Outbox` 是行业固定词，**不许自造 `SendBox`/`ReceiveBox`**；④ 指针落 [`event_bus.md`](event_bus.md) §1.1（四对辨析指针）、**§6 端点契约（入/出是方向、不是 Inbox/Outbox）**、**§9.3 补 Inbox 段（收侧去重表）** | [`glossary.md`](glossary.md) **§3.2**、[`event_bus.md`](event_bus.md) §1.1 / §6 / §9.3、`..\PLAN.md` §6.3-24 |
| 24 | **运维篇落成（运维与可观测性）**（作者原话：「关于软件的可监测性，涉及到运维架构。也是我关注的一个重点！1. DevOp，如 jenkins 集成 2. 系统资源、微服务实例、网络、数据流量、报警推送等」「运维的角度会关注：软件生命周期（SLM），关注可用性、性能和安全性；发布自动化（DevOp）、配置管理；运行监控：基础设施、应用、数据；应急处理：问题定位、响应处理、分析 KPI」「跟开源的 Zabbix，Prometheus，Open Falcon 等如何能集成，需要考虑。我想你单独落盘一份运维篇」，并给出**五层监控图**） | ① **新增 [`operations.md`](operations.md)（§0–§12）**：**五层监控模型与责任归属**（**底座只负责中间三层**：业务 / 应用 / 数据；系统层与网络层交社区采集器，客户端层首版只做 Web）；② **零手写打点**（业务层 = `Action` 元数据、应用层 = [`runtime.md`](runtime.md) §1 四层、数据层 = Repository 与缓存横切面）+ **指标命名规范** + **高基数铁律**（标签禁放 id/单据号/`traceId`/`eventId`）；③ **三支柱 Metrics · Logs · Traces + 三个关联键**（`traceId` / `eventId` / `correlationId`；**事件驱动链路在 Outbox 投递处天然断开 → 靠 `correlationId` 续链**）；④ **四个标准出口**：`/metrics`（**OpenMetrics 文本，拉模型**）· **OTLP 1.11.0**（三信号 stable）· 告警规则文件 · trapper 推送；⑤ **三家开源对照**：**Prometheus** 拉取（不内置本体）· **Zabbix** 官方支持 **HTTP agent 主项 + Prometheus pattern 依赖项** 抓取或 trapper 推送（**不做 agent 插件**）· **Open-Falcon 已转向夜莺（n9e）**，走 **Prometheus-Like / Remote Write** 兼容（不投适配器）；**告警通道复用既有通知器** + **告警内容四项规范**（现象 / 影响面 / 第一个排查动作 / 可行动作，缺项不许上生产）；⑥ **DevOps**：八阶段流水线、**CI 输出契约**（JUnit XML · SARIF · `quality-report.json` · `mmda diff` · 契约 JSON · `SHA256SUMS` · 构建元数据）、**Jenkins 三种形态且零私有插件**；⑦ **配置管理 = 元数据即配置**（环境差异只进 `RuntimeProfile`；**配置漂移检测**：生效版本 ≠ git 提交哈希即告警）；⑧ **应急 SOP 七步 + 分级 + KPI**（DORA 四指标 / MTTR / MTBF / 告警质量，**只进报告不进硬门禁**）；⑨ **运维面安全**（`/metrics` · `/health` · 诊断端点**必须鉴权 + 只绑内网/回环**，进 **ASVS L1 硬门禁**；**默认不外发公网**；重放与配置变更留审计）；⑩ **不新增阶段**（建议 P9 扩为「一致性 + 验收 + 运维出口」）；**待裁 8 条（§11，建议 1A…8A）** | [`operations.md`](operations.md)（全文 + §11）、[`quality.md`](quality.md) §2.3、[`event_bus.md`](event_bus.md) §12、[`readme.md`](readme.md)、[`index.md`](index.md) 第 21 篇、本文件 §三-29、`..\PLAN.md` §6.3-25 |
| 25 | **命名约定（标识符与生成代码）**（作者原话：「还有一点约定，**接口全部 I 开头**，像 Java 那种**以 `Impl` 结尾的有点啰嗦**。**类和对象都是 Pascal 命名，字段、属性都是小写开头的 camel 命名**，**其他的可以尊重 java、c# 和不同编程语言的习惯**」） | ① **新增 [`naming.md`](naming.md)（§0–§7）**，**命名真源**（与 [`glossary.md`](glossary.md) 的分工：`glossary` 管「词」，本文管「标识符怎么写」）；② **两条判据**：**进契约的名字三端逐字一致**（类型 / 字段与属性 / 枚举成员 / 事件名 / 消息头 / JSON 字段 / API 路径与 `operationId` / 指标名 / i18n key）／**不进契约的随本地习惯**（方法名 · 包与命名空间 · 文件名 · 局部变量——**常量与枚举成员例外，见 §五-25 ⑨**）；③ **硬规则**：接口 **`I` 前缀 + PascalCase（含 TS）**、**实现类用业务名、禁 `Impl`**、多实现用**限定词**（8 个 SQL 方言同构）、类与对象 **PascalCase**、**字段与属性 camelCase（含 C# 属性）**、**常量与枚举成员 `UPPER_SNAKE`**（✔ 2026-09-24 补裁）、Handler 接口 `I<事件>Handler`；**禁 `Impl` 的四条理由**（不携带信息 / 二次撞车 / 必生同义异名 / 替代手段已够）+ **不溯及既往**；④ **C# 属性的代价写清**（代价「不像手写 C#」，收益「三端同名 + 与 JSON 载荷天然一致」）；⑤ **生成器 / Profile 责任**：P6 生成器必须遵守，**§1/§2 契约名不许有端开关**，Profile 只改 §3 清单；⑥ **检查落点**：`mmda check` 命名检查（默认 warning）· 设计器预置 · 评审清单「生成物出现 `Impl` 或缺 `I` 即退回」；⑦ [`targets.md`](targets.md) §5 **L3 判定增「标识符逐字对账」**；⑧ 既有散落口径（records / project / statements / templates）**改为指针**；⑨ **常量与枚举成员 = `UPPER_SNAKE`**（**全大写、单词间 `_` 隔开**，**三端一致**——作者 2026-09-24 补充，原待裁 1 **取 B 档**；载荷里按名序列化即为 `DRAFT` 这样的字符串）；⑩ **数据库标识符 = 模型名逐字一致**（**表 / 视图 = 类名 PascalCase、列 = 属性名 camelCase**，**不转写、不加前缀**——作者 2026-09-24 补充，原话「**sql 字段命名同属性，表名同类名，视图也是和表一样，便于 orm 的一致性**」，原待裁 5 **取 B+**）；**四条前置条件一并落 §3.3**（① 方言引号 ② MySQL `lower_case_table_names=0` ③ 达梦 `CASE_SENSITIVE` 进 Profile ④ **ORM 关掉 camelCase→snake_case 自动转写**）；⑪ **命名约定 6 条收口（2026-09-24 作者取 `1A 2A 3A 4B 5A` + 索引前缀建议）**：**检查强度 = warning（不阻断）** · **C# 属性不放宽**（保持 camel，跨端同名）· **方法名随各端**（Java camel / C# Pascal / TS camel）· **既有 `Impl` 提供迁移脚本**（P9 交付、dry-run 出清单、**仍不强制改名**）· **不进 `mmda quality gate`** · **约束 / 索引名不做硬约束**，**仅建议前缀 `IDX_`（索引）/ `FUNC_`（函数）/ `PROC_`（存储过程）**、出 suggestion 不出 warning——**本文待裁清零**；⑫ **前端与 Flutter 的本地命名 = 尊重生态**（作者 2026-09-24 补充，原话「**前端和 flutter 尊重他们的习惯，允许 snake 命名法，包括 `-` 和下划线，例如组件名称，css bem**」）：**组件名（Pascal 或 `kebab-case` 均可）· 文件名 / 目录名 · CSS class（含 BEM 的 `__` / `--`）· 静态资源名 · Dart 文件名** **允许 `-` 与 `_`**，见 [`naming.md`](naming.md) **§3.4**；**边界**：这些名字**都不进契约**（`presentation.md` §5 已裁「`MetaUi` 里不许出现组件名 / CSS」），**`MetaUi` 与载荷名仍按 §1 / §2**，**Dart 类名仍 Pascal、成员仍 camel**（放宽只限外部命名）；⑬ **约束 / 默认值 / 索引名的前缀建议**（作者原话：「**数据库主键 PK_, 外键 FK_ 开头，默认值 DF_ 开头是推荐的习惯**」）：**主键 `PK_` · 外键 `FK_` · 默认值约束 `DF_` · 索引 `IDX_` · 函数 `FUNC_` · 存储过程 `PROC_`**，**全为建议**；**前缀后照抄模型名**；**生成器须显式 `CONSTRAINT <名>`**（否则方言自生成 `DF__表__列__<hash>`）；**约束名与索引名不进 L3 对账** | [`naming.md`](naming.md)（全文 + §6）、[`records.md`](records.md) §1.1、[`project.md`](project.md)、[`statements.md`](statements.md)、[`targets.md`](targets.md) §5、[`glossary.md`](glossary.md) §3.2 末、本文件 §三-30、`..\PLAN.md` §6.3-26 |
| 26 | **快速上手篇扩写（并入 2023 版用户手册精华）**（作者原话：「我在 `D:\项目\2026 mmda\md` 目录下写过快速上手，请你查阅，把有价值的内容加进文档落盘」） | ① **`guide/quickstart.md` 扩写为 §0–§12**：**§0 五阶段全貌**（需求→设计→原型自动生成→逻辑实现→测试→交付，带回环；表：谁做/做什么/产物/真源）+ 原图 `assets/mmda-dev-workflow.png`；**§1 八步实操**（需求+Role → 设计 → 校验 → 生成 → 逻辑 → 跑原型 → 测试 → 交付）；**§4 DDL 注释 → `.m`**（表 `@Name`=Record 真名、`：PK`=**分区键**、`：UK`、枚举 `值;名称;标签`、`REF`、`HAS_ONE … AS … WHERE`、`AS(…) STORED` 计算列、索引 `IDX_`）；**§6 旧命令对照**（`mmda meta`→`mmda import --from-ddl`、`mmda java/vui/flui`→`mmda generate --profile`）；**§7 配置面速查**（`groupLabel` a1../s1..、`listed`/`filterable`/`readOnly`/`hidden`、`fixedFilter`=列表顶端页签、`formatter` `D`/`N3`、`align` 0/1/2、`REF` vs `@One` 判据、多租户 `partitioned` vs `BETWEEN`、为 `uniqueKey` 建索引）；**§8 载荷形态**（大整数→字符串、枚举按名、`@One`→嵌套对象、`REF`/枚举显示标签进 `customProperties.$字段`）；**§9 定制点全景**（后端 `beforeSave/beforeInsert/beforeValidate/validate/beforeDelete` + `afterSaved/afterInserted/afterDeleted`；前端 `beforeEdit/beforeDetails/beforeIndex` + `lockIf/hideIf/hideIfEmpty/onChange/onValidate/requiredIf` + `setCustomEditor/setCustomRenderer`）；**§12 常见坑 6 条**（表名大小写 `@` 前缀、`PK`≠主键、`REF` 误用、多租户两形态、**明文口令不进仓**、`mmda_metadata` 降为产物）；② **已在仓内的旧手册内容改为指针**（DDL COMMENT DSL 全表 → [`legacy/import-from-ddl.md`](legacy/import-from-ddl.md)；SqlBuilder/Repository/UiLogic → [`legacy/runtime-java.md`](legacy/runtime-java.md)；命令映射 → [`legacy/java-factory.md`](legacy/java-factory.md)；元数据类型 → [`datatypes.md`](datatypes.md)；UiField 属性 → [`presentation.md`](presentation.md)）；③ 同步小修补：[`meta-model.md`](meta-model.md)（`fixedFilter` 语义、Relation 的 `displayLabel`/`relationIdx`）、[`legacy/runtime-java.md`](legacy/runtime-java.md)（`setCustomEditor`/`setCustomRenderer`）、[`vision.md`](vision.md)（**新增 §0 思想源头：No Silver Bullet**）；④ **新增 `doc/assets/`**（`mmda-dev-workflow.png` / `org-employee.png` / `mmda-concept.png`，作者 2023 原图）；⑤ **新开 3 条待裁**（§三-31） | [`guide/quickstart.md`](guide/quickstart.md)、[`assets/`](assets)、[`meta-model.md`](meta-model.md)、[`vision.md`](vision.md)、[`legacy/runtime-java.md`](legacy/runtime-java.md)、`..\PLAN.md` §6.3-27 |
| 27 | **旧版手册并入后的 3 条议题（✔ 2026-09-24 作者逐条取定）**（作者原话：「1. 不算 / 2. +beforeSearch / 3. 保留，如果改约定动作太大」） | ① **前端工程目录/文件约定 = 不算规范内容**（降为 `mmda-vue` 项目内部约定，规范只保证 `MetaUi`）；② **五视图钩子补 `beforeSearch`**——`index`→`beforeIndex`、`editor`→`beforeEdit`、`details`→`beforeDetails`、`search`→`beforeSearch`；**`report` 暂不设钩子**（未提，待有实现再裁）；③ **`customProperties.$<字段名>` 保留并写进契约**（`@Ref`/枚举的显示标签；**不另开 `xxxLabel` 投影字段**） | [`presentation.md`](presentation.md) §1、[`api.md`](api.md) §3.5、[`guide/quickstart.md`](guide/quickstart.md) §9、本文件 §三-31、`..\PLAN.md` §6.3-27 |
| 28 | **新增体验篇（`doc/ux.md`）：UX 落成**（作者：「我在 `D:\项目\UX` 目录下有关于前端和用户体验的学习，我希望你读一下，把有价值的内容落盘 ux.md」——材料 = `学习笔记.docx`（2,092 字 / 31 段 / 6 张内嵌图）+ `UX 广义.webp` / `UX 狭义.webp` + `设计心理学.pdf`（247 页扫描件，中信 2003 版）） | ① **UX 与 Usability 分清**（可用性 1979 / ISO 9241-11；UX 诺曼 1993；**不拿 ISO 25010:2023 的 `interaction capability` 当 UX 同义词**）；② **体验四层环**（**Utility → Usability → Desirability → Brand Experience**：生成器先把内两层做对，Desirability 归皮肤，**Brand Experience 靠同一真源而非同一 UI 库**）；③ **认知心理学五条判据**（示能/意符 · 映射 · 反馈 · 概念模型 · 人的差错）+ **差错归因原则**（三哩岛事故：把「用户点错」当设计缺陷追）——**可编辑性只由声明决定，皮肤不许自行改变**；④ **尼尔森十大原则 → A/B 类可判定信号清单**（每条给「生成物里的体现 + 判据落点 + 等级」）；⑤ **UCD 六阶段 → 五阶段对照**，**明确指出缺少「上线度量回流」这条腿**；⑥ **MVP 金字塔** = Functional / Reliable / Usable 三层是承诺面，Emotional 归皮肤；⑦ **Lean / Agile / 传统 UX 三方对照** → **同一真源上的三种节奏**；⑧ **服务设计：承认但零新增**（不新增 `journey`/`touchpoint` 元模型元素）；⑨ **包容性三条可机械判定**（语义标签 / 焦点顺序 / 错误文本）；⑩ **版权边界写死**（**只有作者本人的图进 `doc/assets/`**；第三方图与书籍正文只记出处、只转述、不进仓）；⑪ **待裁 4 条**（见 §三-32） | [`ux.md`](ux.md)（全文 + §11）、[`readme.md`](readme.md)、[`index.md`](index.md) 第 22 篇、本文件 §三-32、`..\PLAN.md` §6.3-28 |
| 29 | **体验面（UX）4 条裁决（✔ 2026-09-24 作者取 `1B 2B 3B 4A+B`）** | ① **1B**：UCD 的上线度量**回流进 `mmda check` 出 warning**（**作者未采纳助手建议的 1A**）——**回流分两半写实**：静态可判定的一半（监控/诊断出口的声明是否存在、关键 `Action` 有元数据、端点声明鉴权与只绑内网）进 `mmda check` warning；**运行期的一半**（任务完成率、校验失败 top N、最常放弃的操作）属 C 类实测，**进 `mmda ops` 报告并喂需求与设计评审，不许写成构建断言**；**前置 = `mmda ops` 进首版**（[`operations.md`](operations.md) §11-5 已标该依赖）；② **2B**：**尼尔森十原则的 A 类 6 条进 `mmda check` 断言集出 warning**（B 类 4 条进报告清单）；③ **3B**：无障碍基线 = **WCAG 2.2 AA 的可自动化子集（语义标签 / 焦点顺序 / 错误文本）出 warning**，对比度等视觉项留皮肤；④ **4A+B**：服务设计旅程**不进语言与元模型**（仍用 `Role` + 流程 + 事件表达）+ **设计器插件侧留只读「旅程视图」扩展点**（`journeyViews`） | [`ux.md`](ux.md) §4 / §5 / §7 / §8 / §11、[`quality.md`](quality.md) §3、[`operations.md`](operations.md) §11-5、[`ide/plugins.md`](ide/plugins.md) §10、本文件 §三-32、`..\PLAN.md` §6.3-28 |
| 30 | **API 契约 11 条裁决（✔ 2026-09-24，作者逐条取定：1–9 依建议、12 作者给形、13 按现状、14 取 B）** | ① **§8.2-2 / 2b** 路径 = **`/api/<模块小写>/<模型名复数>`**——**作者补充「我们是 `/service/repository`」= 第一段模块（service）、第二段资源（repository / Record）**，**字形取「复数」**（原话「**我现在 api 是：`GET /api/mes/WorkOrders` 复数形式**」）；**⚠️ 残余 2c：`/api` 前缀写死还是 Profile 可配 + 复数变形规则**（未定不许进生成器）；② **§8.2-1** 默认 `internal`，显式 `expose` 才对外；③ **§8.2-3** 稳定度（`stable`/`beta`/`deprecated` + `since`/`sunset`）**进语言**；④ **§8.2-4** OAS **3.1**，扩展字段**只出 `x-mmda-*`**；⑤ **§8.2-5** `mmda api export/diff/check` = **独立子命令**（不并入 `generate`）；⑥ **§8.2-6** Mock = **IDE 内置**；⑦ **§8.2-7** 网关粗粒度 / Controller 细粒度；⑧ **§8.2-8** 逆向导入 = **先出报告 + 骨架、人审后入真源**；⑨ **§8.2-9** 基础模块沿用 **`Base`**；`READ` → **只出读端点**、`CRUD` → **全出**（**作者原先「还没想清楚」的部分随之清掉**）；⑩ **§8.2-12** `operationId` = **`moduleName_featureName_op`**（ASCII、不含中文、同项目唯一、`mmda check` 校验）；⑪ **§8.2-13** scope 粒度 = **模块权限 + Action 权限**（**按现状**：module 出读 / 写 scope、Action 出专属 scope，**Feature 级不出 scope**）；⑫ **§8.2-14 = 取 B：首版不带 `webhooks`**（作者口径：「**webhooks `GET /events/mes/WorkOrders` 这样的习惯，我选择 B**」——对外事件走**拉取式端点**，**OpenAPI 不声明回调段**；订阅 / 重试 / 签名留 [`event_bus.md`](event_bus.md) §15）——**§8.2 至此 11 条全部已裁、无余项** | [`api.md`](api.md) **§8.2**（4 列台账）、§1.1（`Base`/`sops`）、§1.2（`expose`/稳定度）、§3.3（`operationId`）、§3.4（路径形态与复数）、§3.6（scope 粒度）、§3.9（Mock）、§5（网关）、§6（导入）、[`naming.md`](naming.md) §2、本文件 §三-23、`..\PLAN.md` §6.3-29 |
| 31 | **运行架构 10 条裁决（✔ 2026-09-24，作者逐条取定；10 条全部已裁——④ 于同日补裁取 A）** | ① **§9-1** 拦截点**挂在 Action / 视图**（不新造顶层 `hooks` 块）；**Action 的准入走 `canDo`**——**Java/C# 不用反射**（现状把逻辑写在 action 内、准入函数叫 `canExecute`；TS 现状 `EntityAction.canDo` 会按名探测 `beforeSubmit`，属 TS 侧实现细节、不进契约），**三端统一为 `canDo` 并支持用 m 语言写逻辑**（重构项）；其余点固定（如 `beforeDelete`）；② **§9-2** 生命周期点**封闭枚举**：标准点就是那几个 `beforeXxx` / `afterXxx`，**不要新增**；③ **§9-3** `save` **语言语义上合并**，但**服务端仍必须有 `insert` / `update` 两个操作**——`save` 只是**给程序员的便利门面**；⑤ **§9-5** 缓存策略：**设计里可声明默认策略**、**语言级定义后续再加**，**允许程序员用 Profile 重写**；⑥ **§9-6** `EntityFactory` 与 `Repository` **都要、分层不替换**（前者更像 .NET EF，后者可基于前者实现）；**硬目标 = 消灭手写 SQL 与手写字符串字段名**；⑦ **§9-7** 事务 = **同模块同事务、跨模块新事务**，跨模块一致性走事件补偿，**2PC / 分布式事务属具体实现**；⑧ **§9-8** 插件隔离 = **首版进程内 + 命名空间与冲突检测**（进程级隔离留给「不可信第三方插件」）；⑨ **§9-9** **一插件可多 module、一个 module 只属一个插件**；⑩ **§9-10** **不允许给他人表加字段**（走主仓 MR），替代 = **扩展表 `xxx_ext`**，**具体设计待细化**；**④ §9-4「`store` 钩子是否对设计师开放」→ ✔ 同日补裁：作者取 A（不开放）**——作者先回「**没明白啥意思**」（解释已写 [`runtime.md`](runtime.md) **§4.4**），随后取 A：**`store` 钩子只给程序员**（KEEP 区代码 + 显式勾选 + 评审清单可见），**语言/模型侧不给它声明语法**；同轮作者补充「**实际上 service 层也有钩子，是给业务逻辑用的**」→ 两个「钩子的家」写死在 [`runtime.md`](runtime.md) §4.2 注（`beforeXxx`/`afterXxx` 住 Service、`store` 住 Repository） | [`runtime.md`](runtime.md) **§9**（四列台账）、§3（事务）、§4.2（`canDo` / 封闭枚举 / `save`）、§4.4（`store` 说明）、§5（缓存策略）、§6（`EntityFactory` + `Repository` 两层）、§7.3 / §7.4（插件粒度与隔离）、§8（缺口表）、本文件 §三-20 / §三-21 / §三-22、`..\PLAN.md` §6.3-30 |
| 32 | **运维面 8 条裁决（✔ 2026-09-24，作者取 `1A 2A 3A 4A 5A 6A 7A 8A`——全部取建议档）** | ① **§11-1** 首版**唯一必需的出口 = `/metrics`（OpenMetrics 文本，拉模型）**，**OTLP 可选**、三出口不并列；② **§11-2** 交付包**附**监控后端 / 采集器镜像 + dashboard JSON + 规则文件，**可选启用、默认不开**（不算内置）；③ **§11-3** Zabbix **只出模板 XML + 抓 `/metrics`**（HTTP agent 主项 + Prometheus pattern），**保留 trapper 口子**；④ **§11-4** **只做 Prometheus 兼容**（夜莺 n9e / 兼容层消费），**不做 JSON 推送适配器**；⑤ **§11-5** **`mmda ops` 只读诊断进首版（P9）**——**同时清掉 [`ux.md`](ux.md) 1B 的前置**（体验度量回流腿有数据来源）；⑥ **§11-6** **P9 扩为「一致性 + 验收 + 运维出口」**（**不新增 P11**）；⑦ **§11-7** `mmda doctor` **由四项扩为六项**（+ 时间源一致性、+ 监控出口可达性）；⑧ **§11-8** 客户端监控**首版只做 Web**（mmda-vue），Flutter 排 P8 之后 | [`operations.md`](operations.md) **§11 + §2 / §4.1 / §4.2 / §9 / §10**、[`ux.md`](ux.md) §11、本文件 §三-29、`..\PLAN.md` §6.3-31（+ §4 P9） |
| 33 | **事件总线与集成编排 9 条裁决（✔ 2026-09-24，**9 条全部已裁**）** | 作者原话：「**1A, 2B, 4A, 5A, 6A,7A,8C, 9A端点是要在API的基础上增加定义数据的转化、过滤规则的**」；第 3 条先回「**3 待定**」，随后另给方案：「**我想把 `.mf` 给数据流图用，跨模块流程 `.mb`**」——① **§15-1 取 A**：执行层 = **内嵌轻量执行器**（语义对齐 Flink）；`flink` 集群留 P10、**第三方消息框架不做**；② **§15-2 取 B**（**与助手建议的 2A 相反**）：**外部端点定义反向导入成语言声明**，走 `api.md` §6 的护栏（报告 + 骨架 + 人审），**外部 collection 仍不进真源**、**靠再导入 + diff 跟进**；③ **§15-3 取作者另给的方案**（三个原选项都不取）：**扩展名职责重划**——**`.mf` 改为「数据流（DataFlow）」**（节点图 / 数据流图 / 数据映射图三张图，靠 `*.mf.g` 多 sheet：`mf-flow` / `mf-dfd` / `mf-map`），**跨模块流程（BPMN）另立 `.mb`（+ `*.mb.g`，sheet `mb-bpmn`）**；`.g` 族扩为 `{ma, mm, ms, mf, mb}`、新增 partType `bpmn`、`flow/` 目录不变——**落点见 §五-34**；④ **§15-4 取 A**：端到端一致性只写**「状态 exactly-once + 汇端幂等」**；⑤ **§15-5 取 A**：**多租户 = 共享执行 + 租户键**（B/C 按客户；**同时收口 `operations.md` §9 同一三档**）；⑥ **§15-6 取 A**：迟到进侧输出 / 迟到队列（可配丢弃或补算）；⑦ **§15-7 取 A**：数据映射**可含**外部报文形态（JSON Schema / XSD，作适配资产）；⑧ **§15-8 取 C**（**与助手建议的 8A 相反**）：**`Sink` 正式名改为 `EventSink`**（与 `EventSource` 对称），**契约 / 生成物 / 正文一律 `EventSink`**、引用 Flink·Kafka 官方词时保留 `Sink`——**全仓现行文档已改名**（`glossary` §3.1、`events.md`、`event_bus.md` §1.1/§4.1/§7.3、`ide/diagrams.md`、`index.md` 目录）；⑨ **§15-9 取 A + 补实义**：**HTTP 接口写「API / 接口」、集成连接写「端点」**，且**端点 = API + 集成定义**（作者原话「端点是要在 API 的基础上增加定义数据的转化、过滤规则的」→ 端点在其上再加**转化（Converter / DataMapper）、过滤（Filter / Validator）、投递方式、触发与重试、对账**，**不改 API 业务语义**） | [`event_bus.md`](event_bus.md) **§15（四列台账）+ §1.1 / §4.1 / §5.3 / §6 / §7.2 / §7.3 / §9.1 / §9.3 / §10**、[`glossary.md`](glossary.md) §3.1 / §3.2、[`events.md`](events.md) §2、[`ide/diagrams.md`](ide/diagrams.md) §4、[`operations.md`](operations.md) §9、本文件 §三-27 / §三-28、`..\PLAN.md` §6.3-32 |
| 34 | **扩展名职责重划：`.mf` = 数据流、`.mb` = 跨模块流程（✔ 2026-09-24）** | 作者原话：「**我想把 `.mf` 给数据流图用，跨模块流程 `.mb`**」（回的是 [`event_bus.md`](event_bus.md) §15-3 的「待定」）——① **`.mf`（Meta Flow）= 数据流 DataFlow**：节点图 / 数据流图（DFD）/ 数据映射图，`partType = flow`；② **`.mb`（Meta BPMN）= 跨模块流程（BPMN）**，`partType = bpmn`（新增 partType）；③ 投影 **`.g` 族扩为 `{ma, mm, ms, mf, mb}`**：`.mf.g` 多 sheet = `mf-flow` / `mf-dfd` / `mf-map`；`.mb.g` 单视图 = `mb-bpmn`（**BPMN 从 sheet 升格为独立文件类型**）；④ **目录不变**（`flow/roles/` · `flow/converters/` · `flow/*.mf` · `flow/*.mb`）；⑤ **语言层新增 = 0**（`event` / `channel` / `subscribe` 已能声明，编排属元数据图）；⑥ 连带收口 [`event_bus.md`](event_bus.md) §7.2 / §15-3 与 `ide/specification.md` 域 6 的图元落点 | [`project.md`](project.md) §1.1–§1.2 / §1.4、[`glossary.md`](glossary.md) §5–§6、[`design-notes.md`](design-notes.md)、[`ide/graph-files.md`](ide/graph-files.md) §2–§8、[`ide/diagrams.md`](ide/diagrams.md) §4、[`ide/diagram-adapters.md`](ide/diagram-adapters.md)、[`event_bus.md`](event_bus.md) §7.2 / §15-3、`PLAN.md` v1.38、[`index.md`](index.md) 0.36 |
| 35 | **嵌入式脚本执行引擎（✔ 2026-09-24，作者取 A + 补三条口径）** | 作者原话：「**runtime store钩子选A**」；「**实际上 service 层也有钩子，是给业务逻辑用的**。我原先想能否有**脚本执行引擎**，嵌入到 java、c# 后台，能够写一些简单的脚本，**起到类似数据库的触发器的作用**。**技术上有没有可能？数据库的触发器没有可移植性，换数据库就废了**」→ 助手给三档与已核技术事实 → **作者取 A**，并补：「**2要允许跨模块调用，能访问所有 Entity，例如查询，条件更新，删，能力我可以不断增加，都是函数**」——① **A 已裁**：**脚本 = m 语言受限子集**，由 **Rust 内核**解析求值，三端走 **P6 嵌入通道**（Panama / P-Invoke + WASM）；**不引入宿主脚本引擎**（Nashorn 已随 JDK 15 移除 JEP 372、GraalJS 在 stock JDK 上不受官方支持、.NET 侧 Roslyn / Jint / ClearScript 的限额≠隔离边界，且宿主引擎会让同一钩子三端各写一遍）；② **允许跨模块调用**：按模块调**声明接口**；**事务按已裁规则**（§3 / §9-7 **同模块同事务、跨模块新事务**），要最终一致靠 **Outbox**（[`event_bus.md`](event_bus.md) §9.3）；③ **可访问所有 Entity**：查询 / **条件更新** / **条件删除**，统一经**内核代理**（`EntityFactory` / `Repository` 的脚本面），**不生成裸 SQL、不拼字符串字段名**；④ **能力 = 内核函数**（`script.<域>.<动作>`）：逐条登记（[`runtime.md`](runtime.md) §4.6 起始清单：查询 / 写入 / 上下文 / 事件 / 日志）、项目用 **`capability` 声明**、**未声明即生成期报错**、**只增不改名**（改名走语言版本）；⑤ **两层红线**：**表达式层（Expression）仍纯函数**（1A 不受影响）——脚本层（Script）≠ 表达式层，判据入 [`glossary.md`](glossary.md) §2；⑥ **脚本层仍禁**：任意 IO（文件 / 网络 / 进程 / 线程）、反射与动态求值、随机与时间依赖（时间只能取 `script.ctx.now()`）；⑦ **语言层新增 = 0**（脚本块不需要新关键字，挂在既有拦截点与 `canDo` 上；脚本块语法形态归**语法专题**）；⑧ **仍待裁 1 条**：**脚本运行身份**（**A 继承调用者** / B 声明 `runAs` / **C 默认继承 + 允许声明 `system`，助手建议**——否则「能访问所有 Entity」等于开越权后门） | [`runtime.md`](runtime.md) **§4.5 / §4.6** + §4.2 注 / §4.4、[`glossary.md`](glossary.md) §2、[`targets.md`](targets.md) §5（capability）、`PLAN.md` v1.40（§6.3-34）、[`errata.md`](errata.md) §三-32 |
| 36 | **脚本语言维持 A + 类 SQL 归入 m 语言未来语法 + A′ 不做脚本语言（✔ 2026-09-24）** | 作者原话：「**维持A, m语言，未来如果支持类SQL语法也是有可能的**」：① **钩子脚本维持 A**（m 语言受限子集、Rust 内核求值、P6 嵌入通道）——**在 Java Compiler API / Roslyn 双端实测之后重申**（实测内存编译 19~34 ms / 热 31 ms 可用，但**无沙箱**、三端各写一遍、进不了元数据静态校验与 L3 对账、依赖完整 JDK、明文可反编译）；② **类 SQL 的方向 = 将来可能成为 m 语言自身的查询语法**（内核解析编译、宿主只执行），**不做「宿主 SQL 包翻译层」**（jOOQ 开源版不含达梦 / 金仓；Java 现状 Spring Data JPA、C# 现状 Dapper 2.1.35 无查询 DSL），**首版不进**，**语法形态归语法专题**；③ **兜底层 = 注入 `EntityFactory` / `Repository` 直写宿主代码 = KEEP 区**（非第三档脚本，采纳上轮助手建议档）；④ **A′（宿主语言运行期编译）不做钩子脚本语言**，保留为 **KEEP 区扩展的运行期加载方式、P6 之后的候选** | [`runtime.md`](runtime.md) §4.5 / §4.6（1）（2）（3）、`PLAN.md` §6.3-35、本文件 §三-33 / §三-34 |
| 37 | **脚本的运行身份（✔ 2026-09-24，作者取 C）** | 作者原话：「**C**」——**默认继承调用者 + 允许声明 `runAs: system`**：① **默认 = 继承调用者**（按触发它的那个调用者的权限与数据范围运行，越权同样被拒）；② **可选 `runAs: system`**（无调用者场景：定时任务 / 事件消费者 / 系统钩子），声明**进评审清单可见 + `mmda check` 出 warning**；③ **身份枚举封闭**（只有 `caller` / `system`，**不许自定义身份**）；④ **`system` 边界**：不受对象级 / 数据范围限制，**但仍受功能开关与审计**——所有脚本写入必须记 `actor`（`system:<脚本名>`）；⑤ **租户键仍必须存在**（与 §10 共享执行 + 租户键一致） | [`runtime.md`](runtime.md) §4.5 尾、`operations.md`（审计）、`PLAN.md` §6.3-36；**`runtime.md` 脚本议题至此清零** |
| 38 | **`api.md` §8.2-2c 取 1A 2A（API 路径前缀与复数形态，✔ 2026-09-24）** | 作者原话：「**1. 1A 2A 实际上我在 module.moduleUrl 中配置了，复数形式遵循英文单词，前后端都有实现**」：① **`/api` 前缀不写死 —— 由模块的 `moduleUrl` 配置**（真源 = `meta-model.md` 的 Module 元素，已在该行补注）；② **复数变形 = 遵循英文单词规则**（`+s` / `y → ies` / `s,x,z,ch,sh → es` 等常规变形），**前后端同一套实现、以现状为准**、**不引 `pathSegment` 手写覆盖**；**§8.2 至此 16 条全部已裁、无余项** | [`api.md`](api.md) §3.4 / §8.2-2、[`meta-model.md`](meta-model.md)（`moduleUrl` 行）、`PLAN.md` §6.3-37 |
| 39 | **插件口径补两条 + Flutter 已有早期实现（✔ 2026-09-24）** | 作者原话：「**flutter 我也有早期版本的实现，后面再说。业务插件是指用户用 java, c#, ts 做的，目前不考虑收费和市场**」：① **业务插件 = 用户用 Java / C# / TS 三端原生代码实现**（**不是 m 语言或脚本写的**；m 只声明接入点与权限）→ `runtime.md` §7 补口径；② **不做收费、不做插件市场** → [`ide/plugins.md`](ide/plugins.md) §9 **由「✔ 已裁：做市场」改判为「⏸ 暂缓」**（机制保留为**方向与口子**、**不进首版、不进版本承诺**），[`vision.md`](vision.md) 共赢行 + [`protection.md`](protection.md) 分发渠道项同步加注；③ **移动端 / Flutter**：**作者已有早期版本实现**，**版次议题延后**（「后面再说」）——§三-26 的 Flutter 第二渲染方三条**暂挂**、不进首版承诺 | [`runtime.md`](runtime.md) §7、[`ide/plugins.md`](ide/plugins.md) §8 / §9 / §9.1、[`vision.md`](vision.md)、[`protection.md`](protection.md)、`PLAN.md` §6.2-17 / §6.2-20 / §6.3-38 |
| 40 | **语法基线取语料形态（四条结构性冲突一并裁掉）+ `BIGID` 定义为自有类型（✔ 2026-09-25）** | 作者原话：「**1 语料 / 2. 语料 / 3. 语料 / 4. 按 varchar(80)?**」+「**另外 bigid 是我们特有的定义，就是指本身是 partitionID，高位存 tenantId**」：① **冲突 1 → 语料**：`@` 注解制为基线（`@Ref` 90 / `@Index` 117 / `@Id` 82；`ref ` 与 `computed` **零命中**）；② **冲突 2 → 语料**：`.ma` = **JSON**、`.mm` / `.me` / `.ms` / `.mi` = **M 语言文本**（实测 215 / 113 / 41 / 1 全文本，`.ma` 2 全 JSON）；③ **冲突 4 → 语料**：**行为住独立 `.ms`**，不内联进 record；④ **冲突 5 → `type(size)` + 尾 `?`**（`varchar(80)?`）；⑤ **`BIGID` 既非错字也非 `bigint` 别名 = 自有类型**（partitionID + 高位 tenantId），位宽与生成方待补。**P2 语法基线闸门由此解除** | 本文冲突 1 / 2 / 4 / 5 + §二-6、[`datatypes.md`](datatypes.md) §2.1 与 `BIGID` 段、[`records.md`](records.md) 顶部、[`statements.md`](statements.md) §5、[`project.md`](project.md) §1.2、[`ide/README.md`](ide/README.md)、[`glossary.md`](glossary.md) 编码与映射、`PLAN.md` §6.1 / §6.3-39 |
| 41 | **内核侧插件加载形态取 B（WASM 组件，✔ 2026-09-25）** | 作者原话：「**B**」（对应 [`ide/plugins.md`](ide/plugins.md) §12 的三选：A sidecar + JSON-RPC over stdio / **B WASM（wasmtime + component model）** / C Rust dylib）——**取 B**：**沙箱最强 + 确定性 + 跨平台分发**，且**绕开 C 的死结**（Rust 无稳定 ABI → 插件与内核编译器版本解绑）；**代价照记**：插件只能用能编译到 WASM 的语言，**文件 / 网络 / 进程必须由宿主函数按能力白名单暴露**（与内核 capability 机制同一条规矩）；**A 降为备选口子**（将来若要用以无法编译到 WASM 的语言写内核侧插件，仍走它）；**C 仍只作官方内置扩展、不对外** | [`ide/plugins.md`](ide/plugins.md) §12、本文件 §三-25、`PLAN.md` §6.3-40 |
| 42 | **`BIGID` 位布局补定（高 28 位 tenantId / 低 36 位 realId）＋ 约束分字段级 / 对象级两种形态（✔ 2026-09-25）** | 作者原话（位布局）：「`int MAX_TENANT_ID = 0x7FF_FFFF;//高28位是租户id`」「`long MAX_REAL_ID = 0xF_FFFF_FFFFL;//低36位是实际id`」；作者原话（约束，纠正助手）：「**约束一律走注解？这个不方便，你还要区分字段级和表级约束**」「**字段级约束，直接在字段后面跟着，`indexed`、`unique` 什么的，简洁**」「**如果是两个字段建立一个索引，那就得单独定义了**」。① **`BIGID` = 高 28 位 tenantId + 低 36 位 realId**（解析 `>>> 36`；`0` = 无租户、`1` 起真实租户；**realId 由分布式 ID 生成、底座 `buildEntityID` 合成**，不是 DB 生成）；真源 `D:\2026\java` 的 `Tenancy.java:15-19 / 42-44 / 85-103`；**同轮修掉两处旧布局残留**（[`records.md`](records.md) §2.4 原写「高 16 位 / 低 48 位」、`Tenancy` 旧 javadoc 的「48bits」）。② **约束分层**：字段级 = 行尾裸关键字（语料 `readonly` 1040 / `indexed` 491）、对象级 / 组合 = record 体末尾单独具名声明（具名 342 处，2 列及以上 45 处）；**撤掉「约束一律走注解」**。余：类型名写法、两套名字表封闭后的扩展白名单机制 | [`datatypes.md`](datatypes.md) §5、[`records.md`](records.md) §1.2 / §2.4 / §3、[`glossary.md`](glossary.md) §5、[`design-notes.md`](design-notes.md) §7.1、本文件 §二-1 / §二-6、`PLAN.md` §6.3-41 |
| 43 | **租户位宽确认 27 位 + `BIGID` 的语料形态实测（✔ 2026-09-25）** | 作者原话：「**27位没错**」—— 确认 `MAX_TENANT_ID = 0x7FF_FFFF` **是 27 位值、不是笔误**：**高 28 位字段中 27 位有效、bit 63 保留恒 0**（ID 恒为正 `long`）。**同轮实测（语料 378 个语言文件）**：`bigid` **0 命中**（这个名字只出现在早期文档）；语料形态 = `@PartitionID [10000,0x000F_FFFF]` 独占一行 + `addressId uint64 identity generated readonly,`（`uint64 identity` **102 处**、`identity generated` **16 处**）；`[min,max]` = **该对象在 realId 空间领的区间**（186 文件同值）→ **realId 不是整段给租户，而是每个对象领一段**（与 `MetaObject.minID`/`maxID`、`getMinEntityID`/`getMaxEntityID` 一一对应）。余：**是否保留 `BIGID` 作类型别名**（文档用名 vs 严格取语料） | [`datatypes.md`](datatypes.md) §5、[`records.md`](records.md) §2.4、[`glossary.md`](glossary.md) §5、本文件 §二-6、`PLAN.md` §6.3-42 |
| 44 | **标识共享（Identity Sharing）与分段机制（✔ 2026-09-25，作者说明 + 语料逐段实测）** | 作者原话：「**有时候我需要多个表 UNION 成视图，不想 id 冲突，所以分段**」（同日附《标识共享》文档：六个组 + 每表最小 / 最大段）。① **分段目的 = UNION 视图主键不冲突**：同一视图族的表各领一个**互不重叠的 realId 段** → 视图不需要额外加「来自哪张基础表」的标记列或前缀；② **两级划分**：**租户位（高 28 位）决定「谁的」、段（`minID` / `maxID`）决定「哪个表的」**，两者合成才是完整 `partitionId` 空间；③ **不做 UNION 的表共用默认段**：语料 **169 张表共用 `[10000, 0x000F_FFFF]`**，只有标识共享组显式细分；④ **语料实测与作者文档逐段吻合**：`Department` `[2048, 32767]` ✔、`Employee` `[32768, 0x7fffff]` ✔、`Partner` `[0x800000, 0x7fffffff]` ✔、`Contactor` `[0x80000000, 0xffffffff]` ✔（作者文档里 Tenant / Bank Account 的 `0`–`0x7FF` 段语料未见）；另记 `ProductionLoc` `[0x10000, 0x1ffff]`、`Project` `[0x800000, 0xffffffff]`、`Material` `[32768, 0x7fffff]`、`Sku` `[0x800000, 0x7fffffff]`、`Equipment` / `Workstation` `[0x20000, 0x7ffff]`、`Tool` `[0x80000, 0x7fffff]`；⑤ **六个标识共享组已登记**（Party · Organization Unit · Person / Inventory Location · Transport Location / 工装器具 / MaterialINSku / Handlable / ProductionScheduleTask）。**⏳ 同轮新开四条待裁**：段由**人工填还是设计器自动分配**、**是否需要显式声明「标识共享组」**、**段重叠是否进 `mmda check` 硬门禁**、**UNION 视图在语言里怎么写** | [`records.md`](records.md) §2.3 + §7、[`datatypes.md`](datatypes.md) §5、[`glossary.md`](glossary.md) §5、[`meta-model.md`](meta-model.md) §5、`PLAN.md` §6.3-43 |
| 45 | **`BIGID` 定为语言类型（等价 `uint64 identity partitioned`）+ 分段配置归属（✔ 2026-09-25）** | 作者原话：「**BIGID 作类型，等价于 uint64 identity partitioned，分段在 metaobject 里面配置 minId, maxId，这是真 id 去掉租户标识的**」。① **`BIGID` 保留为语言类型**（不是别名糖也不废弃）：**`uint64`（类型）+ `identity`（底座生成分布式唯一 ID）+ `partitioned`（分区标记）**；`partitioned` 是既有元对象属性 `MetaObject.partitioned`（物理表分区 + 按租户 / 段隔离查询），已在 [`meta-model.md`](meta-model.md) §5 补行；② **分段不在字段上逐表写死，而是在元对象上配置 `minId` / `maxId`**（`@PartitionID [min,max]` 是它在语言侧的声明形态，最终落 `partitionKey` + `minID`/`maxID`）；③ **`minId` / `maxId` 是「真实 id」（realId）的范围 —— 去掉租户标识之后的那部分**（比较前先 `getRealID(id) = id & MAX_REAL_ID`，`Tenancy.java:95-97`）；④ **同轮修掉旧布局残留**：[`meta-model.md`](meta-model.md) §5 正文、[`guide/quickstart.md`](guide/quickstart.md) §… 的「高 16 位 / 低 48 位」改为 28/36，[`legacy/import-from-ddl.md`](legacy/import-from-ddl.md) 标注为旧口径 | [`datatypes.md`](datatypes.md) §5、[`records.md`](records.md) §2.3、[`meta-model.md`](meta-model.md) §5、[`guide/quickstart.md`](guide/quickstart.md)、[`legacy/import-from-ddl.md`](legacy/import-from-ddl.md)、`PLAN.md` §6.3-44 |
| 46 | **类型名与约束名一律大小写不敏感（✔ 2026-09-25）** | 作者原话：「**类型一律大小写不敏感，包括那些约束，这个跟 SQL 类似**」——① **类型名不敏感**：`BYTE` = `Byte` = `byte`、`BIGID` = `bigid`；**约束名（行尾关键字与注解名）同样不敏感**：`indexed` = `INDEXED`、`unique` = `UNIQUE`、`@Ref` = `@ref`（保留 `@` 符号本身）；② **边界**：**标识符（Record / 字段 / 枚举成员这类契约名）仍大小写敏感**，按 [`naming.md`](naming.md) §1 的写法逐字一致（`WorkOrder` ≠ `workorder`），三端与一致性测试要查；③ 落点：[`datatypes.md`](datatypes.md) §2.1 大小写行、[`records.md`](records.md) §1.2、[`naming.md`](naming.md) §1 | [`datatypes.md`](datatypes.md) §2.1、[`records.md`](records.md) §1.2、[`naming.md`](naming.md) §1、`PLAN.md` §6.3-45 |
| 47 | **标识共享段的分配主体 = 架构师 / 设计师（✔ 2026-09-25）** | 作者原话：「**段是架构师、设计师分配阿**」——① **由人分配，工具不自动分配**（设计器只提供填写入口与校验，不猜测段）；② **职责切分**：**跨表 / 视图族的段规划**（哪几张表 UNION 成一个视图族、各占哪一段）属**架构师**的「边界 / 依赖方向」范畴，**单表在既定段内落地**（字段声明 `@PartitionID` + 对象配置 `minId` / `maxId`）属**设计师**的领域模型范畴；③ **程序员与 AI Agent 不私下改段**（要改回元对象配置改）；④ 由此 §五-44 的四条待裁**第 ① 条收口**，余三条（是否显式声明「标识共享组」、段重叠是否进 `mmda check` 硬门禁、UNION 视图的语法形态）仍挂 | [`records.md`](records.md) §2.3、[`meta-model.md`](meta-model.md) §5、[`datatypes.md`](datatypes.md) §5、[`workflows.md`](workflows.md) §1、`PLAN.md` §6.3-46 |
| 48 | **标识共享组 = 视图声明（视图即组）（✔ 2026-09-25）** | 作者问「**标识共享组在视图那里可否**」→ **可以，且语料已如此**：① 语料证据 —— `view person` / `view organizationunit` / `view materialnsku`（`data/models/base/*.mm:2`）、`view Maintainable`（`data/models/mes/Maintainable.mm:2`）**正是作者文档里的四个组名**；形态 = `/// VIEW` + `view <名> { 列定义 }`，**列直接写在视图里**（基础表公共列），语料 `union` / `from` **0 命中**；② **`Maintainable` 视图自带 `@PartitionID [10000,0x000F_FFFF]` + `identity generated` 主键** → 视图在语料里是**有段的第一公民**（`Person` 视图却没有，现状不一致 → 待裁 ③）；③ **实现侧缺成员概念**：`MetaView.java:20-28` = 主表 `t` + `relatives`（join）+ 列别名 + `whereCondition`/`orderBy` → 现 `view` 是 **join 视图**，**UNION 基础表无处声明**；④ **补上基础表后三条即可机器校验**：同组段两两不重叠 / 一张表最多属一个组 / 成员段落在 realId 空间内；⑤ **待裁三点**：基础表在哪声明（视图侧 `from …` 推荐 ／ 基础表侧标注）、**成员能否是视图**（作者文档里 `Party ⊃ Person ⊃ Employee·Contactor`、`Organization Unit ⊃ Department·Partner` 是嵌套的）、**视图自身要不要段** | [`records.md`](records.md) §7 + §2.3、[`datatypes.md`](datatypes.md) §5、`PLAN.md` §6.3-47 |
| 49 | **修正：视图的基础表不用新声明位 —— 就在视图定义里（`join` / `union` 子句）（✔ 2026-09-25）** | 作者指出「**你看 records.md 322 行，就有视图的定义阿**」—— 视图定义（`view OrderItemV : OrderItem as it join Order as o … { 列 }`）**早已在 §7**，我上一轮把「基础表在哪声明」当成新问题问偏了：① **基础表 = 视图定义里 `join` 子句（join 视图）／ `union` 子句（UNION 视图）中的表** —— 已有语法的自然读法，**不需要新声明位**；② §7 重排：**视图定义与例子回到最前**，标识共享块放到定义之后（不再遮挡原有定义）；③ 待裁收敛为**三条**：`union` 子句的**语法形态**（归语法专题）／ 基础表**能否是视图**（嵌套组 `Party ⊃ Person ⊃ Organization Unit`）／ **视图自身要不要段**；④ 校验三条不变（同组基础表段两两不重叠、一张表最多属一组、段落在 realId 空间内） | [`records.md`](records.md) §7 + §2.3、`PLAN.md` §6.3-48 |
| 50 | **视图的 `:` = C# 风格的继承 / 实现（不是 SQL 的 FROM）（✔ 2026-09-25）** | 作者原话：「**`view person : Employee as e union Contactor as c { … }` 这个 `:` 类似 C# 的集成、实现，语义上是对 `person` 进行定义**」——① **`:` 右端是「基」，视图 = 在该基上「定义」出来的新东西**（**不是**查询投影）；`as it` 是**引用该基时的别名**（列引用 `it.orderId`）；`join … on …` = **再挂别的基**（横向、带关联条件）、`union X as x` = **并列的基**（纵向、无关联条件）；② **语料取证**：24 个 `view` 里**唯一**带 `:` 的是 **`view Product : Bom { … }`**（`data/models/mes/Product.mm:2`），而 `Bom` 在 `Bom.mm:2` 是 **`record Bom`** → **基可以是 record**；其余 23 个 view 只写 `{ 列 }`（`union` / `join` / `from` 0 命中）；③ 由此 **「基础表」= 定义里的「基」**，仍不需要新声明位；④ **待裁收敛为三条（继承语义下）**：多个基的**列合并 / 冲突规则**（同名同类型写一次即可？同名不同类型报错？）／ **基能不能是视图**（继承链）／ **视图自身要不要段** | [`records.md`](records.md) §7、[`glossary.md`](glossary.md) view 词条、`PLAN.md` §6.3-49 |
| 51 | **视图的定义规则三条（✔ 2026-09-25 作者）** | 作者原话：「**每个表的字段都要写，并且名字要对的上，用 `as`，或者你模仿 SQL**」「**能**」「**视图可以要段，大多情况不更新，没必要段**」——① **列清单逐字段显式写出**：每个基的字段都要写（不许省略 / 不许自动推断），**名字要对得上**，基之间列名不一致时**用 `as` 对齐**；**对齐规矩照 SQL**（列按位置对齐、视图列名由别名决定）；② **基可以是视图** → 继承链允许（`view party : person`），段校验按**叶子表**做；③ **视图的段可选**：**只读视图（大多数）不写段**（「大多情况不更新，没必要段」），只有确实需要区间的视图才声明 `@PartitionID` —— 语料 `Product` / `Maintainable` 带、`person` 不带即此意。**§2.3 待裁收敛为一条**（段重叠是否进 `mmda check` 硬门禁） | [`records.md`](records.md) §7 + §2.3、[`glossary.md`](glossary.md) view 词条、`PLAN.md` §6.3-50 |
| 52 | **段重叠进 `mmda check` 硬门禁（✔ 2026-09-25 作者「同意进」）** | 作者原话：「**同意进**」—— ① **同组基础表的 `[min,max]` 两两不重叠 → `mmda check` 硬门禁（error，生成期就拦，非 warning）**（重叠 = UNION 后主键必撞）；② 同一批校验一并实现：**一张表最多属于一个组**（它只有一段）、**基础表段落在 realId 空间内**；③ **标识共享这块至此无待裁**（段分配主体 / 组放视图 / 视图定义三条 / 段重叠门禁 全部已裁） | [`records.md`](records.md) §7 + §2.3、`PLAN.md` §6.3-51 |
| 53 | **未知名 = 解析期 error（关键字表封闭，白名单方案作废）（✔ 2026-09-25）** | 作者原话：「**posittive 写错了肯定要报错，语法都不对，你咋能过。你没有词法分析器吗？**」——① **行尾关键字表 + 注解名表都是语言关键字，进词法分析器的 token 表**；**拼错 / 未知名 → 解析期报错（error）**，诊断带 `<file>:<line>:<col>`（例 `quantity decimal(18,3) posittive` → `error: 未知约束关键字 posittive`）；② **先前提的「扩展白名单走 `Profile` 还是 `customProperties`」作废** —— 没有「自定义关键字」这回事；③ **解析器位置**：`mmda-syntax`（**P2**，按内容首关键字判 partType、诊断带 `file:line:col`）—— **仓里尚无解析器实现，所以现阶段没有东西能拦拼错**（是 P2 未开工，不是设计灰区）；④ **取证口径回正（同日第二次澄清）**：**`D:\2026\java` 是现行实现（新库）**，**老库 = `D:\Java\mmda`**（作者给的路径；先是助手误读成「你看的是老库吧」→ 已改正）；`datatypes.md` §5 的真源标注与新库里的旧注释残留（`Tenancy.java:82`「48bits」、`:27`「高 28 位」）已如实写明 | [`records.md`](records.md) §1.2、[`datatypes.md`](datatypes.md) §5、`PLAN.md` §6.3-52 |
| 54 | **取证口径回正：`D:\2026\java` = 现行实现（新库），老库在 `D:\Java`（✔ 2026-09-25 作者澄清）** | 作者原话：「**`D:\2026` 的是新的，以前老库在 `D:\Java` 下面**」——① `doc/datatypes.md` §5 的真源标注由「老库」改为 **现行实现（新库）`D:\2026\java`**（入库点 `056ef96`，mmda-core 5.0.0）；② **此前全部底座取证都是新库的**，无需重取：位布局常量（`Tenancy.java:15-19`，注释**已写「高 27 位」**）、`parseTenantID`/`buildEntityID`（`:42-44` / `:85-103`）、调用点（`TenancyEntityRepository.java:100/:552/:585`、`SqlQuery.java:1270`）、`MetaView.java:20-28`、`MetaObject.partitioned`；③ **新库里两处旧注释残留**（建议改，助手未动 Java 仓）：`:82` javadoc「@param id 生成后 **48bits** 实际的 id」、`:27`「会改变主键 id 的**高 28 位**」；④ 教训：**「老库」三个字不要替作者下结论** —— 先问路径再标注 | [`datatypes.md`](datatypes.md) §5、`PLAN.md` §6.3-53 |
| 55 | **老库路径 = `D:\Java\mmda`；16+48 旧布局的真源在此（✔ 2026-09-25）** | 作者给出路径：「**老库 `D:\Java\mmda`**」—— 助手实测取证：① **老库 `D:\Java\mmda\mmda-core\mmda-core-entities\src\main\java\cloud\mmda\core\entities\Tenancy.java`**：`:14` `short MAX_TENANT_ID = 0x7FFF`（**只有 15 位有效值域**，早期文档却称「高 16 位」）、`:39` `partitionID >>> 48`、`:82-83` `entityID = tenantID & MAX_TENANT_ID; return (entityID << 48) + (id & 0xFFFF_FFFF_FFFFL)`（**低 48 位 realId**）、`:78` javadoc 写「48bits 实际的 id」；② 由此**早期文档/旧注释里的「高 16 位 tenantId + 低 48 位 realId」出处明确 = 老库**；③ **新库 `D:\2026\java` 已改为 28+36**（`>>> 36` / `<< 36`、`MAX_REAL_ID = 0xF_FFFF_FFFFL`），但 `Tenancy.java:82` javadoc 与 `:27` 注释**漏改**（老库抄过来的话术）；④ 老库**不是 git 仓**（多模块工程：`mmda-core` / `mmda-aps` / `mmda-crm` / `mmda-flow` / `mmda-gateway` …） | [`datatypes.md`](datatypes.md) §5、`PLAN.md` §6.3-54 |
| 56 | **`@PartitionID` 改名 `@Partitioned` + 字段级支持行尾 `partitioned`（✔ 2026-09-25 作者）** | 作者原话：「**`@PartitionID` 改为 `@Partitioned`，字段级支持 `partitioned` 跟在后面**」——① **改名理由**：元对象属性本来就叫 `partitioned`（`MetaObject.partitioned`，DB 列同），**注解与属性同名** —— 一个概念只留一个名字；② **两种形态（同一件事）**：**字段级行尾裸关键字 `partitioned`**（不带范围，用元对象默认段；`BIGID` = `uint64 identity partitioned` 正是它的展开式）／ **注解 `@Partitioned [min,max]`**（带段范围时用，独占一行在字段上方）；③ **旧名 `@PartitionID` 已废** —— 语料 **186 处仍是旧写法（待迁移）**；按 §1.2 的封闭关键字表，**旧名 = 未知名 → 解析期 error 并提示改名**；④ **唯一性**：一个对象只能有一个 `partitioned` 字段 → `mmda check` **error**；⑤ 落盘范围：**规范正文档 10 个文件改名 49 处**（`records` / `datatypes` / `meta-model` / `glossary` / `design-notes` / `guide/quickstart` / `quality` / `testing` / `vision` / `workflows`）；**`doc/archive/**`（历史留档）与台账各表保留旧名**（那里引的是语料原文与历史记录） | [`records.md`](records.md) §1.2 + §2.3、[`glossary.md`](glossary.md)、[`datatypes.md`](datatypes.md) §5、[`meta-model.md`](meta-model.md) §5、`PLAN.md` §6.3-55 |
| 57 | **旧名一次性迁移 = `mmda migrate --rename`（✔ 2026-09-25 作者「好」）** | 作者对「语料那 186 处旧名要不要给一次性迁移」回「**好**」——① **`mmda migrate --rename @PartitionID=@Partitioned`**（可重复传参）：默认 **`--dry-run`** 只出「文件:行:列 + 改动」清单，**`--write`** 才落盘；② **只动语言文件**（`.mm` / `.me` / `.ms` / `.mi` / `.mmda` …），**不碰生成区与 KEEP 区**；③ 与 [`naming.md`](naming.md) §5 既有的命名迁移脚本（`Impl` / 缺 `I`）**合并为同一条线**，一并归 **P9**；④ **升级必经**：解析器只认新名，旧项目不迁移就是解析期 error | [`records.md`](records.md) §2.3、[`naming.md`](naming.md) §5、[`project.md`](project.md) §格式版本 / 迁移、`PLAN.md` §6.3-56 |
