/// BOM状态
enum BomStatus : int {
    /// 新
    NEW = 0,
    /// 已起草
    DRAFTED = 1,
    /// 已审核
    CERTIFIED = 2,
    /// 已批准
    APPROVED = 4,
    /// 变更中
    REVISING = 5,
    /// 已弃用
    ABANDONED = -1,
}
